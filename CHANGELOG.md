# Changelog

Newest first. **The entry lands in the same commit as the change** — that is the whole
discipline, and why this file replaced `process/toolkit-worklog.md` (which rotted the moment
updating it became a separate chore). One line per change:
`kind(area): what and why (field evidence / bug id) — contributor or source project`.
Kinds: `new` · `fix` · `learn` (a skill/learning) · `process` (rules, templates, CI).
Credit the person or project that surfaced the change — the credit line is the thank-you.

**Release cycle (from 2026-09-19).** New lines land under `## Unreleased`, at the top. Every few
days the maintainer cuts a release with `bin/cut-release.sh`: the `## Unreleased` heading becomes
`## vYYYY.MM.DD`, master is tagged `vYYYY.MM.DD`, and a fresh empty `## Unreleased` opens. So the
top section is always "what changed since the last release", a project reports the release it is
on (`bin/sync-project.sh` prints `Toolkit release: v2026.09.22` — or `v2026.09.22-3-g<sha>` when
three commits past it), and a bug report can name a release instead of a sha nobody can place.
Sections dated before 2026-09-19 predate the cycle and stay as they are.

## Unreleased
- fix(module-review): **rubric row 8 judged the nav *bar* but never the nav *menu*.** The row
  already caught an un-skinned default nav bar and a sidebar design shipped as a top-bar app; it
  said nothing about the navigation profile's own items. The menu is chrome on every screen and
  belongs to no page, so a page-by-page LOOK pass structurally never reaches it — Arm A of the PRD
  benchmark passed a full module review and two UI sweeps with five bare text labels and no icons.
  Row 8 now names the menu items (labels, icons, order, grouping) as a finding class, says to judge
  them once explicitly on the first screen opened, and points at `learned-sidebar-collapse-icons.md`
  so the corrected icon capability is reachable from the review rubric and not only from a build
  skill. — PRD benchmark, Arm A (Maurits Visser)
- fix(learned-sidebar-collapse-icons): **the skill told sessions that menu-item icons cannot be
  scripted — they can, and have been able to for some time.** The file asserted that
  `navigation.create` "has no menu-item icon token" and that per-item icons "have to be assigned
  by hand in Studio Pro". `mxcli syntax navigation.create` on **v0.22.0** documents
  `MENU ITEM 'Label' PAGE Module.Page ICON Atlas_Core.Atlas."name"`, the quoting rule for
  hyphenated Atlas names, and `SHOW / DESCRIBE ICON COLLECTION` to browse the 366 stock icons.
  Cost of the stale claim: the PRD low-code-vs-high-code benchmark's Arm A shipped five text-only
  navigation items through a full build and two UI sweeps, because the skill said icons were not
  scriptable so nobody re-probed — the user's own verdict on the shipped nav was "lots of text ugly
  stuff". Corrected in place with the probe command, a worked example, and a note that this is the
  **capability-probe rule** failing precisely where it hurts most: a general prior written into a
  skill file is the most convincing general prior there is. Also split the two halves that were
  conflated — `ICON` places the glyph (MDL), icon-only-when-collapsed is theme CSS (and the class
  names must be read from your own `themesource/atlas_core/`, never copied from another project).
  — PRD benchmark, Arm A (Maurits Visser)
- learn(skills): **`learned-mdl-preflight.md` trimmed 7,173 → 5,311 words; `learned-stylegallery.md` demoted from baseline to on-demand (baseline pack 79,993 → 72,001 words).** Per the 2026-09-17 A/B/C freshness run: rows 2/7/10 cut to one-line RESOLVED cells with a pointer to the retest record; workflow rows 18–20 and the duplicate JUMP TO row 24 collapsed into one row 18 that routes to `learned-workflow-patterns.md` (cross-references in workflow-patterns, module-brief and the bug ledger renumbered); "MDL gotchas" cut to what the generated `CLAUDE.md` and `learned-microflow-patterns.md` do not already say; rows 23–24 (DesignProperties) rejoined to the STOP table from below a stray rule. STOP table itself unchanged — arm C kept it and matched arm B on every routing row — MendixMau
- learn(bug-logs): **`BUG-DRAFT-stale-init-claude-md-declare-object`** — a `CLAUDE.md` written by a v0.16-era `mxcli init` still lists `DECLARE $Entity Module.Entity;` as a supported statement; v0.22 `check` rejects it (MDL043/CE0053, golden capture in the entry). v0.22 `init --tool claude` no longer emits the row, but `init` overwrites instead of refreshing and stamps no version into the file, so nothing flags an old scaffold. The sigil parameter form is accepted — the freshness report's first draft over-claimed that and is corrected in the same commit. Upstream ask (paste-ready in the entry): version stamp, `init --check`, refresh-only-generated-blocks. Toolkit follow-up proposed: a report-only warn in `sync-project.sh` and a strip rule in `bootstrap-project.md` — MendixMau
- process(skill freshness): **A/B/C baseline of the four baseline-tier skills against mxcli v0.22.0 — three still change validator-blind behaviour, one does not.** `process/skill-freshness-2026-09-17.md`: 33 fresh Sonnet drafting runs (arm A no skill, B full text, C pure-deletion ablation; 3 reps) on a scratch copy of the TCX graph PoC, scored by `mxcli check`, `check-page-shell.sh`, `page-fidelity.js`, STOP-row greps and two rubric graders. `learned-mdl-preflight.md`: keep the STOP table, cut the retired-incident narratives and "MDL gotchas", move workflow rows 18–20 to `learned-workflow-patterns.md` (arm C at −37% words matched B on every routing row). `learned-microflow-patterns.md`: keep whole — arm C lost the Dto-section hint and 2/3 drafts regressed to declared object variables. `ui-preflight-pages.md`: keep; two sections are cut candidates (arm C indistinguishable from B: 3/3 top-bar layout, 3/3 H1 vs A 0/3). `learned-stylegallery.md`: no arm-B advantage on the add-a-gallery-page task (B wired the page 1/3, A 3/3) — demote that trigger to situational. Also found: the generated project `CLAUDE.md` teaches `DECLARE $Var Module.Entity;`, rejected by v0.22 (nine drafts across all arms hit it); two bundled skills contradict STOP row 6; the run cost 4.3M subagent tokens against a 0.5M estimate. Follow-up PRs listed in the report, none opened — MendixMau

- fix(sync): **`bin/sync-project.sh` now warns (report-only) when a project's `CLAUDE.md` still
  teaches `DECLARE $Var Module.Entity;`** — a row a pre-v0.22 `mxcli init` wrote and v0.22 `check`
  rejects (MDL043/CE0053), with no version stamp in the generated file to flag the drift. See
  `BUG-DRAFT-stale-init-claude-md-declare-object` in `bug-logs/mxcli-bugs.md`. Same convention as
  the neighbouring ledger-row warning: sync never edits `CLAUDE.md` (init's file, bootstrap's
  merge), it only reports and names the fix. `skills/bootstrap-project.md` now strips the row on
  merge. Field run: `bin/sync-project.sh <root> --dry-run` against a real, currently-open
  requirements-driven greenfield Mendix project scaffolded on mxcli v0.20.0 — its `CLAUDE.md`
  still carries the pre-v0.22 `DECLARE $Entity Module.Entity;` row and the warning fired exactly
  as designed, `--dry-run` confirming nothing was written. Also scaffolded a scratch project via
  `bin/init-project.sh`, wrote a `CLAUDE.md` with the stale row in a markdown table — warn fired;
  removed the row — warn did not fire; and with a digit in the module/entity names
  (`DECLARE $O Sales2.Order1;`) — warn still fires, closing the digit gap in the original regex.
  — MendixMau
- new(obligations): **added the `fidelity` obligation** — every module with wireframes owes a page-fidelity score row (`docs/PAGE-FIDELITY.tsv`, written by `project-bin/page-fidelity.js`) from stage 5; `skills/ui-loop.md` gains the scored step and a "Reachable?" check (built navigation, not deep links); promoted from the contrib inbox after the second field occurrence (#109) — Maurits Visser
- fix(project-bin/page-fidelity.js, bin/lib/obligations.tsv): **the `fidelity` obligation's `names` match could never fire — the producer wrote no module name into `docs/PAGE-FIDELITY.tsv` for the consumer to grep.** `page-fidelity.js` matched the module qualifier in `CREATE PAGE Module.Page` with a non-capturing regex group and discarded it, so a project could score every page in a module and the obligation would report `PENDING` forever. The TSV now carries a `module` column (from the page's `CREATE PAGE` statement), and `obligations.tsv`'s comment states plainly where `_ob_find`'s `names` match reads it from. New `tests/wave2/test-obligation-fidelity.sh` (subject: `bin/lib/obligation-check.sh`) proves a row naming module X discharges `fidelity/X` while an unmentioned Y stays `PENDING`, that the pre-fix (no `module` column) shape reproduces the original bug for both modules, and field-runs the real `page-fidelity.js` against the repo's own `Demo.Demo_Overview` fixture — `obligation-check.sh` correctly reads that real output as `PASS — 1 of 1 discharged`. Also carries the "three questions" → "four questions" wording fix through `ui-loop.md`, `walking-skeleton.md`, the tombstoned `ui-review-loop.md`, and every routing surface `render-routing.sh` regenerates from `bin/lib/skill-routing.tsv` — Maurits Visser

- test(sync): **`test-bug12-sync.sh` plants the retired ledger row shape-agnostically.** Its T11
  setup anchored the always-on bug-lookup row on end-of-line, so the stage-sliced three-column
  table (a `Stage(s)` cell after the path) never got the row planted and two assertions failed in
  CI; the plant now swaps only the path cell. Assertions untouched. — MendixMau
- new(routing): **baseline routing is now stage-sliced.** `bin/lib/skill-routing.tsv` gained a
  `stages` column (a comma list over `P,0-7`, or `-` for stage-independent); the
  `readme-baseline`/`baseline` render now prints a `Stage(s)` column, every-stage rows first then
  stage-specific rows in TSV order, so a project at Stage 5 can see at a glance which baseline
  files actually apply there instead of reading all of them every session. — MendixMau
- new(gate-check): **`gate-check.sh` now prints a baseline-pack-size advisory** — total word count
  of the baseline `.md` files that apply at the requested/current stage, against the existing
  budget — never affecting exit code or verdicts. `render-routing.sh --check` gained the matching
  per-stage breakdown (`stage S: N words / F files`), both sharing one new
  `routing_baseline_pack()` function so the two surfaces can't drift apart. Measured against the
  toolkit's own table: Stage 5 (the heaviest) carries 73,446 words across 23 files against the
  79,143-word/25-file full tier — 93%, i.e. the advisory mostly does not shrink what a Stage-5
  session reads; the saving is real but stage-dependent (Stage 3, for example, is 26,042/8). — MendixMau
- fix(gate-check, routing): **`routing_baseline_pack()` no longer treats an unrecognised stage
  token as if it were a real one.** Every-stage (`stages = "-"`) rows match regardless of `<stage>`,
  so a typo'd or stale token used to come back with a small, real-looking total (the every-stage
  rows only) mislabelled as that stage's whole pack, and `gate-check.sh`'s own "stage unknown,
  skipped" fallback could never fire because the function never actually returned empty. It now
  checks `<stage>` against the runbook's real tokens (`P`, `0`-`7`) and, on a miss, returns the
  FULL baseline pack with an explicit fourth field (`full`); `gate-check.sh` prints "stage unknown
  — full pack: N words across F files" instead of a mislabelled partial number.
  `gate-check.sh`'s CLI already rejects an unrecognised `REQUESTED_STAGE` before this code runs, so
  this is defense-in-depth for `routing_baseline_pack()` as a shared library function — it protects
  `render-routing.sh --check` and any future direct caller the same way. — MendixMau
- new(obligations): **added the `dispatch` obligation** (`bin/lib/obligations.tsv` +
  `agents/mdl-agent.md` step 8a, mirrored in `skills/agent-roles.md`'s mdl-agent template): a
  per-module `.claude/loop/dispatch/<Module>/scripts.tsv` log line proving a module's MDL scripting
  went through mdl-agent's preflight rather than being drafted ad hoc in the main session, where
  `learned-mdl-preflight.md`'s STOP checks never ran. PENDING when the log is absent, PASS once it
  exists — no changes needed to `obligation-check.sh`'s generic path-match logic. Column 9 (the
  obligation table's documented "governing skill" column) now names `skills/agent-roles.md`, whose
  "Workflow" step 8a is the text that actually specifies the log line's path, format and
  no-denominator discharge rule — not `agents/mdl-agent.md`, an agent stub that holds no verdict of
  its own. Note for a fresh project: `dispatch` reports PENDING until its `mdl-agent.md` stub is
  the one `bin/sync-project.sh` refreshes, and `sync-project.sh` only refreshes an UNTOUCHED stub —
  a project that customised its stub keeps whatever dispatch-logging text it had at customisation
  time. — MendixMau
- test(routing): **`tests/wave2/test-baseline-pack.sh`** exercises `routing_baseline_pack()` against
  a scratch routing table and project tree with known word counts: a stage-3-only row is present in
  the stage-3 pack and absent from the stage-5 pack (and vice versa), an every-stage row is in both,
  an `ondemand`-tier row and a non-`.md` baseline row are excluded from every pack, totals are exact,
  and an unrecognised stage token returns the full pack carrying the new `full` marker while every
  real runbook stage does not. Also field-runs the real `bin/lib/skill-routing.tsv` at every stage
  (`P`, `0`-`7`) and asserts a non-empty, non-`full` result. — MendixMau
- fix(routing): **baseline word count pinned to `LC_ALL=C`** in `render-routing.sh --check` and
  `routing_baseline_pack()`. CI (C.UTF-8) counted the tier at 80,238 words while every local run
  (C locale) put it at 78,367 — under a UTF-8 locale `wc -w` also splits on non-breaking and other
  Unicode spaces, ~1,900 words more on the same files. The 80,000 budget was calibrated on 2026-09-08
  at 73,026 words, a figure that only reproduces in the C locale, so the pin restores the calibrated
  measurement; CI's reported number therefore drops by ~1.9k against what it showed before (master
  79,993 → 78,124), no budget change. Same commit drops a duplicate "read your stage's rows only"
  paragraph from `conversion-runbook.md` — the rule line already renders inside its ROUTING block. — MendixMau
- process(bin/lib/skill-routing.tsv, docs/studio-tools.md): one additive skill-routing row for the studio_* MCP tool family, first cut deliberately narrow — `tier: experimental` (never blocks a gate, per `_routing_stage_arm`'s own design comment), `stages: -`, `agents: mdl` only (the sole agent template that references `learned-mcp-patterns.md`'s hybrid-write pattern; a repo-wide grep for the literal string `studio_` returns zero hits anywhere today, so there is no field evidence yet for which agents call these tools — this stays the conservative bound until one exists), and `group: build/mdl` (the MDL tool-reference group `learned-mcp-patterns.md` itself sits in, not `verify`). `docs/studio-tools.md` documents only that the family exists and which hard rules from `learned-mcp-patterns.md` carry over — no tool names, no external home path — because asserting either from imagined input before a real run is the instrument-shipping mistake this repo's own field-proof rule exists to stop. `bin/render-routing.sh` re-rendered `ROUTING.md` and `agents/mdl-agent.md`; `bin/render-routing.sh --check` and `bin/check-skill-routing.sh` both pass clean — this session
- fix(project-bin/exec.sh): the "Uncommitted model changes" guard refused the exec and exited before any row landed in `docs/BUILD-LOG.md` — every other blocked-exec path in the script (pre-exec `mxcli check` failure, a pre-existing mxbuild CE) writes a `🚫 blocked` row, but this guard, the one refusing to snapshot a dirty model, left the refusal invisible to anyone reading the log afterward. Fixed by relocating the existing `log_build()` helper (and its `BUILD_LOG`/`GATE_STATE`/exec-approval-suffix setup) verbatim to immediately after `cd "$PROJECT_ROOT"`, ahead of the guard chain, so guard #4 can call the same helper every other blocked path already uses — no guard's decision logic changed, no row format changed, only where the shared helper is defined. No `tests/wave2/` fixture exercises this guard (its fixture project commits everything before invoking `exec.sh`), so this was verified by reading `tests/wave2/test-bug07-08.sh` in full and diffing the change against `origin/master`: the diff is exactly the block relocation plus one new `log_build "🚫 blocked" ...` call on the actual-refusal branch, `bash -n` passes, and only one definition each of `log_build()`/`BUILD_LOG` remains — this session
- fix(project-bin/lint-gate.sh, bin/status.sh, bin/lib/artifact-manifest.tsv): lint-gate wrote its ratchet verdict nowhere lint-gate itself could be asked about later — only `.claude/lint-baseline.json` on `--update-baseline`, never a record of an ordinary PASS/FAIL run. `bin/status.sh`'s one-screen dashboard had no lint line at all, so the only way to know lint's last verdict was to re-run it. lint-gate now writes `.claude/loop/lint-last.json` (timestamp, verdict, total, per-rule counts) on every run that gets as far as computing counts — all five verdict paths (`ERROR-NO-BASELINE`, `BASELINE-UPDATED`, `PASS`/`PASS-BLIND`, `FAIL`, `FAIL-COLLAPSE`), never on the earlier exit-2 paths where there is nothing real to record, and never touching `docs/BUILD-LOG.md`'s exec table. `bin/status.sh`'s OVERDUE line now reads it, falling back to "lint gate never recorded here" when absent, and `bin/lib/artifact-manifest.tsv` gains the matching row (stage 5, consumer `bin/status.sh`). No `tests/wave2/` fixture covers either script, so this was verified by hand against a disposable scratch tree with `LINT_JSON=` standing in for `mxcli lint`: all six verdict branches produced the correctly shaped JSON, and `bin/status.sh`'s read logic was simulated against both a present and an absent file (issue #66) — this session
- process(efficiency): **Stages P–4 efficiency proposal, re-verified against master.** Three
  of its four asks had already shipped elsewhere (model tiers in `agent-roles.md` +
  `sync-project.sh --pin-models`, `bin/token-burn.sh`, `skills/small-project-tier.md`) and a
  fourth landed as prose in the runbook itself (§1c's vendor-name-free tier section) — this
  revision drops all four to an "already landed" table and keeps only what's still open: a
  proactive span read before a gate (not just PR #99's end-of-gate word count), extending PR
  #99's Stage-5 dispatch obligation up to Stages 1–4, batching Stage P/0 intake questions into
  one turn, and a pilot-one-rep rule for wide fan-outs
  — `process/stage-0-4-efficiency-proposal-2026-09-18.md` — MendixMau
- process(release): **`docs/RELEASE-PROCESS.md` + `.github/labels.yml` + `bin/sync-labels.sh` + `bin/triage.sh` + three issue templates + two PR-template checklist rows** — four intake lanes (inbox / harvest / issue / direct PR) with SLAs and labels, the five-grade triage rubric, T0–T3 test tiers, PR lifecycle (draft-only, WIP cap 8, size cap ≤10 files/≤400 lines), the Mon/Thu release train documented against `bin/cut-release.sh`'s *actual* current flags (`--dry-run`, positional version name — no `--notes` yet, listed as a follow-up alongside `bin/harvest-learnings.sh --split`), and model tiers by purpose only (no vendor names). `bin/triage.sh` is read-only, no network, Bash 3.2, and computes file age via a pure-awk proleptic-Gregorian day count to avoid the `date -d`/`date -j -f` GNU/BSD split. `bin/sync-labels.sh` applies `labels.yml` idempotently via `gh label create --force`, or prints the equivalent `curl` calls when `gh` is absent/unauthenticated. Field run: `bin/triage.sh` against this repo's real `contrib/inbox/` (23 files scanned) and `bin/sync-labels.sh --dry-run` against this repo (24 labels, gh absent here so the curl-print path exercised) — MendixMau
- fix(doctor/_common): three Windows onboarding defects from one field run (Git Bash, Studio Pro 10.24 + 11.8 + 11.11 side by side, project shared with a Dev Container). (1) `check-scripts.sh` reported FAIL on a project without `bin/` — now a WARN pointing at init-project. (2) A project's `./mxcli` can be the Dev Container's Linux ELF binary, which Git Bash can never run and `chmod +x` cannot fix — new `find_project_mxcli` prefers `mxcli.exe` on Windows and refuses an ELF-only mxcli outright; doctor `--install` fetches `mxcli.exe` beside it instead of refusing; `exec.sh`, `lint-gate`, `review-module`, `conformance-check`, `fixture-manifest`, `page-scope` all resolve the binary through it. (3) The macOS-only save-sp/restart-sp section is a note, not a WARN, off macOS. Plus `toolkit.env` — `<project>/.claude/toolkit.env` or `~/.mxcli-toolkit.env`, KEY=VALUE, Windows paths pasted as-is — loaded by `_common.sh` for MENDIX_APP / JAVA_HOME / MXBUILD_PATH / MXCLI_VERSION / PYTHON; doctor reports which loaded, self-gitignores the project file, flags a JRE-without-javac, and its gate/verdict wording now guides instead of alarms. (Rebuilt on current master, 2026-09-16: the Program-Files-glue defect this line originally also claimed is dropped here — Marco Keijsers' #59, already on master, fixed the same `find_sp_app` discovery a different way; this rebuild keeps only its own `seen`-based root de-dup, which solves a distinct problem, the same physical Program Files root listed twice under `ProgramW6432` and `PROGRAMFILES`.) The original "Fixture-proven here (glue, precedence, .exe-over-ELF)" claim on this line had no fixture behind it; `tests/wave2/test-common-windows.sh` (10/10) now actually proves the `toolkit.env` precedence order (env > project file > user file), the Windows-path conversion, `mxtk_is_elf`, and `find_project_mxcli` preferring `.exe` over a same-directory ELF on a stubbed Git Bash platform. No real Windows field run yet for this rebuild; it stays draft until one is cited here — Clinton Chee, via Maurits Visser
- new(bin/doctor.sh, project-bin/_common.sh, project-bin/exec.sh): **a "Gate self-test" section that proves the mxbuild gate can actually SEE an error**, not just that mxbuild is present and runs — every other doctor.sh section only answers the latter. `exec.sh`'s run+read logic (spawn mxbuild, portable timeout, read `--write-errors`) is factored out of its three call sites into one function, `mxtk_mxbuild_error_count` (`project-bin/_common.sh`), with no behavior change (`tests/wave2/test-bug07-08.sh` still 28/28, including the mxbuild-exit-nonzero-no-file and mxbuild-absent discriminator cases). `doctor.sh` now runs that same function twice against a scratch copy of the real project model — once clean, once with a deliberate CE0117-shape type mismatch injected via the project's own mxcli — and requires the count to go from a real 0 to a real >=1; **a "?", an unmoving 0, or a dirty baseline that already reads non-zero before the injection (so the two reads never move and prove nothing) each fail loudly** instead of reporting green — the self-test itself is now proven able to fail, not just able to pass. Full run only (skipped under `--quick`, forceable alone via `--gate-selftest`), bounded by `DOCTOR_GATE_TIMEOUT` (default 300s), scratch dir removed on every exit path. Written after Marco Keijsers' #59 and the July wrong-arch-mxbuild incident, where a gate silently reported 0 errors for several commits before anyone noticed. Fixture-proven with stubs only (`tests/wave2/test-doctor-gate-selftest.sh`, 28/28 across 10 cases: healthy/no-file/always-clean/slow mxbuild behaviors, the no-mxbuild and `--quick` cases, an mxcli that refuses the injection (reported NOT RUN with mxcli's reason, never a false "blind" FAIL), a constant-non-zero mxbuild stub that proves the dirty-baseline check actually fires on a known-bad-shaped input, and a two-tree project (`.mpr` under `app/`, mxcli at the project root) proving MPR discovery, the scratch copy, the mxcli injection and the mxbuild gate all still resolve correctly when the model is not at the project root). The shared run+read function takes a timeout that `exec.sh`'s real gate leaves **unbounded** by default (`MXTK_GATE_TIMEOUT` to set one — the header and inline comments previously implied a 300s default applied there too, which was never true from any of `exec.sh`'s own call sites, now corrected to say so): a big model's deploy build legitimately runs long, and a timed-out gate would read as "non-zero exit, no errors file" = FAIL = auto-restore of good work; only doctor's throwaway copy gets the 300s clock (`DOCTOR_GATE_TIMEOUT`). Field run, by hand, against a scratch project built under the scratchpad from this repo's own stub-fixture pattern (synthetic `.mpr` + `mxcli`/`mxbuild` stubs, same convention as `test-bug07-08.sh`, never a real client project): a healthy mxbuild stub reads `gate-selftest: pass (baseline=0 known-bad=1 2s)`; a constant-non-zero mxbuild stub (a gate that would report the same count regardless of the injection — the exact blind-gate shape this fix targets) reads `gate-selftest: fail (dirty baseline: 1)`, with zero occurrences of "gate-selftest: pass" in that run's output — the fixed self-test correctly refuses to certify a gate that cannot prove it saw anything move — merge desk, after Marco Keijsers' #59 and the July wrong-arch mxbuild incident

- new(process/kiro-same-repo-two-editors-2026-09-17.html, roadmap.md): **the developer-level answer the other two Kiro pages skipped — "is it just a VS Code plugin, and what would I do differently on Monday?"** No: Kiro is a separate application forked from the open-source VS Code codebase, so same window, same file tree, same built-in terminal, Open VSX instead of the Microsoft marketplace. Which means for a toolkit project the answer is almost nothing changes: one figure shows two editors at the top each reading their own pointer file (`CLAUDE.md`/`AGENTS.md`/`.cursorrules` vs `.kiro/steering/mxtk-toolkit.md`) and, **one line down, an identical everything** — one clone, one `PROJECT.md`, `bin/init-project.sh`, `./mxcli` + `bin/exec.sh`, `bin/gate-check.sh`, one `.mpr` — because the process lives in markdown and shell, not in an editor. Side-by-side day-one command columns make the delta literal: `wire-agents.sh . --with-kiro` instead of `wire-agents.sh .`, and `kiro .` instead of `code .`; every other line is the same text in the same order. The what-actually-changes table keeps the one row that bites in view — run permissions are granted per machine in the user's home folder, so a clone ships the instructions but never the allowlist. Commands and layout are read from this repo's own scripts; the Kiro column is second-hand and the page says so. Added to RM-01's asset table as the third page. — MendixMau
- new(process/roadmap.md, README.md, CONTRIBUTING.md): **a roadmap file, so work we want but are not building yet stops decaying in draft PRs and chat logs.** The toolkit had nowhere to park a designed-but-untriggered idea: it became a stale draft PR, an inbox drop that reads as a proposal, or nothing. `process/roadmap.md` gives it a home with a deliberately strict entry format — `Status` · `Assets` (by path, what a future session picks up) · **`Start when`** (a concrete trigger, never "when we have time") · **`Exit bar`** (checkable) · `Explicitly out of scope`. The last four are the load-bearing fields: an entry with no trigger never starts and one with no exit bar never finishes, which is exactly how `process/toolkit-worklog.md` rotted. Seeded with **RM-01 — Kiro supported as a seat, then as an enforcement layer**, which parks this branch's work rather than merging or dropping it: *Phase A* (a fifth seat; a seat owns exactly one of the six boxes in a build step, so the integration is one generated pointer file — starts when someone has a real Kiro install, exits on the field run this branch has owed since 2026-09-16 plus the corrected allowlist path and the `install-harness-permissions.sh` row) and *Phase B* (the five hook points mapped to the five toolkit rules each could make mechanical instead of requested — `preToolUse` refusing a bare `./mxcli exec` so no run happens without a snapshot, and `stop` refusing to end a turn on an `UNSYNCED` drift marker, are the two worth having; starts on **two** field runs, not one, because a hook firing on every tool call is not something to generalise from a single session, and exits against this repo's blocking-instrument rules 6 and 7 — accepts evidence it did not create, never blocks its own remedy). Two things are written down as **refused, not deferred**: spec mode owning Stages 2–4 (two of the four ✋ gates, and two requirement sets with no answer to which one a gate reads) and hand-written native agent definitions (a second copy of the six roles, against this repo's pointers-never-copies rule). The existing `process/kiro-fit-*.html` and `process/kiro-in-the-loop-*.html` pages are cited as the entry's design, not summarised into it. Routed from the README directory tree and a new CONTRIBUTING section that says which ideas belong here versus in the inbox — a roadmap nobody can find is the unrouted-skill defect in another costume. — MendixMau
- new(process/kiro-in-the-loop-2026-09-17.html): **companion visualisation to the fit assessment — how a Kiro seat, the 9-stage pipeline and `./mxcli` would actually share one build step, and what that is worth.** Written because the fit page answered *should we* and the follow-up question was *how would it even work*. Three hand-drawn SVG figures, one claim each: (1) a three-lane swimlane of one build-plan step — pipeline issues the step, the seat drafts the MDL, `mxcli check` then `bin/exec.sh` (snapshot · exec · mxbuild) write it, control returns to the pipeline for the `PROJECT.md` drift-sync mark and the ✋ gate, with a dashed CE-error return arm — which makes the load-bearing point visible: **a seat owns exactly one of the six boxes**, so swapping seats changes drafting and nothing else; (2) Kiro's five documented hook points (agentSpawn, userPromptSubmit, preToolUse, postToolUse, stop) against the toolkit rule each *could* make mechanical instead of requested — `preToolUse` refusing a bare `./mxcli exec` so no run happens without a snapshot, and `stop` refusing to end a turn on an `UNSYNCED` drift marker, are the two worth having — captioned as a hypothesis, because none of it is written and none of it has been run; (3) three postures side by side, differing only in who owns the middle row: **A second seat (adopt — the steering pointer is the whole integration), B hooks as an enforcement layer (park until field-proven; it is a blocking instrument, so it must accept evidence it did not create and must never block its own remedy), C spec mode owning Stages 2–4 (refuse — trades four ✋ gates and one decision register for a vendor's spec folder).** Issues stated as issues: the workspace allowlist lives outside the checkout so a clone cannot ship one, native agent definitions would be a second copy of the six roles this repo's own rule says must be pointers, credits are metered per spec run and nobody has measured a stage, and — the gating one — nothing here is field-run and the source docs are still unreachable from the sandbox. — MendixMau
- new(process/kiro-fit-2026-09-17.html): **the Kiro fit assessment behind this branch's steering pointer, written down instead of left in a chat log.** Verdict: support Kiro as a *consumer* — the pointer is the whole integration and it costs the toolkit nothing — but do not adopt it as the *process*: its spec mode owns requirements → design → tasks, which is Stages 2–4 of the runbook (Requirements, Architecture & Design, Build Plan) and two of the four ✋ gates, so letting both run gives a project two requirements sets and only one of them gated. Three hand-drawn SVG figures carry the argument rather than prose: the layer stack (six IDEs → committed pointer files → `CLAUDE.md` → process layer → `./mxcli`/Studio Pro → the `.mpr`, with spec mode forking off Kiro and stopping short of the gates), where each harness's run-permission actually lives (committed vs machine-only), and the stage-overlap strip. **One finding contradicts this branch and must be fixed before it flips to ready:** Kiro 1.0 keeps workspace permission rules in `~/.kiro/workspace-roots/<hash>/permissions.yaml`, outside the checkout — so the steering preamble's pointer at `.kiro/settings/` looks wrong, and Kiro belongs with Cursor and Windsurf in `install-harness-permissions.sh` (document, never write), not with Claude/Copilot/Aider. Held to this repo's own bar the page is a claim, not a check, and says so in its own caveat section: kiro.dev is still unreachable from the sandbox (same wall the pointer hit), everything is secondary-source, and the 30-minute field run the draft PR has always owed is still owed. — MendixMau
- new(wire-agents.sh, interview-protocol.md): **opt-in Kiro (kiro.dev) steering pointer, `--with-kiro`.** Requested by customers already running Kiro alongside Claude Code on toolkit projects. `mxcli init` has no `--tool kiro` target, so unlike the default six tools the file is written and stamped by `wire-agents.sh` itself: `.kiro/steering/mxtk-toolkit.md` (`inclusion: always` frontmatter, then a short Kiro-only preamble, then the same shared "Start here" marker block every other pointer carries). It's opt-in, not default, because it is a new file and a new `.kiro/` directory that only matter on a project someone actually opens in Kiro. The preamble states the one Kiro-specific hazard: Kiro auto-enters its own "spec mode" (`.kiro/specs/<feature>/…`) unasked, which on a pipeline project would create a second, ungated requirements artifact — the file tells it not to, and points at `PROJECT.md`/`analysis/**/*.brd.json`/`bin/gate-check.sh` instead. `interview-protocol.md` §3 names Kiro in its non-Claude-agent row (plain numbered markdown, then stop — Kiro's own blocking `userInput` prompt is optional); kept to two added words to stay inside the baseline word budget, measured (CI has counted `LC_ALL=C` since 2026-09-19) at 78,962 words on master and 78,964 on this branch against the 80,000 ceiling. **Documented behaviour, not yet field-run in Kiro itself** — kiro.dev was unreachable to this session — draft until one real Kiro session confirms the steering file loads and its capability-grant note is enough to unblock `./mxcli`/`bin/*.sh`. New fixture `tests/wave2/test-wire-agents-kiro.sh`, 12/12 asserts (default run touches no `.kiro/`, `--with-kiro` writes a loadable file, re-running is idempotent and keeps a hand-edited preamble line, `--dry-run` writes nothing, `--check` reports it only when relevant, and `--with-kiro` still writes the file with no mxcli reachable at all) — MendixMau
- process(bin/lib/skill-routing.tsv, bin/lib/install-manifest.sh): wire `project-bin/app-facts.sh` — the read-only facts-collection instrument `split/93-app-layer-map` ships — into the installer allowlist and the routing table. Originally this branch also carried the routing rows for the four `app-analysis` skill files (`app-analysis`, `layering-review`, `module-dependency-review`, `microflow-loop-antipatterns`), reasoning that `bin/lib/skill-routing.sh`'s `routing_missing_paths()` only warns, never fails `--check`, when a row names a file that doesn't exist yet on this branch. That reasoning covered only one direction: `routing_unrouted_skills()` runs independently and DOES fail `--check` when a `skills/*.md` file exists on disk with no routing row — and those four skill files ship on `split/93-app-layer-map`, not here — so CI on that sibling PR (#117) went red. Fixed by moving the four skill rows onto `split/93-app-layer-map` itself, where the skills live; only the `app-facts` row stays here, since `project-bin/app-facts.sh` is a script, not a `skills/*.md` file, and so isn't subject to that audit — the file not existing on this branch yet is the safe, warn-only case. Regenerated via `bin/render-routing.sh` only — `README.md`, `ROUTING.md`, `agents/architect-agent.md` and `agents/review-agent.md` pick up the `app-facts` row (its roles are architect/review only, so `agents/ba-agent.md`, `agents/mdl-agent.md` and `bin/gate-check.sh`'s stage map are unaffected, since `app-facts.sh` isn't part of any stage's skill list) — never hand-edited inside the `<!-- ROUTING:BEGIN -->` markers. `bin/render-routing.sh --check` reports in sync on this branch alone (`NOTE: routed but not on disk yet: project-bin/app-facts.sh`, non-fatal). `tests/wave2/test-bug03-gates.sh` (13/13) run against this branch's `bin/gate-check.sh` confirms its hand-written gate logic is untouched. Split from PR #93 — Maurits Visser
- new(app-analysis): **a standing app dossier for existing apps.** `skills/app-analysis.md` is the umbrella: sections, thresholds as named constants, verdict rules, and refresh discipline, alongside `skills/module-dependency-review.md` and `skills/microflow-loop-antipatterns.md`. The read-only instrument `project-bin/app-facts.sh` forces a full catalog build and refuses fast-mode or schema-incomplete catalogs with exit 2; module edges go through `refs.ModuleName` joined to `objects.ModuleName`, because the stock `graph_module_*` views split on the first dot and invent a phantom Navigation module; strongly connected components replace raw cycle enumeration, since one probe app had 59 bidirectional pairs and 720 cycles of length four or less; every loop-containing microflow, nanoflow and rule is described in parallel and its loop bodies parsed for retrieve, commit, delete, REST, calls, nested loops and transaction control, with scheduled-event reachability. `bin/app-report.sh` renders `architecture/app-dossier.md` plus the facts to `analysis/app-report.html`, where an instrument fault always beats a dossier pass and a missing dossier section counts as a fault. Two platform facts learned on the way, both now in the skills: the catalog holds top-level activities only (a nested loop, and everything inside a loop body, is invisible to it and to every Starlark lint rule), and `mxcli describe microflow` reports not-found for nanoflows and rules, so the instrument describes by qualified name without a type, and rules (no `describe rule` exists) are counted as `rules_not_describable` rather than failures. The parser reads both `loop ... end loop` and the while loop `while ... end while` (the only two real parser misses on a second app were while loops); parsed-more-than-catalog is reported as `catalog_undercount`, parsed-fewer is the only `parse_mismatch`. Scheduled-event reachability is split into enabled and disabled events (24 of 40 events were disabled on the R&D app). Modules named in the project's `.claude/lint-vendor-modules.txt` are classed as marketplace, so vendor modules imported without metadata stop inflating the own-module tangles. Security posture, lint baseline and dead-element confirmation are fault or manual placeholders in this version. Measured on four local projects (62 to 2,700 microflows) and one 107-module R&D app: 178s end to end with 6 workers, 423 of 425 loop documents described, the two remaining being rules. Fixture `tests/wave2/test-app-report.sh` over `fixtures/app-analysis`. The first render of a real app proved the report unreadable: 1,811 rows over 110 printed pages, every section verdict reading `fail` for the one reason that a first dossier has no dispositions yet, and the only prioritised list buried under a 693-row dead-element dump. So the page now carries two independent axes. **Health** (`ok`, `watch`, `at risk`, `unknown`) is scored from facts alone and is identical whether a dossier exists or not; **review progress** (`not started`, `in progress`, `complete`) measures the document. The five-word section verdict vocabulary is untouched and keeps answering its own question, with a legend saying which question that is. **Severity** is a new per-finding axis (`critical`, `high`, `medium`, `low`) from pattern class plus scheduled reach (enabled +2, disabled-only +1) plus blast radius (+1 at `WIDE_BLAST_INBOUND`), aligned with the severity and priority model used in the app review engagements. The page opens with a fixed-shape executive summary: what the app is, the health line, severity tiles, a five-row fix-first table with a recommended fix per pattern, review progress, and what was not measured. Every table over 12 rows folds, including tables written inside the dossier markdown, which took the same app from 1,811 visible rows to 103 and from 110 printed pages to 32. `app-report.json` lands beside the HTML and the same object is embedded as `app-report-data`, so an agent reads numbers instead of parsing 27 tables. Known weak spots recorded rather than hidden: the medium band does not yet discriminate, blast radius is module-level where per-asset in-degree would be better, disposition matching is by substring so a renamed document silently reads undecided, and health has no history to compare against. Then a reader met the restructured page and said they still did not understand it, asking what a loop body was, which is the second defect: structure was fixed, language and meaning were not. **The page now explains itself.** `bin/app-report.sh` carries a `TERMS` table with a reader-facing sentence for every code it can print, renders it as a glossary section and as a plain-language name beside the code in every finding table, and opens the loop section by defining a loop body before using the word; the test fails if any code can reach the page without a sentence. **Zero is a result and is now stated as one:** eight checks render as rows whether they found anything or nothing, each with its count, its method and what the number means, because the two patterns that actually take a Mendix runtime down, a web service call inside a loop and transaction control inside a loop, were both absent on the R&D app and rendered as unlabelled `0` tiles among fourteen, so a reader counting 229 findings concluded the app was on fire. The health line now states proportion as well as count. **The severity model discriminates.** Three causes of the one-band shrug, all fixed: blast radius counted reference EDGES and so told the reader a module in a 73-module app was "depended on by 1707 others", and it fired for 43 of 73 modules, so it now counts DISTINCT own modules at `WIDE_BLAST_MODULES` 10, which 8 of 73 clear; `LOOP_NESTED` was its own finding although a loop inside a loop with nothing in either body costs nothing, so it is an amplifier of the query or save in the same microflow, never a row; and one database query per item weighed the same as a web service call per item, so the per-turn weights are now 4 and 1 with an enabled scheduled event worth +3. `AMPLIFIER_CEILING` (3) is what makes the bands mean anything: nesting, repetition and blast radius never reach `high` on their own, so a finding is high or worse only when an enabled timer runs it today or every turn costs a network round trip or a transaction, and a switched-off timer is a loaded gun worth a medium. A tangle covering `BIG_TANGLE_PCT` (25) or more of the own modules gets a further +1 and its own callout above the fix-first table, because 52 of 73 modules that can all reach each other is a decision about how the app is built, not a row like the other 228. On the same R&D app the page went from 0 critical / 14 high / 187 medium / 28 low over 229 findings to 2 critical / 5 high / 109 medium / 52 low over 168, which reads as six loop findings worth opening plus one structural decision, and it says so in those words. Fixture grown to thirteen invented own modules, a ten-module tangle, a hub module and a loop carrying all three amplifiers; `tests/wave2/test-app-report.sh` 80 checks to 109. Still weak: per-asset in-degree is collected for the top 40 assets only, so blast radius stays module-level; the facts carry neither entity event handlers nor the size of the list a loop iterates, both of which would sharpen the medium band further and both of which need `project-bin/app-facts.sh`; the medium band still holds 109 findings on this app, honestly a backlog but not yet ordered within itself; disposition matching is still by substring; and health still has no history to compare against. Split from PR #93 into its own branch (`split/93-app-layer-map`): this entry carries the instrument, skills, fixtures AND their routing. The four `skill-routing.tsv` rows for `skills/app-analysis.md`, `skills/layering-review.md`, `skills/module-dependency-review.md` and `skills/microflow-loop-antipatterns.md` ship in this branch too — a skill file on disk with no routing row fails `bin/render-routing.sh --check`'s unrouted-skill audit, so the routing has to travel with the skill it names, not with the sibling gate-wiring branch. Rendered surfaces (`README.md`, `ROUTING.md`, `agents/architect-agent.md`, `agents/ba-agent.md`, `agents/mdl-agent.md`, `agents/review-agent.md`, `bin/gate-check.sh`'s generated stage-map block) are regenerated via `bin/render-routing.sh` in the same commit — never hand-edited inside the `<!-- ROUTING:BEGIN -->` markers. Only `project-bin/app-facts.sh`'s routing row and its `bin/lib/install-manifest.sh` allowlist entry still ship separately in `split/93-gate-changes`, since `app-facts.sh` is not a `skills/*.md` file and so is not subject to that same audit. `tests/wave2/test-app-layer-map.sh` (16 checks) is new in this split, pinning `bin/app-layer-map.sh`'s tangle/mismatch detection, back-edge weight+kinds, run-to-run determinism, and degrade behaviour — #93 shipped the instrument without a fixture for it. Maurits Visser, loop pattern vocabulary from Stefan Gareis's production support analysis.
- new(docs): three planning docs land — `docs/toolkit-roadmap.md` (the numbered backlog table,
  its ordering rationale, and a field run recording what shipping the gate-check mode parser
  (1.2a) actually taught, including a real false-WAIVED hazard from unanchored substring
  matching); `docs/existing-app-mode-design.md` (the existing-app entry-mode design note);
  `docs/architecture-views-design-note.md` (a proposal plus one working prototype for
  architecture-stage report views — explicitly not yet merged into `bin/app-report.sh`).
  Caveat: the roadmap table originally credited PR #92 as shipped/half-fixing item 1.9; #92 is
  not yet merged to master as of this split, so the two status cells and the field-run narrative
  were reworded here to say so explicitly rather than document a fix the repo does not have yet
  — MendixMau
- new(bin/exec-approval.sh, bin/install-harness-permissions.sh): `auto` is now the unconditional
  exec-approval default everywhere, not just Claude — **already-wired projects that never set the
  knob silently move from "ask before each exec" to "run it" on their next toolkit pull.** One
  rule text (`bin/lib/wiring-item3.sh`) now backs every mention (interview-protocol.md,
  toolkit-guide.html, README, intake-template.sh); new `install-harness-permissions.sh` extends
  the Claude-only allow-lister to Copilot and Aider (Cursor/Continue/Windsurf are user-level
  IDE/CLI settings, nothing to write); `sync-project.sh` repairs projects wired under either
  earlier wording. `tests/wave2/test-exec-approval.sh` (43/43), `test-install-claude-permissions.sh`
  (66/66). Field run: scratch project scaffold — `init-project.sh` → `exec-approval.sh --explain`
  resolves `auto` from default → `sync-project.sh` idempotent on re-run. (MendixMau)
- new(gate-must-run): **the mxbuild gate could silently not run and the model still got written,
  committed and pushed — on any machine, cloud or session.** `project-bin/exec.sh` treated
  `GATE_STATE=skipped`/`unverified` as exit 0, so a `count()`-as-expression microflow (CE0117,
  invisible to `mxcli check`, visible only to mxbuild) reached a customer's `master` from a
  cloud session where mxbuild had never been downloaded. PR #49's Windows fixes repaired the
  gate's *symptoms* (path mapping, errors-file location, output capture) but left the class
  intact: an unverifiable gate still wrote. Four layers, each mechanical, none of them a
  convention: (1) write time — `mxtk_ensure_mxbuild` in `_common.sh` runs
  `./mxcli setup mxbuild -p` when discovery fails, and `exec.sh` **refuses to write** when the
  gate still cannot run (`ALLOW_UNVERIFIED=1` to override, `MXTK_NO_INSTALL=1` to skip the
  download); (2) commit time — new `project-bin/model-stamp.sh` fingerprints the `.mpr` +
  `mprcontents/` (git blob hashes, working tree and staged), `exec.sh` writes a pass stamp on
  a green gate and clears it otherwise, new `project-bin/verify-model.sh` is the standalone
  gate that also stamps, and `project-bin/install-project-hooks.sh` installs a pre-commit
  hook that refuses a commit touching the model unless the **staged** model is stamped, or
  other evidence the hook did not itself create shows the model was verified nearby
  (`MODEL_UNVERIFIED_OK=1` overrides once; the hook chains any pre-existing hook and never
  blocks the remedy — rules 6/7); (3) session start — new `project-bin/session-check.sh`
  (stamp state via `find_mpr` so single-tree and two-tree checkouts are both probed,
  doctor-receipt freshness, stale installed scripts, hook presence) wired as a
  Claude Code `SessionStart` hook by `bin/install-claude-permissions.sh`, which now also adds
  the one `permissions.deny` for bare `mxcli exec`; `sync-project.sh` installs the hook and
  reports the stale-script class platform-neutrally; (4) discipline — the generated
  `CLAUDE.local.md` wiring block says every model write goes through `./bin/exec.sh`, and
  `gate-check.sh` notes an unverified model on disk, with a route to clear the note when the
  model is gitignored (see the fix below). `init-project.sh` installs the hook at scaffold.
  Field run on a two-tree field project (`.mpr` under `app/`, root symlinks): the stamp
  fingerprints that `app/*.mpr` + `app/mprcontents` identically from the working tree and the
  index; `verify-model.sh` ran mxbuild in 63 s, 0 errors, stamp written and self-gitignored;
  the hook refused a staged unit change and let the same commit through with the override; with
  an empty `$HOME` the gate downloaded the 818 MB 11.14.0 toolchain itself and then ran clean;
  `exec.sh` with mxbuild unreachable refused before the snapshot, wrote nothing, and logged a
  `refused` BUILD-LOG row. `tests/wave2/test-model-stamp.sh` (27 assertions: fingerprint
  equality across working tree/staged/symlinked paths, hook refusal and override, foreign-hook
  chaining, idempotence, settings deny + SessionStart install/uninstall) plus
  `tests/wave2/test-install-claude-permissions.sh` (deny entry + SessionStart hook present after
  install, byte-identical settings file after uninstall). `test-bug07-08.sh` case H flipped from
  "mxbuild missing → UNVERIFIED, exit 0" (the very contract that let the CE0117 through) to
  "→ refused, exit 1, no snapshot", with the old expectation kept as case H2 behind
  `ALLOW_UNVERIFIED=1`. Overlaps draft #76 (doctor `--gate-selftest`) in intent, not in
  code paths. — Maurits Visser
- fix(project-bin): **`model-stamp.sh` fingerprinted an empty set as a constant hash, and
  word-split the model paths; `install-project-hooks.sh`'s pre-commit refusal accepted only its
  own stamp file, and the gate-check UNVERIFIED note had no way to clear on a gitignored
  model.** Three bugs in the guard whose whole job is to prevent green-by-absence: a `.mpr` with
  a space in its name (`My App.mpr` is routine) was split into two pathspecs that matched
  nothing, so edits to the `.mpr` alone left the fingerprint unchanged; a clone whose model is
  gitignored hashed empty stdin — `e3b0c4…`, the SHA-256 of nothing — so every stamp matched
  every model state forever, and `fingerprint`/`check -q`/`write` all returned `rc=1` there, so
  `gate-check.sh` printed the UNVERIFIED note with no in-tool remedy (CLAUDE.md "Shipping an
  instrument" rule 6: never only the guard's own stamp file); and the pre-commit hook refused a
  commit touching a gitignored model outright, which is exactly the guard blocking the action
  that resolves it (rule 7). Paths now travel through an array; an empty path set or an empty
  file list refuses (`rc=1`) instead of hashing, and `write` computes the fingerprint before
  touching the stamp file so a refusal never leaves an empty `fingerprint:` field behind. The
  pre-commit hook and `gate-check.sh` now also accept evidence they did not themselves create —
  a commit in the last 24h that touched the project (`git log -1 --since=24.hours`), or a
  handoff doc (`PROJECT.md`/`docs/brain/*.md`) modified in the last 24h — and print `VERIFIED
  (external evidence)` rather than staying stuck; that path warns once in the commit/gate output
  and then lets the commit through, per rule 7. Reproduced on a throwaway repo with a
  space-named `.mpr`: pre-fix, an `.mpr`-only change kept the same fingerprint and the
  gitignored tree returned the empty-input hash with `rc=0`; post-fix, the change is detected,
  both empty cases refuse, and a gitignored model with a same-day `PROJECT.md` edit clears the
  note instead of blocking forever. `exec.sh` also no longer swallows the exec's real exit code
  when `model-stamp.sh clear` fails on the failure path (an `[ -x … ] && …` AND-list under
  `set -e`). The three file headers that asserted rule-6 compliance outright
  (`model-stamp.sh`, `install-project-hooks.sh`, `verify-model.sh`) now describe the
  evidence path instead of claiming it unconditionally. `tests/wave2/test-install-claude-permissions.sh`
  gained the deny-entry, SessionStart-hook-present-after-install and
  byte-identical-settings-after-uninstall assertions that previously lived only in
  `test-model-stamp.sh` T8 — this fixture is the one that owns `install-claude-permissions.sh`
  — Maurits Visser
- fix(bin/gate-check.sh): a single-stage query (`gate-check.sh . N`) that resolves the stage
  the `## Current stage` readout currently names to PASS or WAIVED now advances that readout,
  instead of leaving it stale until the next full run. Before this, `gate-check.sh . 3` passing
  Stage 3 left the register still reading Stage 3 (or worse, whatever the last full run wrote)
  until someone remembered to re-run the plain `gate-check.sh .` — nobody's normal next move
  after a stage query is "now also run the full gate-check just to move the header." The fix
  reuses the existing readout-rewrite block and its "never overwrite a hand-written line" guard
  unchanged, adding only a narrow eligibility check: the query must target exactly the stage the
  readout already names, and it must have just resolved PASS/WAIVED — a query about any other
  stage, or one that hasn't resolved yet, stays as read-only as it always was (issue #108)
- fix(bin/gate-check.sh, skills/design-artifacts.md): the Stage-3 wireframe check only confirmed
  `design/wireframes/*.html` was non-empty, so a 22-screen app with one wireframe read as done —
  `skills/design-artifacts.md` promises one wireframe per screen. Where `design/target-ui.md`
  carries the `## Screen Inventory` table that Step 2 now defines, the gate counts it against
  `design/wireframes/*.html` (a Notes cell recording reuse/same as/shares excuses a row from
  needing its own file) and reports `MANUAL <built> wireframes for <inventoried> inventoried
  screens` when the build falls short; absent the table, behaviour is unchanged (presence-only).
  Verified with `tests/wave2/test-wireframe-count.sh` (5/5) — issue #107
- process(bin/check-pr-discipline.sh): **a new CHANGELOG entry must end with its credit segment.**
  Third check over the merge-base diff: every added `- kind(area):` entry, joined with its 2-space
  continuation lines, must end ` — <credit>`; rule 1 only saw that CHANGELOG.md was touched, so a
  review pass over 18 open PRs found headline-only entries riding through. Field-run over eight open
  PR branches (all clean) and a one-shot audit of master (18 of 256 entries uncredited, 4 of them
  with an ASCII `--` separator; only added entries are checked, nothing is retro-blamed). Six
  fixture cases in `tests/run-tests.sh` — MendixMau

- fix(bootstrap-project.md): **`CLAUDE.md` is a pointer when `CLAUDE.local.md` exists, never a
  second copy.** Step 2 told the merge to paste the Baseline routing table into `CLAUDE.md` even
  on a project `bin/init-project.sh` had already scaffolded — where it writes that same table into
  `CLAUDE.local.md`, refreshed by `bin/sync-project.sh` — so every session loaded the identical
  block twice, and the `CLAUDE.md` copy had no sync to keep it current. Step 2 now branches on
  whether `CLAUDE.local.md` exists: if it does, `CLAUDE.md` gets a 3–5 line pointer and nothing
  else; if it doesn't, the old verbatim-copy behavior stands. Step 3 and the Anti-Patterns list
  route the same way. Field-run: scaffolded a fresh project with `bin/init-project.sh`, wrote a
  `CLAUDE.md` carrying a duplicated Baseline block, then replaced it with the new pointer text
  per the rewritten Step 2. — MendixMau
- fix(sync-project.sh): **warn when the Baseline routing block is duplicated in `CLAUDE.md` and
  `CLAUDE.local.md`.** Nothing previously caught the case bootstrap-project.md's old Step 2 could
  produce — a project with both copies, drifting apart the moment `CLAUDE.md`'s went unsynced.
  Section 3 now detects a Baseline row (`skills/learned-mdl-preflight.md` or
  `skills/query-the-model.md`) surviving in `CLAUDE.md` while `CLAUDE.local.md` exists, and warns
  with the approximate word count of the duplicated block; sync never edits `CLAUDE.md` itself, so
  the warning points at the bootstrap-project.md Step 2 fix above as the by-hand remedy.
  Report-only, same as the existing ledger-row warn beside it. Field-run: on the same scaffolded
  project, the warn fired with a word count on the duplicated `CLAUDE.md`, went silent once
  replaced with the pointer block, and `sync-project.sh --strict` exited 0 on the fixed version.
  — MendixMau
- fix(README.md, CLAUDE.md): stop teaching the duplication the two entries above just fixed.
  **`README.md`'s "Consuming this toolkit" section and Baseline-routing heading, plus this
  toolkit's own `CLAUDE.md`, still told every project to copy the Baseline table into
  `CLAUDE.md`** — the exact instruction that produces the duplicate `bin/sync-project.sh` now
  warns about. Both now route through `bootstrap-project.md` Step 2: `CLAUDE.local.md` owns
  the table when a project has one (the normal case since `bin/init-project.sh`), `CLAUDE.md`
  gets only the Step 2 pointer, and the verbatim-copy path is reserved for a project with no
  `CLAUDE.local.md` convention. — MendixMau
- test(tests/wave2/test-bug12-sync.sh): T12 — a `CLAUDE.md` duplicating the Baseline table
  (a `|`-row citing `learned-mdl-preflight.md`/`query-the-model.md`) trips the new
  `sync-project.sh` warn and is left byte-for-byte alone; replacing it with the Step 2 pointer
  block silences the warn, also without a write. Closes the gap the PR that added the warn
  shipped without a fixture. — MendixMau
- fix(sync-project.sh): loosen the duplication anchor beyond `|`-table rows (a duplicate pasted
  as a bullet list was invisible to it) and reword the warn to hedge — "check whether" rather
  than a flat assertion — since the looser anchor can also match a `CLAUDE.md` that cites either
  skill for an unrelated reason; the `~N word(s)` figure is now labelled a "section" (heading to
  EOF), not a "copy", since it is not guaranteed to be all routing content. — MendixMau
- chore(skills): **`ui-preflight-pages.md`'s "Common failure modes" section cut (3,727 -> 3,377
  words); "Report-back format" kept.** `process/skill-freshness-2026-09-17.md`'s A/B/C experiment
  found arm C (both sections removed) indistinguishable from the full skill and recommended
  cutting both — but `skills/learned-stylegallery.md` names the report-back block's
  class-promotion row directly ("every page script's report-back ... the class-promotion row"),
  so only the failure-modes table was removed; the report-back format stays live and depended-on.
  This trades against skill-authoring rule 7 ("show the failure, not just the rule") — each of
  the 14 rows paired a rule with its concrete bad output, exactly what rule 7 asks for — but rule
  6 ("write for the trigger, then stop") wins here on the measured evidence: the ablated arm hit
  the same shell-check and fidelity numbers as the full skill (3/3, 3/3, shell 2,1,2) on the
  fixture the table exists to guard against, so the table was not changing drafting behaviour it
  was retained to change. The individual failure modes it covered (BUG-18 conditional visibility,
  association-mode COMBOBOX, empty-state, validation-feedback) remain covered by name in the
  Step 4 cross-check rows the table only duplicated in table form. — MendixMau
- chore(bug-logs): **BUG-107 (workflow `CALL MICROFLOW ... WITH (...)` segfault on an unquoted
  value) fixed upstream in mxcli v0.22.0, mendixlabs/mxcli#1023 — pending report dropped.**
  Deleted the three drafted `bug-logs/pending-github-issues/bug107-*` files (README, patch,
  issue draft) and the stale, already-mismatched `bug107-workflow-call-with-unquoted-value-segfault`
  entry in `render-paste-ready.sh`'s `ORDER` list; added a RESOLVED status line under the BUG-107
  heading in `bug-logs/mxcli-bugs.md`, keeping the rest of the entry intact. — MendixMau
- new(project-bin/build-plan-status.sh, artifact-manifest.tsv): **`--json` writes `architecture/build-plan.json` from what `build-plan.md` actually says, so a viewer can build a plan view from data instead of embedding the HTML.** On a language learning app conversion the plan carried 7 phases with full Step-5 row tables and `claims:` blocks while `mdlsource/` was flat, so view A and the `--html` render were honestly empty and the only record of phase state was prose no program could read. The new flag parses the `### Phase N — Name *(note)*` headings (any heading level; em dash, en dash, hyphen or colon), the `# | Kind | Step | Produces/Proves | Depends on | Skills | State` rows (columns mapped by header name, `Depends on`/`Skills` split on comma, bold markers removed, everything else verbatim) and the `claims:` blocks (`pointer (count) [BRD]`; a bare pointer, the skill's leaf form, has `count: null`, and `brd: null` when the block names no BRD rather than a guess from the heading; an unfenced block ends where `coverage-preflight.sh`'s extractor ends it), and rolls each phase's `state` up from its own rows' State cells: `built` / `not built` / `pending a person` / `in progress`, and `unknown` when a cell says something else. Encoded by Python's json module through `resolve_py`, never shell concatenation: one real Produces cell contains double quotes, and an invalid file is worse than none because the viewer shows nothing while the file looks present. Unrecognized table shapes and unparsable claims lines land in a `warnings` array, never a non-zero exit; a plan with no Phase headings gets no file at all. Views A and B, stdout, `--html` and the always-0 exit are untouched. `build-plan-json` added to the artifact manifest as a report-kind surface. Checked against copies of every build plan on one machine (29 files, 7 heading and table variants): every one either parses or degrades to a warning, none breaks. **The file is a build product, not a committed artifact**: a committed generated file produces a large diff on most commits and, worse, can sit stale in git while `build-plan.md` has already moved on, and a dashboard that is quietly out of date is what makes people stop trusting it; regenerating costs about 0.45 s and zero tokens. So the producer adds `/architecture/build-plan.json` to the project's `.gitignore` on first write (the rule `snapshot-mpr.sh` already follows for its own output, chosen over an `init-project.sh` scaffold change because it reaches existing projects too). Fixture `tests/wave2/test-build-plan-json.sh`, 57 assertions. Closes #65 — Maurits Visser, from MXTK Studio's Plan room
- fix(skills/brd-to-build-plan.md): Step 5's worked example wrote its three phase labels as plain
  text (`Phase 1 — App Scaffold`) inside a fenced listing, not as the `### Phase N — Name` ATX
  headings `project-bin/build-plan-status.sh --json` actually looks for — so a project that
  followed the example literally got a plan `--json` reads as having no phases at all. The three
  labels are now real `###` headings, matching the level the script's own header comment
  documents. `tests/wave2/test-build-plan-json.sh` gains T9, pinning that this exact fenced
  plain-text shape produces the same "no Phase headings" outcome as an empty plan, run against
  the real script rather than assumed — MendixMau
- test(tests/wave2/test-render-routing.sh): **`bin/render-routing.sh` had zero fixture
  coverage.** New fixture asserts a TSV row whose `agents` column holds an unknown token
  (e.g. `any`) makes `--check` exit non-zero and name the row (the `BAD_AGENT` check), and
  that the baseline word count `--check` reports is identical under `LC_ALL=C` and
  `LC_ALL=C.UTF-8` — the case the locale fix above needed a regression test for — MendixMau
- fix(bin/lib/skill-routing.tsv): **`agent-permission-friction` shipped as a new baseline-tier
  row**, and merging it onto master's own independent baseline growth put the union over the
  80,000-word budget (80,377) though neither branch was over alone. Its trigger ("any refused,
  denied or blocked command") is reactive, not "every session regardless of task" — the
  toolkit's own bar for the baseline table — so it is retiered to `ondemand`. `--check` now
  passes at 79,279 words — MendixMau
- fix(skills/ui-preflight-pages.md): **Step 0's completion criterion was unfalsifiable** ("done
  when you can name every existing entry point" — nothing says when you're done naming) and a
  command example embedded a concrete field project's module/page name instead of a
  placeholder, per this repo's own authoring rules 4 and 5. Step 0 now ends on a checkable
  bound (every caller/impact/page-reference result actually returned is named, and the new
  entry point states which of them it doesn't already cover), and the example command is
  generic — MendixMau
- fix(render-routing.sh): **the baseline word count depended on the caller's locale.** GNU
  `wc -w` splits on locale whitespace, so one tree measured 79,539 words under `LC_ALL=C` and
  81,427 under the runner's `C.UTF-8` — a PR that passed `--check` locally failed it in CI by
  words no file contained (master itself sat 7 words under budget on the runner). Both counts
  now pin `LC_ALL=C`, so the ratchet reads the same number on every machine. Budget unchanged;
  it is now measured in C-locale words — Maurits Visser
- process(check-no-client-data.sh, CONTRIBUTING.md, checks.yml): **warn-only new-capitalised-words report, plus a written reviewer checklist.** Prompted by the user asking "can we just scan customer names from each PR and remove them?" — the honest answer is split: scanning yes, automated removal no. The blocking check stays denylist-driven (`bin/check-no-client-data.sh` step 1) because the guard cannot know a name it was never told; what step 7 adds is opt-in (`LEAKGUARD_BASE=<ref>`) and warn-only — it diffs the PR against that base ref, extracts new words from added lines only and prints the survivors for a human to eyeball, never touching the exit code — this toolkit's own vocabulary is full of legitimate new identifiers (entity/module/microflow names) that a gate on this signal would just train people to silence. CI's leak-guard step now sets `LEAKGUARD_BASE=origin/<base_ref>` and fetches it explicitly. `CONTRIBUTING.md` gained a "What a reviewer does before merging" section (proper nouns, field-run claims, changelog, scoped fixtures, merge not squash, hand-rewrite a found name — never auto-replace) and a clause on the no-client-data rule pointing at the new report. Caught its own bug in the field run: the step-7 diff filter (`grep -v '^\+\+\+'` with no `-E`) silently dropped every added line on GNU grep, because BRE treats a backslash-escaped `+` as a "one or more" quantifier, not a literal `+` — fixed to `grep -vE`. Review fix: the original regex only matched PascalCase/ALLCAPS, so a single-word name (`Smith`, `Acme`) was never flagged — the real-world common case — added a third alternative for a lone capitalised word of 4+ letters; the already-in-base-tree survivor filter is what keeps that from flooding on ordinary capitalised English words. Also decoupled the fixture from a word that happened to already live in `pipelines/outsystems/pipeline/extractors/xml-extractor.js` — it now seeds both a single-word and a PascalCase name entirely inside its own scratch git tree, then proves both shapes get caught in the next commit and both base-tree words are excluded. And capped each printed survivor at 40 characters so a diff that renames one widely-used identifier can't turn the report into a wall of near-duplicate lines. `tests/run-tests.sh`'s LEAKGUARD_BASE block now runs 4 manual assertions (new single-word AND new PascalCase name flagged while both base-tree words are excluded, in one; warn-only exit-0-with-survivors asserted on its own so a future accidental gate can't hide behind an unrelated failure; unresolvable base warns without failing; no `LEAKGUARD_BASE` means no new-words line) — script total 25 checks (18 `assert` calls + 7 manual pass/fail blocks). Field run: `LEAKGUARD_ALLOW_NO_DENYLIST=1 LEAKGUARD_BASE=<base commit>` against a scratch two-commit git tree correctly flagged only the new commit's single-word and PascalCase names, excluded both pre-existing base words, and exited 0 throughout — MendixMau
- new(bin/lib/entry-mode.sh): **one shared entry-mode tokeniser replaces three divergent
  parsers.** `bin/gate-check.sh` and `bin/lib/artifact-check.sh` each carried an unanchored-glob
  `case` where first-arm-wins meant "migration (not greenfield)" parsed as greenfield and
  `stage_waiver()` excused stages on the wrong label; `bin/status.sh` had its own third, anchored
  regex that read the value correctly, so the screen and the gate silently disagreed. The shared
  tokeniser matches the runbook's own multi-word phrases first ("Change an existing app"), then
  the first word of the value, and reports an unrecognised mode loudly on stderr instead of
  resolving to greenfield by accident. Also fixes the gate's own `intake.md` fallback, which
  passes the whole matched answer line ("answered: migration") rather than the bare register
  value — a fix aimed only at the register shape would have silently regressed every young
  project with no register line yet. Verified against a real requirements-driven Mendix build's
  messy register value with zero side effects. — MendixMau
- fix(bin/gate-check.sh): **migrated to the shared `bin/lib/entry-mode.sh` tokeniser** (see the
  entry-mode.sh line above) and stopped `check_stage_3` counting zero-byte artifacts as present
  (`resolve_artifact()` tested `[ -e ]`, so four empty files could discharge a Stage 3 sign-off),
  stopped missing a real wireframe nested one folder deeper than a `-maxdepth 1` search looked,
  and stopped reporting only the first missing artifact instead of the full present/missing set
  in one line. — MendixMau
- fix(bin/lib/artifact-check.sh): **migrated to the shared `bin/lib/entry-mode.sh` tokeniser**,
  dropping its own unanchored-glob entry-mode parser that shared `bin/gate-check.sh`'s
  first-arm-wins misread. — MendixMau
- fix(bin/status.sh): **migrated to the shared `bin/lib/entry-mode.sh` tokeniser**, retiring a
  third, independently-written anchored regex. It happened to read the value correctly, which is
  exactly why the `gate-check.sh` misread survived as long as it did — the dashboard showed the
  right mode while the gate acted on the wrong one, and nobody had reason to look twice.
  — MendixMau
- fix(project-bin/snapshot-mpr.sh): **a v1 single-file model no longer dies silently.** `find` on
  a nonexistent `mprcontents/` exited 1, `pipefail` propagated, and `set -e` killed the script
  before the v1-aware refusal further down — written for exactly this case — could ever run,
  leaving a half-snapshot on disk that `exec.sh` would treat as a safety net. A v1 model now
  snapshots and says so ("v1 single-file"); an empty `mprcontents/` is still refused loudly, as a
  fault rather than a format. — MendixMau
- fix(project-bin/restore-mpr.sh): **gained the v1 mirror of the fix above, with an assertion.**
  Restoring into a model with no `mprcontents/` on either side now copies the `.mpr` and checks
  the copy landed non-empty before reporting success, instead of trusting `cp`'s exit code alone
  — a truncated or failed copy now fails loudly with the existing `git checkout` recovery line
  rather than reporting a restore that did not happen. — MendixMau
- fix(gate-check.sh): **a migration project whose entry-mode line merely contained the word
  "existing" had its cutover gate waived.** #88 added the fourth mode as `*existing*`, and the
  mode arms are substring matches (roadmap 1.9) — on `Entry mode: Migration from an existing
  Oracle Forms system` that reported `Stage 7 (Cutover): WAIVED`, excusing a real migration from
  the gate that exists to catch it. The arm now matches the documented phrase `change an existing
  app`, OR-ed with the mode's own short token `existing-app` so a project that recorded that
  token (what `existing-app-change.md` calls the mode by, and what `bin/status.sh`'s display-only
  line greps for) keeps its Stage 7 waiver — the first draft of this fix dropped that token
  silently. `*migration*` is now checked before the existing-app arm as defence in depth.
  `bin/lib/artifact-check.sh` never had an `*existing*` arm of its own (it deliberately refuses a
  second entry-mode parser and fails loud on an unrecognised mode instead) and `bin/status.sh`'s
  grep is display-only, so neither was actually exposed by this bug — corrected from the first
  draft, which claimed both were. Covered by two new `tests/run-tests.sh` cases (20 asserts
  total) and a new `tests/wave2/test-bug03-gates.sh` case, both asserting all three outcomes: the
  migration string still pends Stage 7, the documented phrase still waives it, and the
  `existing-app` short token still waives it too. Found by writing the roadmap entry for #88,
  not by the tests it shipped with. — MendixMau
- new(skills/upstream-feedback.md): **routes a finding to the right repo and the right vehicle before it's drafted.** Two destinations that don't overlap — `mendixlabs/mxcli` for defects in the binary itself (issues only, never a PR against source we don't hold) and the toolkit for everything process (PR for a small obvious fix, Idea for a judgement call, Issue for a reproducible script defect) — a two-part local `submissions/` draft (publishable half, never-posted half), a scrub gate for credentials/emails/internal URLs/session tokens/DSNs before anything is posted, and a close-the-loop check against the live API instead of a local note that goes stale the moment it's written. Routed in `bin/lib/skill-routing.tsv` as an ondemand diagnose skill. — MendixMau
- learn(skills/learned-constants-and-secrets.md): the Mendix PAT gets a resolution ladder, starting one rung lower than sessions start it — **rung 0 is `env | grep -i '^PAT='`, and no session may report a token unavailable without having run it**. Field failure: a session mid-push to Team Server grepped `.docker/.env`, `.docker/.env.example` and `stack.env`, found nothing, and told the user the token was gone and would have to be re-provided; it had been in the session environment the whole time. Absence from the files you happened to grep is not absence. Only when rung 0 is empty does the session ask, in one batch: export it, `mxcli auth login --token` it, or point at the file that holds it — and pointing is half a step, so rung 3 ends by wiring the path into the project's gitignored env file and naming it (the path, never the value) in the constants register, or the next session asks again. Plus the git half, which cost an hour of suspecting a healthy token: Team Server takes the **literal string `pat` as the username** and the token as the password, so an askpass that echoes `$PAT` for every prompt sends it as the username and fails with `Invalid username or password`. Never in argv, never in the remote URL, never in a credential helper — and never printed to "check it". `project-bin/ts-sync.sh`'s header stops naming the credential without saying where it comes from and points here — a Mendix build project
- learn(skills/learned-constants-and-secrets.md) + new(project-bin/constants-audit.sh): where an environment-varying value gets its value, decided before it is written, plus the instrument that checks it. Field-found on a Mendix **free node**: `Encryption.EncryptionKey` shipped by its marketplace module as `default ''`, which locally came from the gitignored `app/.mxcli/constants.json` and in the deployed sandbox came from nowhere. MxGenAIConnector encrypts a GenAI key's access token while storing it, so the empty constant blocked Import key, Create key AND the startup registration at once — with an error naming *Encryption* while the symptom was a coaching agent stuck loading, and half a day went into "the Cloud GenAI resource pack must be missing". A free environment has no Constants tab at all and the Deploy API offers no way in with a PAT (v4 404s on `.../settings/constants` and `.../constants`, v1 rejects a PAT outright, both verified against a live node), so on a free node the model default is the only channel there is. The skill sets the five channels and their reach, the three defaults that tell the truth (`__SET_ME__` over `''` for a required secret — an empty string is a legal value that fails several layers away in someone else's message), and the register that records the decision; the audit reports CLIENT-SECRET / EMPTY / MODEL-SECRET / WAIVED per constant and **never prints a value**. Field run: a Mendix build project, 11.14.0, 30 constants — three findings, all three true readings, cleared to 0 by three register lines. Its secret test is a name heuristic and says so: `FeedbackModule.LocalStorageKey` is a browser bucket name, not a secret — a Mendix build project
- process(contrib): three e2e false-green mechanisms queued in `contrib/inbox/` — (1) a row-scoping test that only counts passes over a deleted XPath conjunct when the fixture has one organisation and one team, so the test must move a row out of scope and assert exactly that row disappears; (2) a table with rows and no READER is dead data, and a zero-inbound-refs sweep structurally cannot find it because every reference is a write — reference data seeded by eight microflows and shown to nobody survived because a derived integer on the same records was used everywhere; (3) an idempotency guard that returns early on any existing SIBLING row can never backfill an attribute a later build step adds, so the page has rendered a title, a dangling separator and an empty body in every demo since. All three measured against a live runtime, all three reported as `gap` lines so the suite stays green and the defect is read out on every run. Proposed target `skills/testing-shape.md` — a sales-qualification greenfield build
- learn(skills/testing-shape.md): false-green register gains the session-leak row — a logout helper that returns HTTP 200 without ending the session turns a trial-licence cap into a phantom product failure. Measured on Mendix 11.14: `POST /xas/ {action:'logout'}` answers 200 and the session survives, so three login/logout cycles left three live rows in System.Session and the fourth login was refused; the runtime log says "Maximum number of sessions exceeded" while the browser shows only "Sign in failed", so a seven-journey suite reported two journeys INVALID on a healthy app and the blame landed first on the credentials and then on the feature. A persona switch leaked a second session per run by calling the login helper again without releasing the current one. The guard is `mx.logout()`, returning the verified outcome rather than a status code, plus a session count before and after — the suite now runs twice back to back at PASS 74 / FAIL 0 / INVALID 0 with zero leaked sessions — a sales-qualification greenfield build
- fix(project-bin/graph-sweep.sh): the catalog-freshness guard read the wrong mtime twice over and refused to sweep at all on Linux — `stat -c %y` reports a SYMLINK's own mtime, so a project whose root `.mpr` links into `app/` was permanently "stale", and the BSD fallback is GNU's `--file-system`, which prints filesystem stats to stdout before exiting 1, so the `||` chain captured both outputs concatenated. Both branches are now shape-checked against a real ISO timestamp and both dereference the link (`-L`). Field run on a two-tree field project (Linux, 2026-09-11): FAULT before, 2447 objects and 5946 typed edges after — Maurits Visser
- fix(project-tests/page-audit-rules.js): `style/page-heading` counted DECLARED H1 widgets, so a page that swaps its hero between mutually exclusive `Visible:`-guarded branches failed for declaring one heading per branch while rendering exactly one. H1s under a Visible-guarded ancestor now collapse into a single conditional heading slot; unconditional H1s still each count. Measured on a sales-qualification build: the deals overview stopped being reported as "2 H1 heading widget(s)" for a defect that does not exist on screen, while the admin home's two big metric numbers marked up as page headings still fail, which is the case worth catching — a sales-qualification greenfield build
- fix(project-tests/page-audit-rules.js): the `exactly one H1` and `content blocks must be wrapped in a card` rules had no popup exemption, so every correctly-built modal failed both. A popup's heading IS its `Title:`, rendered by the layout in the modal's own title bar, and an H1 in the body duplicates it on screen and in the accessibility tree; a modal is already separated from the page behind it, so the card wrapper has nothing to separate. Measured on a sales-qualification build: two popup pages, four findings, none a defect — 27 fails instead of 30 over 11 pages once the guard lands, and a rule that cannot be satisfied without making the page worse stops training the reader to skip it — a sales-qualification greenfield build
- fix(project-bin/review-module.sh, project-bin/verify-module.sh): `_tool()` searched `$MXTK_ROOT/bin` as step 3 of its lookup, but nothing in either script ever set `MXTK_ROOT`, so the toolkit half of the search was dead on every run that did not export it by hand — including the case it exists for, running the toolkit's own copy where `$BIN` is `project-bin/` while `coverage-check.sh` sits in the sibling `bin/`. Both now resolve it from the `Toolkit root` row of the project's `CLAUDE.local.md` Wiring block, the way `close-task.sh` already does, then from the script's own parent if that still looks like a clone; no clone path is hardcoded. Field-run on a sales-qualification build: coverage and review went from `coverage-check.sh not reachable (neither bin/ nor the toolkit)` to running and faulting on the genuine missing input, `no coverage-ledger.md for <Module>` — a sales-qualification greenfield build
- process(CHANGELOG, bin): **release list — `## Unreleased` collects what lands between
  releases, `bin/cut-release.sh` turns it into a dated `vYYYY.MM.DD` section plus a tag, and
  `sync-project.sh` prints the toolkit release a project is on.** Until now the changelog was
  grouped by commit date only, so "what did I get since last week" meant reading commit shas,
  and a project could only say which sha it was pinned to. Field-run against a scratch clone:
  `cut-release.sh` refused (exit 2) off master, with a dirty `CHANGELOG.md`, with a bad name, with
  an existing tag, and with nothing under `## Unreleased`; the real run renamed the heading,
  committed only `CHANGELOG.md`, and tagged; `sync-project.sh --dry-run` printed `Toolkit release:
  none tagged yet (commit …)` before the tag and `v2026.09.19-1-g<sha>` after. — Maurits Visser

## 2026-09-19
- learn(conversion-runbook §1c): **the dispatch table names tiers, not Claude models — cheap / mid / strong — with a per-harness mapping and a longer cheap-tier list.** The table was written in Claude Code's vocabulary (Haiku/Sonnet/Opus), so a session in the desktop app, Cowork, Copilot, Cursor, Windsurf or Aider had no way to follow it, and the cheap tier held one job (image descriptions) although Stage 1 inventory lines, table/figure transcription and Stage 5 `SHOW` dumps meet the same test: a mechanical check follows. New sub-section defines each tier by the check after it (cheap never ends in a verdict, rule, or question — the 78 %-recall evidence stays), maps the tiers to Claude Code aliases, to a vendor's small/standard/largest tier read from the tool's own picker (names churn, the file does not chase them), and admits local/open-weight models for the cheap tier only; names effort level and batch size as the levers below the tier (README's 1.67× vs 3.4×), and rules that a failed unit is re-run at the same tier. Stubs keep their Claude aliases because the frontmatter accepts nothing else — Maurits Visser
- fix(agent-roles, sync-project): **`agent-roles.md` said `model: inherit` for all six agents
  while `agents/*.md` pins opus/sonnet; the prose now matches the stubs, and `sync-project.sh`
  warns when a completed project agent's `model:` line drifted from the template and re-pins it
  on `--pin-models`.** Inherit was wrong because a project running the whole pipeline under Opus
  paid Opus prices for the gate/test/mdl work §1c assigns to Sonnet. Field-run on a scaffolded
  project: a completed ba-agent left at `model: inherit` reported `Kept` + a drift warning against
  the template's `model: sonnet`, `--dry-run --pin-models` printed the pin without touching the
  file, and `--pin-models` rewrote only that one line. — Maurits Visser

## 2026-09-18
- new(bin): **`bin/token-burn.sh` — tokens per model / day / stage from the Claude Code transcripts on this machine; `status.sh --brief` gains a "Tokens this stage:" line.** Sums `message.usage` once per `message.id` (the harness writes one record per streamed block — 4,336 records for 1,989 messages in the captured session, a ~2× overcount if summed raw), skips `<synthetic>` records, attributes by each record's own `cwd` (a session launched in `~` that works in `~/proj`), headline = input + cache-write + output with cache-read shown apart, stage by the dated Decisions rows in PROJECT.md; prints NOT AVAILABLE and exits 0 where no transcript tree exists (claude.ai chat, Cowork, Copilot, Cursor). Fixture is a captured-then-scrubbed transcript with sums computed independently (`tests/wave2/fixtures/token-burn/CAPTURE.md`). Field runs: a requirements-driven PoC at Stage 4 — 358k headline / 19.2M cache-read over 5 days, 76% sonnet; this toolkit's own clone — 14.5M headline / 491M cache-read, 64% of it in Agent-tool subagents — MendixMau

## 2026-09-17
- process(contrib/inbox): **triage promotes and deletes in the same commit; two of the three 2026-09-16 drops sat in the inbox after their content had already landed.** `2026-09-16-cd-prefix-defeats-permission-allowlists.md` was fully carried by `skills/agent-permission-friction.md` (the cd-prefix rule, the three sibling refusal shapes, the turn-cost economics, and the "not CLAUDE.md" placement note all present) and is now deleted. `2026-09-16-configuration-surface-is-a-requirement.md` was only **partially** promoted — one bullet in `skills/deploy-to-sandbox.md` Step 1 ("no environment constants / runtime settings UI") — and stays, since the architecture-time NFR question, the "Mendix has no password constant" correction and the `/xas/` driver offer have no home yet. `2026-09-16-secret-ingest-validation-and-guard-strings.md` was never promoted anywhere (checked `skills/testing-shape.md`, its own proposed target) and stays — Maurits Visser
- fix(skill-routing): **`deploy-to-sandbox`'s row used `any` in the agents column, a token the renderer never reads — the agents column is `all` or a comma-list of real agent names, and `any` failed the `!= "all"` membership test silently, so the skill rendered into 0 of 6 agent stubs while `render-routing.sh --check` stayed green.** The same row also listed stage `8`, which does not exist (`gate-check.sh`'s stage map only loops `P,0..7`); both are now `all` and `7`. To close the class of bug rather than just this instance, `render-routing.sh` gained a `BAD_AGENT` check mirroring the existing `BAD_TIER` one: agent names are read live from `agents/*-agent.md` and any row whose agents column contains a token outside that set (or isn't `all`) now fails `--check` by name instead of rendering into nothing unnoticed. The new check immediately caught a second, pre-existing instance already on `master` (`learned-mdl-cannot-express`'s agents column carries `design`, which is not an agent) — fixed in the same PR (`architect,mdl`; design work is owned by the architect stub, there is no `design` agent) so the check ships green — Maurits Visser
- new(share-demo-package): **a shared package is broken until it has been checked in the shape it will be received — one file, no repo, no you.** Four steps for sending a demo guide, screenshots or a quickstart doc outside the working tree: inline every image as a `data:image/png;base64` URI (denominator: base64 count == PNG count, `\.png"` count == 0, anything else is a fail); write the cast table from usernames read fresh from the model and cross-checked against the attribute the login page actually carries them in (grep for it — it is per-project, not `data-user` everywhere); issue every token for that one recipient; then open the package from an empty directory outside the repo and read it as the recipient. Field evidence 2026-09-02 — two defects shipped in one package and both were found by the customer, neither visible from inside the repo: eight annotated screenshots delivered as eight broken image boxes, because `<img src="beat3.png">` resolves to nothing once the HTML travels alone; and a login the customer could not pass, because the guide's cast table listed only display names ("Somchai", "Admin") while the sign-in field wants the account name (`demo.initiator`), and the manual-fallback section pointed at "the account name shown above" — information that was never there. A cross-reference to something the reader cannot see is the same defect as a broken image path — Maurits Visser, from a MOC/PSSR app replacement
- process(register): **the toolkit never mentioned `mxcli brain`, while mxcli writes "read
  `docs/brain/project.md` first" into every project's CLAUDE.md** — so a wired project ran two
  instruction sets over two registers and nothing said which won. Split by who asked:
  `PROJECT.md` keeps gate answers (stage, entry mode, CONFIRMED/ASSUMED, waivers, opt-ins) because
  `gate-check.sh` and `checkpoint.sh` read it; `docs/brain/` takes what a session learned — a
  pattern and its why, a disposition, an open question outside a gate, a slice requirement —
  anchored into the model so `mxcli brain check` fails when an anchor dies, which a `PROJECT.md`
  row never does. Rule stated in `skills/checkpoints/checkpoint-template.md`,
  `skills/close-the-loop.md`, `project-bin/close-task.sh` and one baseline row in
  `bin/lib/skill-routing.tsv`. `skills/existing-app-change.md` now uses `brain plan` as the
  coverage ledger (forward anchors, derived BUILT/PLANNED, denominator is the slice by
  construction) instead of a hand-kept `coverage-ledger.md`. Probed on a 107-module app before
  adopting: init/capture/promote/check/plan/drop all as documented, check and plan 1.5 s each —
  the one weakness found, that `capture` and `promote` accept dead anchors and only `check`
  catches them, is why `brain check` now sits in close-the-loop before every commit. — MendixMau
- fix(gate-check.sh): **`Change an existing app` parsed as an empty entry mode, so Stage 7
  (cutover) was demanded of a live app that never cuts over.** `skills/existing-app-change.md` has
  told projects to record that string since 2026-08-20; the parser only knew
  greenfield/requirement/migration. Added as a fourth mode, Stage 7 waived, with rule 0 and a
  fourth row in `conversion-runbook.md`'s Entry Modes table. Two fixture tests in
  `tests/run-tests.sh` discriminate: on the same fixture master exits 3, the fix exits 0 with
  `Stage 7: WAIVED`, and an unknown mode string still exits 3. Field evidence: Marketplace-RnD,
  whose Stage P had been blocked on exactly this since 2026-09-16. — MendixMau

## 2026-09-16
- new(deploy-to-sandbox): **a deployment is untested until a test has run against that URL — local green is a prerequisite, never a substitute.** Seven steps for promoting an app to a cloud sandbox: enumerate what the target lacks (a Mendix Free App Sandbox has no M2EE admin API, no runtime-settings UI, no deploy API, no allowlist, and cold-starts); poll for readiness instead of sleeping for it; audit every guard for a localhost premise; report a non-assertable check as SKIPPED with its denominator, never PASS; verify config through behaviour rather than presence; package it as one command; and decide the handover shape from the evidence. Field evidence 2026-09-16 — an e2e suite that had never once run against its sandbox reported `FAIL demo.admin Administrator 0/6 []` on an app whose navigation was entirely correct. Three localhost assumptions produced it and **none was about the app**: a fixed `waitForTimeout(2500)` tuned to a warm local runtime (a poll for rendered `mx-name-*` widgets read all six items back seconds later); an ownership guard refusing a REMOTE run over a stale LOCAL port out of `stack.env`, whose port-ambiguity premise cannot apply to a named host; and eight journeys whose oracle is the local database reporting a healthy sandbox as broken (`DB read failed ... for PlantA`). After the fix, 4 passed / 0 failed / 8 skipped of 12, and J1 went 0/3 roles to 3/3. Two rules carry most of the value: fix a guard's premise rather than reaching for its `ALLOW_UNVERIFIED=1` escape hatch, which teaches operators to keep the bypass permanently on; and a skipped assertion that reports PASS is green-by-absence — Maurits Visser, from a MOC/PSSR app replacement
- learn(ui-preflight-pages): **Step 0 — enumerate what already reaches a capability before building a page or adding a way into one.** Positive recipe, not a ban: list the existing entry points with the reference tools (`mxcli impact`, `mxcli callers`, `DESCRIBE PAGE <HomePage>` for buttons and snippet calls already placed), then read the target page's own `Layout:`. Two things it prevents, both measured 2026-09-16: `SEARCH '<PageName>'` is a **text grep, not a reference check** — it did not surface the `show_page` action sitting in the home page's widget tree, so two chat pages that already had floating buttons (`btnAgentFab`, `btnGraphFab`, each passing a `ChatContext`) were diagnosed as unreachable; and a browser scrape of navigation captions could not match an **icon-only** floating button, so a second silent instrument agreed with the first and the pair was read as confirmation. The layout half is the sharper rule: a page on a popup layout (`ConversationalUI.Layout_FloatingPopup` and kin) is a **popup, not a destination** — it is opened by a button passing a context object, and a navigation item cannot pass a page parameter, so route one through navigation only where the requirement asks for a dedicated page. Done when you can name every existing entry point and say why another is needed — Maurits Visser, from a MOC/PSSR app replacement
- learn(learned-detection-gaps): register row — **a navigation menu item pointing at a popup-layout or parameterised page passes the entire static ladder.** `check --references` reports **0 errors**, exec succeeds, and `SHOW NAVIGATION MENU` reads the entry back cleanly; the first oracle is live runtime or reading the target page's own `Layout:` and parameter list. Note precisely what the green certifies — the *reference* is valid, the page exists — and that nothing on the ladder checks whether a nav item is a legal way to reach it — Maurits Visser, from a MOC/PSSR app replacement
- process(iterative-build-loop): **"committing after an exec — enumerate, then name", resolving a collision between two of this toolkit's own rules.** `mxcli exec` rewrites the `.mpr` *and* every `mprcontents/*.mxunit` it touched; a blind `git add -A` is banned (it sweeps in a concurrent session's staged work, and in a gutted-tree state commits a ~108 MB blob), so commits pass explicit pathspecs — but **an explicit pathspec cannot name a file you did not know changed.** The resolution is `git status --short` after every exec, before writing the pathspecs, with `mprcontents/` as a directory pathspec being the right grain. Field evidence 2026-09-16: a commit named the `.mpr` and the script and left the exploded navigation unit behind; git reported `2 files changed`, the tree looked finished, and the `.mpr` disagreed with its own contents in the commit — an internally inconsistent Team Server checkout that nothing in the exec, the check or the commit mentioned, surfaced only because a stop hook noticed a dirty tree — Maurits Visser, from a MOC/PSSR app replacement
- new(agent-permission-friction): **two different things refuse an agent's command, they render identically, and only one can be fixed by editing a file — telling them apart is the skill.** A *permission rule* is a deterministic prefix match that ships with the repo and behaves the same for every user on every device; a *safety classifier* refusal is a judgement, exists only in managed/sandboxed environments (cloud, container, web session) where a local CLI user never sees it, carries a bracketed category label, and **cannot be allowlisted away** — so the guidance is: if you cannot tell, assume classifier and reshape, because reshaping is free and rule-editing when the rule was never the problem is what cost roughly **fifteen turns** in the field run (2026-09-16). Four checks before the word *blocked* reaches the user, highest-yield first: (1) **is the command line compound** — a rule matches the START of the line, so `./mxcli exec ...` matches an allowlisted `./mxcli` rule and `cd /path && ./mxcli exec ...` matches **nothing**, which is invisible from the refusal text because the refusal is about `cd`, and the remedy is free since the working directory persists between calls; (2) does it merely *resemble* something dangerous — renaming a credential variable on the command line reads as exfiltration prep, a heredoc near a credential is refused even when the body only *discusses* one (writing the file and passing a path worked every time and the heredoc worked none), and anything that reads as redirecting traffic or rewriting the agent's own config; (3) **a refusal is sticky for the rest of the turn** — an unrelated, fully permitted command was refused with the *same* label and succeeded unchanged on a fresh turn, so a second denial is not evidence about the second command and the move is to end the turn; (4) only then read the settings the harness actually reads, because in the field run the correct rule had been there since scaffold. Completion criterion carries its denominator: the word *blocked* is legal only after naming which of the four checks failed. Carries its own placement rule, learned the same day: this knowledge does **not** go in a project `CLAUDE.md`, which `mxcli init` overwrites and which Copilot/Cursor/Windsurf never read — it lives here, referenced not copied, and reaches projects through the baseline routing table in their generated `CLAUDE.local.md`. And it deliberately ships **no table of harness settings paths or rule syntaxes** (authoring rule 5): every tool has some allow/deny list, they are all spelled differently and they all move, so the file says read your own harness's config and reports only the principle that generalises — Maurits Visser, from a MOC/PSSR app replacement
- process(skill-routing): `agent-permission-friction` routed **baseline** for all agents, trigger-phrased on *"any refused, denied or blocked command — BEFORE rewriting a permission rule and before telling the user a tool is blocked"*. Baseline rather than situational because a session in a denial loop is not browsing a situational table, and on this repo's standing rule that a skill living only there goes unnoticed by projects that are not hunting for it. Baseline budget after the add: **77,323 of 80,000 words** — headroom is now under 3,000, so the next baseline routing needs a displacement, not an append — Maurits Visser, from a MOC/PSSR app replacement
- process(contrib/inbox): **three drops from a deploy-and-configuration retrospective — the arc from first deploy to a configured, working app cost about three days, and five of its six walls were knowable in advance.** (1) *"Read it from the environment" is only a design if the environment can be written to* — an app whose credentials were designed to come from environment variables was deployed to a **Mendix free sandbox, which has no configuration surface at all**: no constants tab in the free-app portal, Deploy API v4 has no constants endpoint (24 environment sub-resources probed, **all 404**), Deploy API v1 has one but is a licensed-node API that rejects PATs by design (four auth-header combinations tried, and a full-rights key authenticates and still does not apply), and the app's own `/xas/` protocol reaches app data and never platform configuration. The tier was in hand the whole time — `"licenseType": "free"` was in the very first `/apps/{id}` response, days earlier — and nothing in the process asked for it, so the proposal is an environment-capability question at architecture time plus the one-call probe that answers it. Carries a correction worth having: **Mendix has no "password constant"** — a constant has Name, Type, Default Value, Documentation, Export Level and Exposed to Client, and hiding a credential from users is attribute-level entity access (the way `MxGenAIConnector` omits `AccessToken` from its read grant), so an instruction to "mark it as a password in Studio Pro" had sat in a repo for weeks being followed by nobody because it could not be followed. (2) *A tolerant parser upstream of a strict one is a silent-failure machine* — a GenAI configuration key supplied at **1345 characters, `1345 % 4 == 1`, which base64 can never be**; Node's decoder swallowed the stray `=` and reported success, Mendix's rejected the import with a message pointing at the portal, i.e. at the one place that was not the problem. Plus the false-green sibling: a chat path returning **HTTP 200 on both calls** with the reply stuck `Loading`, empty, and no trace rows past **153s** — the transport succeeded and the product did not happen. And the convention that did work, a guard string naming which of *unconfigured* / *broken* / *empty* you are in, at the cost of one string literal. (3) *`cd X && cmd` defeats every Bash permission allowlist* — rules match the **start** of the command line, so an allowlisted `./mxcli` call prefixed with `cd` matches nothing; ~15 turns were lost writing new rules that also could not match, against an allowlist that had never needed changing, and the working directory persists between calls so the fix is free. Expensive because session cost tracks turn count × context size, not output length — Maurits Visser, from a MOC/PSSR app replacement
- fix(coverage-preflight.sh): **a build plan's `claims:` blocks inside a fence, with a `(note)`
  suffix, or indented, were silently ignored — only the plainest shape was ever read.**
  `project-bin/coverage-preflight.sh`'s own inline `extract_claims` handled exactly one of five
  real shapes; a plan using any other (most commonly a bare ``` ``` ``` fence wrapping the whole
  block with `claims:` as its first line inside — the shape `skills/brd-to-build-plan.md` Step
  5b's own worked example uses) measured as zero claims and reported "the build plan predates the
  `claims` convention," which was false. New shared reader `project-bin/_claims.sh`
  (`mxtk_extract_claims_tsv`, awk, no Python, bash 3.2 compatible) replaces it, sourced the same
  way as `_common.sh`; `coverage-preflight.sh`'s LEVEL 3 message now distinguishes "no claims
  blocks at all" from "claims blocks present but zero usable pointers," with the unparsed-line
  diagnostics named. `skills/brd-to-build-plan.md` Step 5b now documents all five accepted forms;
  `skills/coverage-ledger.md`'s Step 2 recipe (a `grep -A20` that missed fenced/indented blocks
  and stripped the `(N)` count off anything it did find) now points at the shared reader instead.
  Field run: a workflow-migration project's build plan (MendixMau) — the old extractor returned 0
  of 33 real claim pointers on it; the new one returns all 33, with the 4 lines that are genuinely
  prose (not pointers) reported as `claims-line-unparsed` rather than silently dropped or folded
  into a neighbouring row. `tests/wave2/test-claims-extract.sh` asserts both counts against that
  real capture (a golden fixture dropped in separately, not hand-written) plus a small labelled
  synthetic section for shapes the capture does not happen to use. Closes #74; supersedes the
  parser part of #73, whose competing Python parser read fewer of the five shapes and needed
  Python, which `coverage-preflight.sh` deliberately does not require. — MendixMau
- new(bin/install-claude-permissions.sh): **allow-list the toolkit's safe wrappers in `<project>/.claude/settings.json`, so Claude Code's default permission mode stops prompting on them.** `mxcli init` seeds `Bash(./mxcli:*)` but nothing for `bin/exec.sh`, the other installed `bin/*.sh` scripts, this toolkit's own `bin/*.sh` run by absolute path, or `mx` under `~/.mxcli/mxbuild/*/modeler/mx` — every skill and CLAUDE.md tells an agent to run the model through the SAFE wrapper instead, so every one of those calls raised a prompt, which silently defeated `bin/exec-approval.sh --set auto` (2026-09-15: the user asked for autonomy on execs specifically because the prompting was constant — the knob resolves whether the agent asks, not whether the harness prompts first). Same merge pattern as `install-claude-hooks.sh` (Python via `lib/portable.sh`'s `require_py`, backup before write, merge never overwrite); `--check` reports what's missing without writing, `--uninstall` removes only the entries this script itself added (tracked by exact string in a sidecar file), so a pre-existing `Bash(./mxcli:*)` from `mxcli init` is never touched either way. Rule syntax verified against the Claude Code permissions docs before picking the patterns. Wired into `init-project.sh` (scaffold time, non-fatal) and `sync-project.sh` (`--check`, reported as a stale-wiring finding). New fixture `tests/wave2/test-install-claude-permissions.sh` (26/26): fresh project, merge into a real captured `mxcli init` settings.json, idempotent re-run, `--check` naming, `--uninstall` leaving pre-existing entries alone. Field run: a scratch project scaffolded under the scratchpad. (MendixMau)
- fix(gate-check, source-ledger): a `--waive` with a glob key (`source/legacy/*`) wrote a NEW `Waived source ...` line — and a new `## Toolkit position` heading — every time it ran, instead of replacing the first, because `register_set_line()`/`reg_field()` in `gate-check.sh` blanket-stripped every `*` from a candidate register line before comparing its label, so the stored label ("...legacy/*") never matched the search key again; `reg_field()` also returned the FIRST (stale) reason. Non-glob waivers were unaffected. Fix: drop the blanket strip in both `awk` blocks and remove only markdown bold precisely — the same lookaround rule already used by the `source-ledger.sh` parser — leaving `gsub(/\*/,"")` on the read-back *value* alone so a bolded reason still prints clean. Separately: a waived file that later leaves the source folder used to report plain `WAIVED`, indistinguishable from one still on disk, because `source-ledger.sh`'s WAIVED branch returned before the on-disk check ran; it still reports `WAIVED` — a deliberate waiver must never turn the ledger red — but the note now reads "... — and no longer on disk", in both the text output and the `--json` rows, with counts and exit code unchanged. New fixtures T15 (repeated `--waive` on the same glob key → one line, one heading, latest reason wins; a non-glob key checked as a control) and T16 (waived-then-deleted file: verdict, note, counts, exit code) in `tests/wave2/test-source-ledger.sh`; suite went 43/43 → 55/55, `test-source-sufficiency-gate.sh` unchanged at 7/7. Found by the 2026-09-16 triage of PR #63.
- fix(source-ledger): `source-ledger.sh`'s register parser stripped every literal `*` from each line before matching (`.replace('*', '')`, intended to remove markdown bold), which silently destroyed glob wildcards in `Waived source <glob>: ...` lines — `legacy/examples/*` became `legacy/examples/` and then matched zero files via `fnmatch`. Directory-level waivers (the documented use of the one waiver vocabulary shared with `--waive`) were silently non-functional; only exact-path/basename waivers worked. Found on a Glide-to-Mendix migration whose corpus mixed one in-scope source folder with ~1,800 unrelated workshop-example files across 8 directories — writing a waiver line per file was the only workaround the bug left, which is not a workaround at that scale. Fix: drop the blanket `.replace('*', '')`, keep only the leading list-marker strip, then remove markdown bold markers precisely (lookarounds that require a path character beside the `**`, so `dir/**` and `**/*.bak` survive) rather than every star in the line. Removing the strip without that second half would have been a quieter bug in the opposite direction: `Waived source **legacy/one.pdf**: ...` would have kept the asterisks in the fnmatch key and silently waived `legacy/one.pdf.bak` too, and a waiver means "out of scope", so a widened key hides files nobody named. Re-verified against the same project: waived went from 4/1812 to the correct 1809/1812, with the 3 in-scope files EXTRACTED and name-verified, gate PASS. New fixture `tests/wave2/test-source-ledger.sh` T13 pins both halves (a directory glob waives its 3 files; two bold spellings waive exactly their own file and leave the neighbouring `.bak` alone); suite 43/43, `test-source-sufficiency-gate.sh` 7/7; follow-up in the same PR: single-`*` emphasis around a key no longer widens the waiver, and the `--waive` message now says globs are accepted — a workshop-project ba-agent
- process(privacy): scrubbed two client identifiers (a project codename, an engagement code) that survived the 2026-09-02 anonymisation in CHANGELOG.md, bin/gate-check.sh, contrib/inbox/, project-bin/wf-set-call-captions.py and skills/learned-workflow-patterns.md — wording matches PR #63's privacy commit so both merge clean. Found by the 2026-09-16 triage; the name check runs only where `.leakguard-deny` exists, so CI never saw them. (MendixMau)
- fix(assemble-prototype.js): **a screen's inline `<script>` is namespaced per route too, not just its ids.** Concatenating twenty screens into one document put every screen's top-level `function name(){}` on the same global object; the last screen's declaration silently won, so another screen's `onclick="toggleCopilot()"` ran the wrong screen's code against the wrong screen's (hidden) elements, with no console error — the same disease `ade3851` fixed for ids, one layer up. Each inline script is now wrapped in an IIFE that exports its functions onto `window.__proto['<route>']`, and `on*=` handler attributes are rewritten to call through it; the selftest (`tests/wave2/test-assemble-prototype.sh`) grew 8 asserts (48/48) covering a same-name collision, an `onclick` with an argument, and a script-less screen. Field run: assembled the 9-screen TCX graph PoC prototype (56 handler/export sites namespaced across 9 routes) and, with Playwright/Chromium, opened it at a non-default route (`#/tfc-npd-gate`) and clicked its `toggleCopilot` button — the effect landed on that route's own `id="tfc-npd-gate--copilot-modal"` only, the default route's modal stayed shut, zero console errors. Also: `data-bind="X"` now follows the `<tr id="X">` it names when that id is namespaced — the first id-namespacing pass renamed the row and left the reference behind, so `check-prototype-links.js` reported every bound control as `unbound` (`tests/wave2/test-prototype-links.sh` went 32/32 → 24/32 and CI caught it on #62; back to 32/32, `test-assemble-prototype.sh` 50/50). — MendixMau (#53)

## 2026-09-15
- chore(contrib/inbox): last client-named inbox file genericized (deal-app) — merge desk
- new(bug-logs, learned-workflow-patterns §25/§26, workflow-structure-rules §12): **three of the five v0.22.0 defects reproduced and filed upstream, plus the `EXPOSED AS WORKFLOW ACTION` clause proven and wired into the design gate — the proven third of a five-defect PR split at the merge desk.** `mxcli-bugs.md` gains **BUG-DRAFT-loop-var-expression-typecheck** (the expression type checker is skipped inside a `LOOP` body — `E004` fires on a parameter and is silent on a loop variable, reaching mxbuild as CE0117), **BUG-DRAFT-nested-aggregate-over-filter** (`COUNT(FILTER(…))` writes an Aggregate activity with no `List` property — `check` passes, `exec` writes, mxbuild refuses with CE0012), and **BUG-DRAFT-system-enumerations-undiscoverable** (`System` enumerations are invisible to `show`/`describe`/`search` though `describe entity` prints attributes typed against them, so valid values can only be found by guessing until CE1613) — each staged as a paste-ready draft in `bug-logs/pending-github-issues/` and FILED: mendixlabs/mxcli#1100, #1101, #1102. New `learned-workflow-patterns.md` §25 records what upstream's v0.22.0 changelog claims and why a changelog entry is not a clearance; §26 documents `EXPOSED AS WORKFLOW ACTION '<caption>' IN '<category>'`, proven end to end (writes, native `mx check` 0 errors, `DESCRIBE MICROFLOW` round-trips it, and — the result that makes it safe to adopt — **a later rewrite that omits the clause preserves the stored exposure**), with the rule that every called microflow a workflow calls carries it except `SUB_`-prefixed plumbing. `workflow-structure-rules.md` §12 gains **row 12** with a real denominator (N of N called microflows that are **not** `SUB_` prefixed; N from `mxcli callers <Module.MF>`, which reports the workflow at depth 1). **Held back, pending re-verification on `--local`:** two further BUG-DRAFT entries found the same session (a `.test.mdl` block's `RETRIEVE … LIMIT` misrouted to the OQL grammar, and `mxcli test` swallowing mxbuild's actual error on an injection failure) did not reproduce cleanly against this queue's own re-run and are held rather than merged; §11's full coverage-table rewrite (the eight-row CTRL/UPSTREAM pass against v0.22.0/`main` HEAD) is held with them, since its verdicts depend on the same re-verification — the table here is unchanged from before this branch. Split from PR #52 by MendixMau — probed on mxcli `main` HEAD (`7b42100d`), Mendix 11.13.0
- fix(examples/stylegallery): **the gallery set is now field-proven, and one file was wrong** — executed all 13 files in order against a blank Mendix 11.13.0 scaffold (mxcli v0.21.0), `check --references` + `exec` per file, then native `mx check`: 0 errors in `StyleGallery` after fixing `14-kpi-tiles.mdl`, whose `DynamicClasses` used a bare `TrendDir` instead of `$currentObject/TrendDir` (CE0117 — `learned-mdl-preflight.md` STOP item 12, invisible to `mxcli check`). README's "never executed" honesty note replaced by the field run; the visual side-by-side is still owed. The set was merged the day before on gates alone — a thing nobody has run is a claim, and this one had a defect — Maurits Visser
- new(design-artifacts Step 3, assemble-prototype.js, prototype-route.js): **wireframes assemble into one clickable prototype.** `node project-bin/assemble-prototype.js` turns `design/wireframes/*.html` plus `design/ds.css` into `design/prototype.html`: every screen a `<section data-route>` switched by hash route, CSS scoped per screen, the binding tables behind a Show bindings toggle. Three conventions make a screen assemble: `data-route` on the body, `#/route` links, and `data-bind="<row>"` or `data-cut="<reason>"` on every control. WHY: a project that hand-built such a page found the stakeholder review markedly better than twenty separate files. `prototype-route.js` is the one reader of the section format, so no second parser can drift from the first. Wireframes stay the source; the prototype is generated and never edited.
- new(page-fidelity.js, check-page-shell.sh, ui-preflight-pages, iterative-build-loop): **build checks read a screen as a prototype route.** The wireframe argument and a `PAGE-MAP.tsv` row accept `design/prototype.html#/order-list` as well as a per-screen file, and give an identical verdict for the same screen (pinned by `test-prototype-route.sh`). An unknown route exits 2 naming the routes that exist; a missing prototype file is still reported as a stale map row.
- new(design-artifacts Step 3c, check-prototype-links.js): **the mechanical half of the Step 3b scope crosscheck is a script.** A static parse of the prototype, no browser: `dead-link` (a `#/route` no screen has), `orphan` (a screen no other screen links to, default route exempt), `unbound` (a control with no matching `data-bind` row and no `data-cut`, including a `data-bind` naming a row that was renamed). A screen with no `table.bind` at all only warns, so wireframes drawn before the convention do not get the check switched off. Exit 0/1/2 like `check-page-shell.sh`. Both new scripts are routed (`skill-routing.tsv`, design group, stage 3).
- new(brd-generation, brd-validation check 8, brd-to-build-plan Step 7, coverage-ledger): **BRD use cases name the prototype routes they walk.** `useCases[].routes` (or `#/a -> #/b` in prose) and `pages[].route`, so a reviewer clicks the flow instead of imagining it. `check-prototype-links.js --brd <file-or-dir>` cross-checks both directions: `brd-unknown-route` (the BRD names a screen the prototype does not draw) and `uncovered` (a screen no use case walks, unless marked `data-chrome="<reason>"`). A route listed only under `pages[]` covers nothing, because a page list says what gets built, not that anyone walks it. The build plan carries the page route so `PAGE-MAP.tsv` is written once from it; `coverage-ledger` ledgers routes as review metadata.
- learn(e2e-harness-base Step 4): **happy-path scenarios replay the BRD route paths.** One scenario per use case, one page checkpoint per route (mapped through `PAGE-MAP.tsv`), one click per arrow on the link the prototype draws, with step ids like `UC001-2` so a failure names the arrow of the approved flow that broke. WHY: a scenario written from the BRD prose tests the flow its author imagined, and drifts from the approved one at exactly the extra confirm screen or the edit opened from the list.
- process(leak guard): client and project names already on master genericized in CHANGELOG, bug logs, inbox contributions, two project-bin scripts and a harvest test, so `check-no-client-data.sh` passes on the whole tree. Git history still holds the old names.
- new(examples/stylegallery): **a client-free, copy-in StyleGallery reference set** — the full numbered `mdlsource/gallery/` file set (`00`/`05`/`11`–`19`/`15b`/`90`) plus a matching `ds.css` (three-tier primitive→semantic→component tokens, neutral palette + one accent) and a self-contained `design-system.html`, all rewritten onto the toolkit's invented purchase-request domain so no client name, brand, or path survives. Every `.mdl` passes `mxcli check` clean or info-only; never executed against a live project, and the README says so. Pointed to from `examples/README.md` and `skills/learned-stylegallery.md` — harvested from two field projects, genericized (Maurits Visser)
- new(bin/exec-approval.sh, wire-agents.sh, interview-protocol.md, sync-project.sh): **exec approval is now a knob, not a hardcoded ask.** User complaint, verbatim: "I'm going a bit insane of claude keeping to asking me if they can run an exec mdl. Can we for unattended mode remove the check for exec, for attended keep it, but give the person the option to turn it off at any time?" `bin/exec-approval.sh <project-dir>` resolves `ask` or `auto` (env → `.claude/.exec-approval` → `PROJECT.md`'s `Exec approval:` → **derived from interview mode**: `auto` → `auto`, anything else → `ask`; an unrecognised value anywhere falls back to `ask`, on stderr), the separate switch `interview-mode.sh`'s own header predicted on 2026-08-12 and never built. `wire-agents.sh` item 3 — the "Start here" block stamped into every project's CLAUDE.md/AGENTS.md/per-tool pointer files — now defers to it instead of hardcoding "without asking the user first — every time"; the item's text is shared between `wire-agents.sh` and a new `bin/lib/wiring-item3.sh` so it is typed once. `bin/sync-project.sh` gained a step for the seven of twelve live projects already wired under the old sentence: `wire-agents.sh --check` only tests that the marker block exists, never that its content is current, so nothing had ever re-run it there — the new step rewrites item 3 in place (own awk, no `mxcli` dependency, so it works even where `wire-agents.sh` itself would silently skip). `project-bin/exec.sh`'s BUILD-LOG row now appends `· approval: auto (<source>)` when the resolved mode is `auto`, so an unattended exec is recorded as unattended — the row is the safety net `auto` moves the check to. `interview-protocol.md` gains a ≤120-word "Exec approval is a separate knob" subsection stating the never-on-your-own-judgement rule. Routed baseline (`exec-approval`, mdl/gate, stages 5–6). Fixture `tests/wave2/test-exec-approval.sh`, 25 assertions — Maurits Visser

## 2026-09-14
- fix(bin/doctor.sh, project-bin/exec.sh): **with Studio Pro 11's mxbuild, `doctor.sh` and the exec.sh gate hung forever on Windows.** `mxbuild.exe` 11.12.4 starts `modeler/tools/deno/win-x64/deno.exe`, which inherits stdout and keeps running after mxbuild has exited — after `--version`, `--help` and a `BUILD SUCCEEDED` alike. Every `$(mxbuild ...)` command substitution then waits for EOF on a pipe that deno never closes: doctor sat in *Build toolchain* for >10 minutes on the one machine it was meant to diagnose, and finished the instant the stray deno was killed by hand; a real build through exec.sh hung the same way. mxbuild's output now goes to a temp file (`probe_capture` in doctor, `_MXOUT` in exec.sh) and stdin is closed — bash returns when mxbuild itself exits, whatever its children do. Field run, Windows 11 / Git Bash / Studio Pro 11.12.4: doctor completes in 16 s with `mxbuild runs` + `gate WILL RUN`; exec.sh clean script 42 s, failing script 51 s including the restore rebuild. Not fixed here, only observed: the orphaned deno process itself stays behind (one per invocation) — Marco Keijsers, from a Mendix 11.12.4 client app
- fix(project-bin/exec.sh): **on Windows the mxbuild gate was blind on the failure path: a real CE0117 was applied, reported `errors file could not be parsed`, left in the model, and the script exited 0.** `err_count`/`err_codes`/`err_set` hand Python the mktemp'd `/tmp/...` spelling inside a `-c` string, where MSYS does not convert it, and a native Windows Python cannot open it (`/tmp` there is `C:	mp`), so every helper answered `?` and the gate degraded to `unverified` instead of fail + restore. The path now goes through `native_path()` (`cygpath -m`). A second, quieter fault in the same block: Studio Pro 11's mxbuild writes `--write-errors` *only if the project has errors* (its `--help` says so), so on a clean model the pre-flight read `Model ALREADY has ? error(s) [?]` on every run; empty file + exit 0 is now a verified 0, in the pre-flight and in the post-restore rebuild. Field run, Windows 11 / Git Bash / Studio Pro 11.12.4, on a scratch copy of a 1,650-unit client model: clean script → `baseline clean` + `mxbuild: 0 errors`; a microflow assigning a String to an Integer → `1 error(s) found — restoring snapshot`, `CE0117`, `The restored model builds clean`, microflow gone, exit 1. Pre-fix the same script exited 0 with the microflow in place — Marco Keijsers, from a Mendix 11.12.4 client app
- fix(project-bin/_common.sh): **Windows Studio Pro discovery fused two Program Files roots into one path and could pick `gradle-8.5` as the newest install.** `find_sp_app` appended each root's `ls -d` output to the list without a separator, so on a machine with Studio Pro 9.24, 10.24 and 11.12 side by side the candidate became `.../gradle-8.5//c/Program Files/Mendix/10.24.21.108016` and doctor reported `mxbuild not found` at a path that does not exist. With the separator restored a second fault surfaced: `Program Files\Mendix` also holds `gradle-8.5` and `Version Selector`, and `sort -V` ranks letters above digits, so the "newest version" was gradle. Only a directory carrying `modeler/mxbuild.exe` now counts as a Studio Pro install. Field run: Windows 11, Git Bash, three Studio Pro versions installed; doctor went from FAIL to `Studio Pro install: /c/Program Files/Mendix/11.12.4` with no `MENDIX_APP` override — Marco Keijsers, from a Mendix 11.12.4 client app
- learn(learned-workflow-patterns §24, project-bin/wf-set-call-captions.py): **`EXPOSED AS WORKFLOW ACTION` only labels the Toolbox entry — it does not retroactively relabel a `Call a microflow` activity already placed on a canvas.** Caption is stored per placed `Workflows$CallMicroflowActivity`, independent of the microflow; exposing the microflow changes what a *future* drag stamps, never what an existing activity already carries, and there is no MDL statement that reaches back and fixes it. Both `mxcli check --references` and native `mx check` pass clean either way — Caption is cosmetic to the compiler — so the only way to see the gap is reading the canvas in Studio Pro. New `project-bin/wf-set-call-captions.py` (built on `mxunit_bson.py`, same order-preserving BSON approach as `wf-add-path-terminators.py`) walks a workflow unit and rewrites only `Caption` on matching call activities, leaving `Name` and all flow references untouched. Dry run by default, `.bak` on `--apply`. Field run: an approval-workflow app replacement, 36 call activities on one approval workflow relabelled in one pass, `mx check` 0 errors before and after — an approval-workflow app replacement
- new(workflow-structure-rules §13, architecture-blueprint, architect-agent): **the pipeline now reads a BPMN source as BPMN.** Before this the string `bpmn` appeared exactly once in the whole toolkit — in `source-formats.tsv`, as a file extension to inventory — so a process arriving as a BPMN/Visio/swimlane diagram was screened against *what mxcli can write* (§11) and never against **what the Mendix engine can express at all**. Those are different axes, and an unsupported source element fails at the second one. New §13 carries the [Mendix BPMN coverage](https://docs.mendix.com/refguide/bpmn-coverage/) mapping as a two-hop table — BPMN element → Mendix construct → §11 writability — separating the three verdicts that were being blurred: native-and-scriptable, native-but-hand-add (event sub-process, interrupting boundary timer, annotation), and **NOT SUPPORTED, which is a redesign belonging in `fit-gap.md` at Stage 3, never an approximation at build time** (event-based and complex gateways, embedded/transaction/ad-hoc subprocesses, terminate end, multiple events, group). **The sharpest gap was swimlanes**, which the toolkit treated as documentation only (Step 3d's cross-persona journeys, an optional artifact wired to nothing): Mendix treats them as implementation, so **one pool is one workflow** — a three-pool source built as one is a different application no later gate catches — and **a lane is a targeting statement**, carried into §6's derivation as the requirement sentence, with §6's warning that a lane named for a role is not evidence for a role XPath. Two costing traps named: the **inclusive gateway (OR)** is modelled as a parallel split with a decision per path, i.e. BUG-76 and BUG-121 at once, and the **event-based gateway** substitute lets both branches run, which changes the process and is therefore a `PROJECT.md` decision rather than an architecture call. Wired so it fires rather than sitting unread: §12 gains **row 11** (pools counted, lanes carried into row 5, N of N elements screened; *"source is not a process diagram"* is a legal entry), making it an eleven-row count — the `workflow-count` obligation already demands a denominator on it — and the rows plus the pool/lane rule are **inlined** into `architect-agent.md` per this repo's "a citation is not a read". Blueprint Step 3b gains pools/lanes as the construct to settle *before* drawing, and Step 4 gains the per-element screen. Convergence worth recording: **terminate end is on Mendix's own unsupported list**, which is the platform stating from its side the same branch-ending hole §4 and §11 describe from the MDL side — Maurits Visser
- fix(bug-logs, workflow skills, project-bin): **the BUG-121 terminator finding reaches the ledger, the §11 coverage table and the detection-gap register, and the repair ships as a runnable instrument.** The finding itself landed the same morning in `learned-workflow-patterns.md` §18 and `workflow-structure-rules.md` §4 (PR #47) — this is the other half, and it exists because three surfaces still carried the pre-finding verdict: `bug-logs/mxcli-bugs.md` said the mechanism was unidentified and told readers to hand-build every split in Studio Pro (and `bug-lookup.sh` reads the ledger, not the skill), §11's row read **RETRACTED**, and `learned-detection-gaps.md` had no row for the one construct where every rung is blind in *both* directions. Ships the repair as a project-side pair — `project-bin/wf-add-path-terminators.py` walks the stored unit and appends the terminator to every unterminated `ParallelSplitOutcome` flow, recursing so nested splits get theirs; idempotent, dry-run by default, `.bak` on write. `project-bin/mxunit_bson.py` is the order-preserving BSON codec it reads through (**1068 of 1069 units in a real 16-module project re-encode byte-identically**; the one miss carries BSON type `0x09`, which no workflow unit uses, and the codec raises rather than guesses). Both named in `install-manifest.sh` as a pair, since the patcher imports the codec. **Field-proven on a 16-station approval workflow with a seven-leg split and a nested two-leg split inside one leg**: 9 paths, 9 terminators, unit 87236 → 89486 bytes, **8 of 16 stations before and 16 of 16 after, six open concurrently at the split**, native `mx check` 0 errors both times; the shipped tool re-run against that model reports `already terminated: 9, to add: 0`. §11's row moves from RETRACTED to proven-with-a-mandatory-post-exec-step, and §11's *"explicit `END WORKFLOW`, end-of-parallel-split-path — not expressible, **and not needed**"* row is corrected: `mx check` is indifferent, the runtime is not, and it is **one grammar gap with two silent runtime faults** — this one and §21 gap 2's empty outcome block, which rejoins the enclosing flow instead of stopping the instance. New `learned-workflow-patterns.md` §23 covers only what §18 does not: how to run the repair, and the rule that bites the *second* time — **any later script that rewrites the workflow drops the terminators again**, so the pass belongs in the build plan as a named repeated step and in the MDL script's own header, never as a one-off fix in the build log — Maurits Visser, from a VBA workflow-engine replacement
- learn(learned-workflow-patterns §22, workflow-structure-rules §11): **the first workflow this toolkit has on record that was designed in MDL and shipped — and it contains zero `DECISION` activities and zero `JUMP TO`, neither by preference.** `WF_MOCApproval`, mxcli v0.21.0 / Mendix 11.14, 14 activities over a project entity: Start → user task *Initial review* → user task *Initial approval* → **multi-user task** *Expert assessment* fanning to eight assessment topics → user task *Director approval* → End — six structural steps for four user-facing stages, native `mx check` clean, running in the app, nothing hand-added in Studio Pro. §11 until now cited probes and a rebuild of a human-drawn workflow; this is the producing case. Three findings a probe cannot reach: **(1)** BUG-76 forced approve/reject onto the **user tasks' own outcomes** instead of an exclusive split, and that is the better shape anyway — a user task's outcomes already branch on a human's answer, so a `DECISION` after every task is redundant before it is dangerous; reach for one only where the branch is on *data*. **(2)** The obvious reject path — jump back to the previous task — is a **dangling** jump at the first task, where nothing exists behind it, and the error **describes the wrong fault**: `mxcli check --references` clean, `exec` reports `Created workflow`, `DESCRIBE WORKFLOW` reads it back, and the native build refuses with **CE6681** *"not possible to jump to end activities or jump-to activities"* — a message about the target's *kind*, sending you to inspect a target that does not exist. Read CE6681 as *"this jump does not resolve"* and check existence before kind; §11's forward-`JUMP TO` row is the same code from the opposite cause. The fix is not a better jump: **a reject ends the workflow**, so the shipped model has no `JUMP TO` at all. A fourth entry for the detection-gap register — clean at three rungs, broken at the fourth, message pointing away from the cause. **(3)** Five approver populations were collapsed onto one `Approver` user role, so `[%UserRole_Approver%]` would have put every stage in every approver's inbox — builds clean, runs, assigns the wrong people, invisible to every static check; each stage got its own targeting microflow, each carrying the platform's **two-parameter** signature (`System.Workflow` *and* the context entity), since a one-parameter wrapper passes `mxcli check` and the native build refuses it. Not exercised by this run and unchanged: boundary events, `WAIT FOR TIMER`, event sub-processes, parallel split (BUG-121 stands), sub-workflows — Maurits Visser, from a MOC/PSSR app replacement
- fix(gate-check): **the `## Current stage` readout never replaces a hand-written line.** Regression check of the morning's readout against twelve real project clones: on eight it rewrote the register's own words (`Stage 5 verdict: COMPLETE`, `Stage 4 ✋ re-gate PASSED`, `Stage 4 — Build Plan: LOCKED`) with the mechanically derived position (`Stage 3 — needs attention`, `Stage 0`), a correct reading of the gate checks and the wrong thing to do to a register. The readout now writes only over the untouched scaffold line or its own earlier line; anywhere else it prints `is hand-written and left alone: …` plus `gates derive: …` and touches nothing. Fixture gains T8 (hand-written line untouched, disagreement printed) and T9 (the readout's own line still advances); 10/10. Field run: the twelve clones, registers restored from git afterwards — Maurits Visser
- new(skills/small-project-tier.md): **the small-project tier — same gates, a bounded artifact set.** Token-path A/B conclusion 5: a 3-file corpus produced 30k words of Stage 0–4 artifacts and a 200-file corpus 44–58k, so the artifact set is a fixed cost per project and a three-screen app pays the same as a 200-screen one. The tier is *declared, never inferred* — CAC-1 gains a conditional Q3, asked only when the Stage 0 inventory is at or under 1 module / 8 screens / 25 use cases, recorded as `Size tier: small — <counts>` (a No is recorded too). It removes nothing a gate reads: every gate, interview, ledger, validation, wireframe-per-screen and ✋ decision stays. It bounds what each gate reads — one `triage.md` with a six-row assessment table instead of two files, one BRD for the whole app (facts-lock skipped: one writer, nothing to freeze), `blueprint.md` ≤ 600 words, fit-gap ≤ 20 rows, design system limited to the components the wireframes use, build plan ≤ 12 scripts, one brief, the single-file coverage ledger — and waives three report-only renders by name (`module-design`, `build-plan-html`, `improvement-register-html`), written as the register lines `artifact-check.sh` already reads, since `--waive` has no artifact target. Caps are denominators for the §1b checklist; exceeding one is fine when the close-out says why, silent exceeding is the defect. Routed ondemand (ba, architect, stages 0–4); the runbook's Entry Modes carries a six-line pointer; `checkpoint-scope.md` carries Q3. Not yet field-run — the first small project to declare the tier should record its artifact word count here against the 30k baseline — Maurits Visser
- learn(conversion-runbook §1c): **a dispatch table — what leaves the main session, in what batch, on which model.** The six agent stubs have carried model tiers since generation (architect Opus, the rest Sonnet) and §2's Owner rows say who is accountable, but nothing said *when* the main session hands work over, so across the token-path A/B (nine runs) it did nearly everything itself at its own price and only `brd-generation.md` and `image-transcription.md` ever asked for a fan-out. §1c fixes what always stays (questions, gates, exec, the merge into PROJECT.md) and, per stage, the unit that fans out, the batch bound, the model, and the fence-before/check-after; measured bounds (5–8 images, one BRD per agent) are marked as such, the rest as judgement. Haiku is scoped to mechanically checked reads (78 % recall, ~1 in 15 misreads on the 2026-09-14 image run), never verdicts. Names the harnesses where stubs do not load (desktop plain chat, Cowork, Copilot, Cursor, Windsurf) and what to do there. `agent-roles.md` gets a one-line pointer — Maurits Visser
- fix(gate-check): **`PROJECT.md`'s `## Current stage` line is now a readout gate-check writes, not a line an agent has to remember to advance.** `init-project.sh` wrote `**Stage P — Kickoff**, in progress.` and nothing ever touched it again: every hand-in of the token-path A/B (nine projects, gates P–4 passed) still read Stage P. The position is a fact each full run already establishes — the first stage that is neither PASS nor WAIVED — so the run now rewrites that one bold line (`**Stage 3 — Architecture & Design**, in progress — gates passed: P, 0, 1, 2 (derived by gate-check on <date>; …)`), under the dashboard's own conditions: full informational run or `--html`, never a stage query, never a run about to be blocked, only when the position moved (a new date alone is not a diff). A MANUAL stage (Build) holds the position until a later stage has something in it, because the script cannot certify Build done and must not skip it on nothing; FAIL reads `needs attention`. Adopt/waive lines remain the human's authority. Fixture `tests/wave2/test-stage-header.sh` (7 assertions: fresh, advance, stage-query read-only, idempotent, adopt, manual hold, no-section untouched); pre-fix script fails T1/T2/T5/T6 as the positive control — Maurits Visser
- new(bin/images-to-md.sh + bin/lib/images-to-md.py + skills/image-transcription.md): **one worklist of every unique picture in the source corpus, one description file each, coverage checked — the step `bin/html-to-md.sh` left undone.** html-to-md lists every image in `documents-index.md` with an "Images to read (vision)" denominator, but nothing then reads the pictures except whichever session happens to look, and the description is never stored anywhere another session can cite. Field precedent this closes: a 25-slide functional-description deck (22 distinct PNGs, 1,173 words of slide text) was read as prose at Stage P and its images were extracted 20 days later by hand; the pictures overturned an analysis conclusion and closed four open questions, and the hand capture left no per-image file, so nothing downstream could point at "img-014 says X". `bin/images-to-md.sh <project>` collects from every corner a picture hides in — loose files, HTML sidecars (plus html-to-md's own inline-decoded images), every `.pptx`/`.docx` under the source root (media, kept mapped to its slide + `<a:t>` text, or its nearest preceding `Heading1`), and PDF `/Subtype /Image` objects (`/DCTDecode` → raw JPEG; 8-bit `/FlateDecode` DeviceRGB/DeviceGray → a PNG written with `struct`+`zlib`, no Pillow; anything else `needs-render`, counted rather than dropped) — dedupes by sha256, and writes `manifest.json` + `worklist.md` under `analysis/knowledge-base/images/`, plus one `slideNN.md` + `slides.json` per pptx deck (the same shape as the hand capture). `skills/image-transcription.md` is the template (`## Kind` / `## Verbatim text` / `## Structure` / `## Implied requirements and rules` / `## Uncertain` / `## Summary` — verbatim in quotes, never translated, no inference beyond the pixels) and the dispatch rule (read it yourself, or fan out to subagents in batches of 5–8, each returning the file it wrote). `--check` verifies every content image's description exists, is non-empty and carries the six headings (`described N of M`, exit 1 while any is missing), then inlines each one's verbatim text and summary as a quoted block right under the page's own `![alt](path)` line (idempotent, `begin`/`end` markers) and prints the `bin/source-ledger.sh mark ... --media N` command owed per source file — printed, never run, judgement stays with the human (skills-over-scripts.md). An image's id is stable across runs (carried over by sha256), so a description written against img-014 never orphans. One real bug caught before merge: a sidecar image's `source` field (the owning page, for the ledger command) was also being used to match the `documents-index.md` row for the `described` column, so a sidecar's status landed on its OWNING PAGE'S row instead of its own — fixed by carrying the image's own path separately from its ledger owner. Routed baseline (`bin/images-to-md.sh`, ba, stages 0–2) and ondemand (`image-transcription`, all agents, stages 0–2); `conversion-runbook.md`'s docs-ready Stage 1 row now names it as the concrete next step after html-to-md, and html-to-md's own summary prints a pointer to it. Fixture `tests/wave2/test-images-to-md.sh`, 49 assertions, over a corpus built at test time by `gen-fixture.py` (real OOXML zips and a real hand-built PDF with a stated `/Length` — CAPTURE.md says why a built corpus is the right call for well-specified container formats, unlike a saved webpage's undocumented shape): dedup folds a pptx image reused under a different zip entry name, the 16×16 size floor, slide→image mapping with `<a:t>` context, per-slide md + slides.json, docx heading mapping, sidecar-vs-inline no-double-count, PDF DCTDecode extraction, `--check` failing then passing, inlining, `documents-index.md`'s new column, and idempotence (byte-identical manifest/worklist on rerun). **Field run (2026-09-14):** one real 25-slide functional-description pptx (5.2 MB, the deck behind the precedent above) as the sole `sources/` file — 24 slide images, 22 unique after sha256 dedup (2 folded), 0 chrome, 0 needs-render; per-slide md for all 25 slides; the 22 descriptions written by four Haiku subagents in batches of 5–6 (78–87k tokens, 1.6–3.1 min each), `--check` reported `described 22 of 22` and inlined into 20 slide files; scored against a hand-built key of 246 fact tokens from the earlier manual capture, the descriptions recalled 192 (78%, or 81% over the 21 slides that carry an image). Two defects the run exposed are now rules 5 and 6 of the skill: real user names copied verbatim off a user-management grid, and roughly one string in fifteen misread on small or dense German text (`Entfall` → `Einfall`) with one detail-panel column skipped outright. Instrument code unchanged by the run — Maurits Visser
- fix(bin/harvest-learnings.sh): **section 3's installed-script drift is now direction-determined, not just flagged.** Field run 2026-09-14 across 10 projects (8 fresh clones plus the two live checkouts tfc-tcxgraphpoc and approval-app-main): 115 installed scripts differed from their shipped `project-bin/` copy, and every one was written as a full "direction is undetermined" diff — 68k of the 79k words the harvester produced. Checking each project copy's sha against the toolkit's own git history of the shipped file resolved the direction mechanically: 84 were byte-identical to some older shipped commit (**STALE** — a stale install, nothing to harvest, now written as one line: `- bin/X — STALE: identical to shipped <sha> (<date>); fix: bin/sync-project.sh <project> --upgrade-bin <X>`) and 31 were genuine edits matching no historical commit (**LOCAL-FIX** — diff kept, headed accordingly, naming the closest historical base by diff-line count). A toolkit worktree with no `.git` falls back to the original undetermined wording for every item rather than guessing or crashing. Fixture `tests/wave2/test-harvest-learnings.sh` (new — no fixture targeted this script before): 16 assertions over a real toolkit clone (never the shared worktree's own `contrib/inbox/`) proving both verdicts plus the no-git fallback; the pre-fix script fails 7 of them as the positive control — Maurits Visser
- process(bug-logs): **mxcli v0.21.0 retest — 34 ledger entries probed, 8 archived, 137 → 137 headers (93 → 85 live).** Fresh build (`git describe --tags` = `v0.21.0`, `mxcli version v0.21.0`), every verdict against a disposable blank-app copy with `mxcli docker check` auto-resolving to a real native Linux mxbuild (`~/.mxcli/mxbuild/11.13.0/modeler/mx`) even though no Docker daemon was reachable. Covered the 8 v0.20.0 STILL-OPEN carry-overs (all 8 reproduce identically — BUG-76's workflow DECISION corruption is still byte-exact, and BUG-107's `CALL MICROFLOW WITH(unquoted)` segfault still panics the tagged release even though an unmerged fix branch for it exists upstream), the v0.20.0 "not retested" backlog (mixed: 3 NOT REPRODUCED, 1 already independently confirmed FIXED by a separate real-project probe, 9 NOT RETESTED — several because `DATAGRID`'s v0.21.0 unification onto the DataGrid2 engine makes the original v1-shaped repros uncertain to construct), and 11 never-verdicted entries picked crash/corruption/silent-no-op first (4 FIXED — BUG-98's `calculated by` wiring now shows a real `SHOW CALLERS OF` edge, BUG-103's changelog-confirmed apostrophe-escape fix, BUG-124's `without events` round-trip, BUG-23's MDL-WIDGET14/24 refusal; 1 NOT REPRODUCED; 6 STILL OPEN including BUG-117's fix landing narrower than the bug's own claim — only the two named properties, not the general case). Verdicts: 4 FIXED / 4 NOT REPRODUCED / 16 STILL OPEN / 10 NOT RETESTED. New files `bug-logs/mxlabs-v0.21.0-retest-2026-09-14.md` (full method + notes) and `bug-logs/archive-resolved-2026-09-14.md` (the 8 entries, full original text preserved); `bug-logs/mxcli-bugs.md` updated in place, per-entry, matching the existing archive-pointer convention — Maurits Visser

## 2026-09-13
- learn(record-demo-video): **three rules added after a delivered demo film was returned twice by the user — once for an error dialog nobody had noticed, once for interactions that read as canned playback.** (10) *Mark what is being chosen, not just where the cursor is*: a visible pointer on the combo box or the radio group is not the same as a marker on the option being picked, and a viewer watching eight form fields fill in with no visible act of choosing reads the whole beat as a recording of a script rather than someone using the app; the fix is mark-hold-click-release on the option/label (click the `input`, mark the `label` — the label covers the input in most frameworks), and it must be marked BEFORE the click, because a marker that appears and disappears inside one animation frame is not in the film at all. (11) *Take the external dependencies down or up on purpose, before rolling*: a demo runs the real app, so every outbound dependency is in the film whether anyone decided about it or not — measured 2026-09-12, the mail relay constant pointed at `127.0.0.1:1025` with nothing listening and an **Error / Connection refused** dialog sat over the app at 2:08 of a delivered film, immediately after the registration hand-off, the one beat in the tour meant to feel like success; the recorder exited 0, reported zero gaps, and could not have known. Down on purpose is legitimate (rule 8 records warts) but then it is captioned; unplanned is neither, so list the dependencies the path touches, decide each, prove it (`aiosmtpd` + a `noop()` returning `(250, b'OK')`), and sample frames around every commit-shaped beat rather than only the head of the film. (12) *Fit the running time afterwards, never race the app to hit it*: a length target is a cutting-room problem — a pace multiplier of 0.45 against a 1.7s per-beat floor moved a nine-minute film by well under a minute, because the app's own response times do not shrink; re-time the finished file uniformly with `setpts=PTS/F` and refuse above **1.6×**, the readability ceiling (1.10–1.20× is invisible and is usually all a tour needs). If it is still long at the cap, cut a whole act and re-record — never shave the holds rule 5 bought — Maurits Visser, from a MOC/PSSR app replacement

## 2026-09-12
- new(teamserver-alignment): **the recurring Team Server operation had no toolkit skill — only a birth skill and a project-local copy, and a push was reported to the user as blocked three times across two sessions while not being blocked any of those times.** `platform-link.md` covers creating the app, PAT scopes, adopting a model without rewriting history and deploying; it is read once per project. Nothing covered the thing every later session does: *which of these three remotes does the team actually run from, and did my push land.* Measured on a MOC/PSSR app replacement, 2026-09-07 → 2026-09-12 — three remotes, two of them Team Server URLs and one a dead app id from an earlier attempt, so *"push to Team Server"* with no remote named is not self-evident and a push to the wrong one is silent: git reports success and the team runs yesterday's model. The four checks that have to fail before the word *blocked* reaches the user are the point of the file, because **all four read as something they are not**: (1) the credential is usually already in the environment and a single guess at a variable name returning empty is not evidence of absence; (2) **a wrong username fails identically to a wrong token** — Team Server answers `fatal: Authentication failed` for both, and the identity it wants is the user's Mendix login, not the GitHub handle and not `git`/`oauth2`, so suspect the username first; (3) probe with `git ls-remote` printing a fixed label only — **never interpolate the credential into probe output**, which on 2026-09-11 leaked a live token into a transcript and cost a rotation because the token was one of the candidates being tried; (4) an inline `credential.helper` shell function is refused by a sandbox as credential exploration and the refusal looks like a git failure, so use askpass reading from the environment. Two messages that are normal and are **not** the block: `warning: --negotiate-only requires protocol v2` and `push negotiation failed; proceeding anyway` — the push lands, and the verification is `git ls-remote <remote> main` matching your local head, never the absence of an error. Deliberately carries **no remote table** (authoring rule 5): the file says `git remote -v` and read the project's own declaration, because a cached table goes stale the day someone adds a fourth remote — which is how the dead app id got there. Completion criterion carries its denominator: three matching values — remote name, sha pushed, sha read back — or the name of which of the four checks failed — Maurits Visser, from a MOC/PSSR app replacement
- process(skill-routing): `teamserver-alignment` routed **baseline** for mdl/gate/review at stages 5–7, trigger-phrased on *"any session that will push a model to Mendix Team Server, and BEFORE telling the user a Team Server push is blocked"*. Baseline rather than situational is the whole fix: the knowledge existed in two places the whole time — `platform-link.md` at tier `ondemand`, and a project-local `.ai-context/skills/git-teamserver` that cannot travel to the next project — and **not being reached is precisely what the incident was.** `platform-link.md` gains a pointer to it and keeps its birth-skill scope — Maurits Visser, from a MOC/PSSR app replacement

## 2026-09-10
- fix(bug-logs/mxcli-bugs, learned-mdl-preflight): **the `marketplace install` STOP (#879) is LIFTED — fixed upstream in v0.21.0, and this toolkit told four days of sessions otherwise.** Install no longer shells out to `mx module-import`; it copies units with mxcli's own writer and MPR v2 survives, with the destructive path now opt-in behind `--allow-format-change` so the corruption can only be produced deliberately. Re-probed 2026-09-10 on a real 501-unit split-model project: **501 → 1075 `.mxunit`, `.mpr` 84 KB → 156 KB** (an index, not the 35 MB blob this bug used to produce), native mxbuild **0 errors**, app boots, two e2e journeys unchanged. **The cost of not re-probing is the finding, not a footnote:** v0.21.0 shipped 2026-09-06 and the project carried *"marketplace install is BLOCKED, needs a human in Studio Pro"* in its register until 2026-09-10 — with both `.mpk` files already downloaded and sitting in the project root the whole time, because the blocker was never the download. `retesting-learned-rules.md` exists for exactly this and nobody ran it; **a STOP is a claim about a binary version and it expires.** Also records the zero-risk way to re-probe a formerly-corrupting command on a real project — `git clone --depth 1 file://$(pwd) /tmp/probe`, install into the clone, count `.mxunit` and run `mx check` there, decide from the measurement — which is cheaper than the documented recovery and answers what a changelog cannot. **Three new traps replaced the old one, all live on v0.21.0 and none self-announcing:** (1) marketplace packages **do not carry their dependencies**, and the failure is not "module missing" but 406 individual `CE1613` element errors — the aggregation one-liner that turns that wall into a shopping list is in the entry, and the closure for Email Connector 6.4.3 is MxModelReflection + Encryption + CommunityCommons, which checks at 0; (2) the two `mxcli fix` repairs the installer prints are **mandatory, not advisory** — 128 units and 12 units changed on the confirming run, and both exist precisely because Mendix's own tools would collapse MPR v2 without them; (3) a **bundled widget can silently fail to land and the installer cannot self-repair it** — `widgets/RichText.mpk` landed on the probe and not on the real run (33 vs 34 widgets, 3 × CE0462 over an otherwise clean model), a re-run refuses with *"already installed"*, and the repair is to `unzip -o -j` it out of the `.mpk` the installer already downloaded, so **count `widgets/` before and after every install**. One thing genuinely still needs Studio Pro and is now scoped rather than assumed: SAML + UserCommons install cleanly but leave `CE0066` ("Entity access is out of date"), there is no `mxcli fix security`, and an MDL `GRANT` was tried and does not clear it — Maurits Visser, from a MOC/PSSR app replacement
- learn(learned-css-that-never-applied): **the difference between a UI the user called "poor" and one they called "much better", same day, same design system, was that the first pass verified the SCRIPT EXECUTED and the second verified THE PIXEL CHANGED.** Everything green in the first pass was true and irrelevant. Three ways a perfectly correct rule paints nothing, all three met in one field run on a MOC/PSSR app replacement, 2026-09-10: **(1) matches nothing** — `ds.css` styled `.ds-tablewrap`/`table.ds-table`, but DataGrid2 renders a row as `<div class="tr">` and there is no `<table>` on any page, so not one table rule had EVER applied, through a full design port, two build phases, mxbuild clean, lint clean and two journeys green; and three top-bar selectors (`.navbar`, `header.navbar`, `.mx-layoutgrid-fluid`) were guessed in one sitting and all three matched zero elements, where one read of the live element chain named the real carrier `.mx-scrollcontainer-top.region-topbar`. **(2) matches too much** — the instinct after (1) is to widen, and `.mx-page { background }` painted the nav bar with it: white menu text on a white bar, an invisible top nav. **(3) matches and loses the cascade** — `.mx-listview-empty` named exactly the right element, sat ~11,000 lines after Atlas in the compiled sheet, and still lost: Atlas ships `.mx-listview > ul .mx-listview-empty { border-style: none; background-color: transparent }` at **0,2,1** against a bare class's **0,1,0**, so source order was never consulted; and that declaration means Atlas is *erasing* the element, not merely failing to style it. The diagnostic is two reads before you touch the rule — computed style, then (only if not found) a six-level `parentElement` walk printing tagName+classList — and the three possible answers have three different fixes, so **a blind rewrite would have been wrong in all four field misses, and in case (2) the rewrite WAS the outage**. Completion criterion carries a denominator: *every component class a pass claims to have shipped has one computed-style read against a named element in the running app, or is reported unverified* — the run that justifies it had row tone implemented, screenshotted and **looking fine while rendering a rejected project amber**, caught only by reading two values. Also records the check that comes first and no CSS instrument can see, because nothing is wrong with the CSS: `check-design-reaches-app.sh` counts classes that ARRIVED in the built sheet, and at the moment the user called the UI poor, four complete semantic tint sets, a five-step elevation scale and a styled empty state were all present, all correct, and referenced by **zero widgets** — the design system was finished and the app was grey. Measure it as *N of M component classes have at least one carrier in the model*; the fix for a zero-carrier class is never to rewrite it, it is to bind it. Three of the five improvements the user reacted to were pure binding of CSS already sitting in the file — Maurits Visser, from a MOC/PSSR app replacement
- learn(learned-mdl-cannot-express): **when MDL cannot set a class, CSS usually can — check before you go looking for the property.** A UI spec carried per-row status tone on three grids as an `M`-sized MDL task prescribing `DynamicRowClass`, a property that exists in neither MDL nor Studio Pro; it had been written months earlier against an imagined API, the same defect as an instrument built against imagined input. The requirement was met with **zero MDL**, because whatever class MDL *can* bind is a hook the stylesheet reads from anywhere in that element's subtree: every grid already bound `DynamicCellClass: 'ds-statecell ds-statecell--<state>'`, so `.tr:has(.ds-statecell--rejected) > .td` gave three pages row tone with no script, no exec and no gate run. Same move fixed a second item the same day — `.ds-card > .ds-cardhead` is unwriteable from a page because a Mendix class lands on its own element only, and `.ds-card:has(> .ds-cardhead) { padding: 0 }` says it. Two caveats: **source order inside your own rule set becomes load-bearing** (an overview row carries two status cells, matched several rules at equal specificity, and rendered a rejected project amber until the rules were reordered weakest-claim-first), and degradation where `:has()` is unsupported is silent and benign — the selector never matches and you get today's appearance, which is why it must be confirmed by value read and not by eye — Maurits Visser, from a MOC/PSSR app replacement
- process(skill-routing): `learned-css-that-never-applied` routed **baseline** for mdl/review/gate at stages 5–6, trigger-phrased on *"a style change that appears to have done nothing, or an app still grey after a design port every instrument called green"*. Baseline rather than situational on this repo's own rule that a skill living only in the situational table goes unnoticed by projects that are not hunting for it — and not being reached is precisely what this one records — Maurits Visser, from a MOC/PSSR app replacement

## 2026-09-09
- fix(bin/sync-project.sh + bin/check-portability.sh): the retire-ledger-row rewrite passed its regex to awk as `-v re="^\\|..."`; gawk (the GitHub runner's awk) processes escapes in -v values, so the pattern became an alternation that matched every line and the whole CLAUDE.local.md was replaced with copies of one row — green under mawk locally, red only in CI. Bracket expressions now; check-portability flags any backslash inside an awk -v value — the toolkit merge queue
- process(tests/ab + process/token-path-ab-2026-09-09.md): the token-path A/B — answer-key corpus generator and scorer (synthetic Harbour Berth Booking, stdlib only), and the report: wave 1 Sonnet P–2 (branch 31% cheaper, recall 54%→81–94%), wave 2 Haiku P–4 on three corpora (4–8 min, <$1.10 each), the non-reproducing hollow run, the per-stage mandated reading load, and the proportionality finding — the toolkit merge queue
- process(skills/conversion-runbook.md → Entry Modes): **the docs-ready fast path for a requirements-driven, text-native corpus (Markdown + HTML, no legacy code, no Office/PDF containers)** — measured cause four on the same project: the ledger wanted one disposition per file, every sidecar png/css/js included, for a corpus nothing extracts. Stage 0 = `source-sufficiency.sh init` + CAC-1 + sign-off, triage rows written `N/A (text-native corpus)`; Stage 1 = `bin/html-to-md.sh` once, the KB IS the converted text + `documents-index.md`, ONE ledger mark (`mark <p> 'sources/**' --artifact analysis/knowledge-base/documents-index.md --evidence ... --by ...`), images still read by vision with the index's count as denominator; Stage 2 = one use case per section with a `sourceRef`, rules lifted as stated, validation once; Stages 3–4 unchanged and not skippable (module boundaries, grants, script order). Every command was run against the real gates first, and two things had to change for it to pass: (1) `--adopt 1` is the wrong instrument — adoption waives the stages *before* the point (Stage 0, which ran) and leaves Stage 1 demanding an extraction report no extractor wrote — so the path records `--waive 1 --reason "text-native corpus: ..."` (Stage 2 still runs the ledger: index hidden → Stage 2 exits 1, verified); (2) `source-ledger.sh mark` now strips the source-root segment from a target, so `'sources/**'` — the natural spelling — matches the corpus instead of being refused. Verified that a glob mark whose artifact names every basename yields EXTRACTED for every row, css/js/png included, and that image rows owe no `--media`. `bin/lib/source-formats.tsv` gains route `chrome` for css|map|woff|woff2|ttf|otf|eot|ico ("web export chrome — listed, never content"; `.js` deliberately stays on the code route — one extension, one route, and a `.js` in a migration corpus is application source); `bin/html-to-md.sh` is routed baseline (ba, stages 0–1: "HTML in the source corpus — convert once before anyone reads it"), surfaces re-rendered, baseline 74,147 of 80,000 words. Fixture: tests/wave2/test-source-ledger.sh T12 — the toolkit merge queue, from a Markdown+HTML requirements project
- new(bin/gate-check.sh + skills/conversion-runbook.md): **a stage run names the runbook lines to read — `Read for this gate: skills/conversion-runbook.md §"Stage N — …" (lines A–B) + §1b Live Checklist (lines C–D)` — instead of the whole 11,700-word file.** Measured cause three on the same Markdown+HTML requirements project: every project's CLAUDE.local.md says "read the runbook FIRST — every session", so every session read all of it. What a stage session needs is §1b (applies to every stage) plus its own stage's §2 section; the spans are derived on every run by grepping the runbook's actual headings (`### Stage N — `, `## 1b.`; keyed on the em dash because `### Stage 0 runs in every entry mode` also starts with `### Stage 0`), never hard-coded, and `build-ready` reads Stage 5's. The runbook's top now carries the matching "How to read this file: not whole" paragraph. Fixture: tests/wave2/test-bug03-gates.sh T10 (line numbers checked against the file's headings, span ends at the next heading, build-ready → Stage 5, whole-project run prints none); test-source-ledger.sh and test-stage-p.sh still pass — the toolkit merge queue, from a Markdown+HTML requirements project
- fix(bin/sync-project.sh): **retires the always-on `bug-logs/mxcli-bugs.md` row in a project's CLAUDE.local.md → `bin/bug-lookup.sh`, and says so.** Measured cause two on the same Markdown+HTML requirements project: master replaced the 47,500-word ledger's baseline row with bin/bug-lookup.sh on 2026-09-08, but every project scaffolded before that still read the whole ledger every session. A marked routing block was already re-rendered by section 2d — silently; now the retirement is announced ("retired the 47k-word ledger row → bin/bug-lookup.sh"). A hand-written, UNMARKED `## Baseline routing` table (every project bootstrapped before 2026-08-18) got 2d's whole-table refusal and kept the row; new section 2e rewrites that ONE row in place with the row's own path prefix kept and every neighbouring row byte-identical (new `routing_row <name> [prefix]` in bin/lib/skill-routing.sh renders it from the table). CLAUDE.md's "mxcli-project-toolkit Integration" block is NOT edited — it is bootstrap-project.md's LLM merge, not something init-project.sh writes — the row is reported with the exact replacement instead. --dry-run announces and writes nothing; a second run is silent. Fixture: tests/wave2/test-bug12-sync.sh T11 (marked, unmarked, dry-run, idempotence, CLAUDE.md report) — the toolkit merge queue, from a Markdown+HTML requirements project
- new(bin/html-to-md.sh + bin/lib/html-to-md.py): **convert a saved-webpage corpus to Markdown once, so no session reads raw HTML again.** A requirements-driven project whose sources were Markdown plus browser "save page as" exports (page + `_files/` sidecar) was slow and token-hungry through Stages 1–4; measured cause one: agents opened the .html themselves, at 3–10× the tokens of the page's own text, every session. Now `bin/html-to-md.sh <project>` walks the source root (the inventory's own walk — bin/lib/source-inventory.py, same prune rules) and writes each page to `analysis/knowledge-base/text/<same path>.md` — headings, lists, tables, links, pre/code, bold/italic kept; script/style/noscript/svg/comments and nav/sidebar/footer/cookie/breadcrumb chrome dropped, conservatively (never `<main>`, `<article>`, or anything holding an h1–h3); every `<img>` as `![alt](src)`, inline base64 images decoded to `<page>_images/NN.ext` so a vision read needs no HTML. Each .md opens with source, raw size, words, image count and the section list with md line numbers — the read's denominator. Plus `documents-index.md`: ONE row per file under the root, sidecar png/css/js included, with role (page · markdown · image of <page> · chrome of <page> · other), so a single ledger glob mark can point at it. Idempotent (skips up-to-date pages; `--force`). Prints raw KB → md words and the ratio per page. Stdlib `html.parser` only, bash 3.2. Fixture: tests/wave2/test-html-to-md.sh over a synthetic export (its CAPTURE.md says why synthetic is acceptable for a format with no tool contract), 67 assertions: structure kept, every chrome sentinel absent, base64 written and referenced, section line numbers verified against the file, index names every basename, idempotence, --force, ratio printed — the toolkit merge queue, from a Markdown+HTML requirements project
- new(project-bin/page-fidelity.js): **`class="bound"` — the wireframe author saying "this text is a sample of bound data".** The scorer already had this concept and applied it structurally: `<td>` is excluded from the text corpus because a wireframe's table cells are sample ROWS and a page that correctly BINDS them contains none of that literal text. The same is true of a bound value anywhere else on the screen, and there the structure gives nothing away — an `<h1>` holding a record's number and name looks exactly like an `<h1>` holding page copy, and a chip holding a filename looks exactly like a chip holding a label. Measured on a MOC/PSSR app replacement's project detail page, 2026-09-09: **54%**, and all three misses were sample values the page could not contain — the record's own title, and a mocked attachment's filename, twice. The page was right; the denominator was wrong, and the number was about to be read off as a gate verdict. So the marker is **DECLARED, not guessed**: an element carrying `bound` leaves both the text corpus and the class denominator, and guessing was the alternative and does not work — a heading is bound or it is not, and only the person who drew the screen knows which. Put it on the smallest element that holds only the value (`<h1 class="bound">` leaves no heading behind; `<span class="bound">` inside a chip leaves the chip). Fixture grows to **18 assertions** including that a bound heading leaves the DENOMINATOR rather than merely stopping being reported as missed — otherwise the score would be 1 of 2 and still look clean. Known-bad control without the marker: **5 of 18 fail**. Same page scores 100% (3/3 headings, 2/2 content) with it — Maurits Visser, from a MOC/PSSR app replacement
- process(bug-logs): BUG-114 filed upstream as mendixlabs/mxcli#1076 with BUG-118 as its second reproduction; both drafts and the ledger header stamped FILED — the toolkit merge queue
- fix(project-bin/page-fidelity.js): **structure classed as bound data, and the score of record silently going `null%` again.** A wireframe's own `<style>` defines two very different kinds of class — BOUND-DATA MOCKS (a repeated region of sample rows, whose literal text a correctly *binding* page contains none of) and STRUCTURE (the wrapper, the page header, the toolbar — furniture the page script genuinely must declare) — and the classifier **drops a mock's whole subtree**, so a misclassified structural class deletes page content from the denominator. The test was `uses === 1 && kept < 0.4`, once-used AND dominant, which contradicts the paragraph directly above it stating that a mock is a **repeated** region: a once-used class whose subtree was *small* fell through the second conjunct and was dropped as sample data. Measured on a MOC/PSSR app replacement's project overview, 2026-09-09: `.page-head` (uses=1, kept=0.513) was deleted **with the page's only `<h1>` inside it**, `.toolbar` (uses=1, kept=0.825) with it, and every dimension reported `0 of 0` — which normalizes to **`null%`**, so the run printed a clean report over an empty corpus. That is the same silent-null failure this file's own header note says was worth fixing, arriving a second time through the other conjunct, on a page that scores **100%** once the page is not gutted. The threshold is **removed rather than retuned**: it was a guess, and any value for it splits once-used wrappers on a measurement that has nothing to do with whether their content is bound data; a repeated region is still a mock at any size, which is the half that was always right. New fixture `tests/wave2/test-page-fidelity-mocks.sh`, **14 assertions**, with two captured wireframe shapes and a `CAPTURE.md` stating that only literal text and class prefixes were changed. Its **first assertion is that the score is not `null`, not that it is high** — a null on a wireframe that draws an `<h1>` means the scorer gutted the page, whatever the eventual percentage. Proven against a known-bad control carrying the old rule: **8 of 14 fail** — Maurits Visser, from a MOC/PSSR app replacement
- learn(workflow-structure-rules, learned-workflow-patterns): **two workflow signatures that `mxcli check` passes clean and the native build rejects**, plus a v0.21.0 re-probe of the four constructs a real approval chain needs. (1) **A user task's `PAGE` must take a `System.WorkflowUserTask` parameter, not the workflow's context entity** — a context-parameterised page passes `mxcli check --references`, execs, round-trips through `DESCRIBE`, and the native build then says *"The selected page 'X' should accept a parameter of type 'WorkflowUserTask', but expects parameters of types 'MOCProject' instead"*. (2) **A targeting microflow must accept TWO parameters, `System.Workflow` AND the context entity** — one parameter is the natural thing to write and it is wrong. Neither signature appears anywhere in `mxcli syntax workflow`, whose own targeting example is `TARGETING MICROFLOW HR.GetApprovers` with no signature stated. **The expensive half is the planning consequence: the task pages are a PREREQUISITE of the workflow row, not a later UI row** — a plan that builds the workflow at row 41 and its task pages at row 45 cannot make row 41 green, which is exactly how one project's Phase 7 was ordered. Also: with the signatures right, user task + targeting microflow, **multi-user task**, `CALL MICROFLOW` with a quoted `WITH`, and a **backward `JUMP TO`** inside an outcome all build clean on v0.21.0. That result is only worth having because **the same run included the construct known to be broken as a control**: a scripted `DECISION` passed `mxcli check`, printed `Created workflow`, and left the project unopenable (`StorageLoadException` → `BUILD FAILED`), so BUG-76 is open on v0.21.0 and the probe demonstrably distinguishes working from broken. A probe with no known-bad control cannot tell *"it works"* from *"the probe is blind"* — this repo has already published one wrong conclusion for that exact reason (BUG-121) — Maurits Visser, from a MOC/PSSR app replacement
- new(project-bin): **`check-design-reaches-app.sh` — did any of the design system ARRIVE in the built app?** The gap it closes is a sentence in its own sibling: `check-design-portability.sh`'s header opened by asserting *"a design system's tokens port into Mendix perfectly — colours, radii and spacing variables survive the SCSS port intact"*, which is false and was the sentence that made everyone comfortable. Tokens survive the port into a **file**; nothing about landing in the file makes the framework read them. Measured on a MOC/PSSR app replacement, 2026-09-09: 55 tokens sampled from the customer's own screenshots, ported correctly, into the right file, at the stage that owed them — and the app rendered every screen in Atlas's default blue for **two more build phases**. Three independent failures, none visible to anything in this toolkit: the bridge assigned **SCSS variables** (`$brand-primary: var(--brand)`) to a theme running `$use-css-variables: true`, so it compiled to nothing (`--brand` present, `--brand-primary` untouched, `var(--brand)` **0 occurrences** in the built sheet); the **20 component classes were never ported at all**, so `ds-muted` on 12 live widgets resolved to the body colour; and a theme module's `:root` is emitted **before** `theme/web/custom-variables.scss`, so even what it did set lost the cascade. `mx check` 0 errors, `mxcli lint` 0 errors, MDL suite green, both e2e journeys green — every one of them reads the model or the authored stylesheet, and the defect lives between the authored stylesheet and the built one. **Found by a human opening a screenshot and asking why the UI was poor.** The check deliberately does NOT look for a known default hex (a cache of one framework version — authoring rule 5); it inverts the question to *"is the winning definition of each knob the framework declares pointed at a design-system token"*, which needs no defaults table and survives the next Atlas release. Four passes with denominators (knobs bound · tokens arrived · classes arrived · dead SCSS bridge) plus a WARN for classes the **model** asks for that the built sheet never defines — which found 17 of 23 on the same project, `ds-help` among them, class names typed from memory onto live widgets. Requires a build and exits **2** without one, because "did it reach the app" cannot be answered from source, and answering it from source is how this shipped — Maurits Visser, from a MOC/PSSR app replacement
- fix(project-bin/snapshot-mpr.sh): **it now gitignores its own output.** It writes a FULL copy of the `.mpr` and every `mprcontents` unit before each exec and keeps the five newest, and nothing ignored it — so a project commits the client's whole model several times over, and then `mxcli exec` REFUSES to run, because `exec.sh` reads the pruned snapshot as uncommitted model changes: **the snapshotter's output blocking the snapshotter's own guard.** Measured on a MOC/PSSR app replacement, 2026-09-09: **2,089 snapshot files tracked**. Written in the snapshotter rather than in `init-project.sh` deliberately — there it also reaches every project that already exists, on its next exec, instead of only the ones scaffolded after today. A producer guarantees its own output is ignored — Maurits Visser, from a MOC/PSSR app replacement
- fix(project-bin/page-fidelity.js): **the same body-extraction defect as `check-page-shell.sh`, and one annotation-chrome class it could not reach.** (1) The page body was matched by a regex ending at `\n\}` — a closing brace at **column 0** — which finds nothing at all for an indented page. Measured on a MOC/PSSR app replacement, 2026-09-09: two wizard pages whose bodies are wrapped in a `LAYOUTGRID` — which **mxcli's own MPR010 advises** for any DataView holding inputs — close at two spaces of indent, so the tool exited 2 with *"no declaration of page X found in input"* for pages plainly there. A project that follows mxcli's advice could not score any page. Now the same character-counting, paren-tracking extractor the shell check got, for the same reason: a multi-line declaration's `Params: { $X: Mod.Entity }` opens and closes a brace before the body does. (2) `.anno` is **not** reached by the existing `annot` pattern, because `\b` after "anno" needs a non-word character and "t" is one — so a project whose annotation block is `div.anno` scored its own annotation as missing page content, reporting *"heading: Annotation"* as the first miss on a page whose real H1 is correct. Two faithful pages at **9%**. **Deliberately NOT added: a fourth content-boundary name.** The project's wireframe generator emitted `wf-wrap` as the screen itself with the annotation inside it, and `wf-head` for the page header — which the tool's own *"every `wf-*` div is annotation chrome"* rule then ate. Teaching the tool a name that contradicts its own convention was the wrong half to change; the generator now emits the documented `.wf-wrap > .wf-screen` shape with the annotation as a **sibling**, and renames its page structure to `page-head` / `page-actions`. Both pages then score 100% — over a thin denominator, and the reason is worth knowing: this project inlines its whole `ds.css` into every wireframe so they open standalone, which makes **every** design-system class look "wireframe-local" and moves the button labels into the not-scored bound-data set. So the action dimension is unusable here, `0/0` rather than wrong — Maurits Visser, from a MOC/PSSR app replacement
- fix(project-bin/check-page-shell.sh): **it had never passed once on a project whose wireframes are named after the SOURCE screens**, and it had no fixture, which is why both defects below sat in it. (1) The wireframe path was hardcoded `design/wireframes/<PageName>.html`; wireframes drawn from a legacy system are named after that system's screens (`moc-project-detail.html`) while Mendix pages are named in Mendix convention (`MOCProject_Detail`). Measured on a MOC/PSSR app replacement: 15 wireframes, 8 built pages, *"no wireframe"* for **every** page, exit 2 — a check that cannot find its input across a whole class of project is a check that gets switched off. Now an optional two-column `design/wireframes/PAGE-MAP.tsv`; absent map, unchanged behaviour; a page the map does **not** name is still reported **naming the map**, so the map cannot hide a page by omission; and a row pointing at a missing wireframe is reported, because a stale row is how a rename turns into a page nobody checks. (2) The page body was bounded *"to the closing brace at column 0"*, which reads the rest of the **file** for a page whose body is on one line — a nav-shell script with five such stubs reported *"page MOCProject_Overview declares 5 H1 titles; its wireframe draws one"*, and every number was the count for the whole file. **The worse kind of false positive: it fires on correct code, cites a real rule, and the fix it asks for would break four pages.** Two line-based repairs were tried and each broke the other case (a `Params: { $X: Mod.Entity }` brace closing on its own line ends a "started on any brace" scan before the body opens; a "started only when depth > 0" scan never starts on a one-line body), so the extractor now counts **characters** and tracks parens. New fixture `tests/wave2/test-page-shell-body.sh`, **18 assertions**, pinning both cases in one file because that is the pair that must hold at once, plus that the rules still FIRE on a real `Class: 'h1'` title and on two H1s in one body. What it found once it could run: **9 title widgets across 4 page scripts using `Class: 'h1'` instead of `RenderMode: H1`** — which is the same defect the screenshots showed, because `Class: 'h1'` leaves a DYNAMICTEXT an inline `<span>` and the caption rendered on the same line as the heading — Maurits Visser, from a MOC/PSSR app replacement
- new(obligations): **`design-reaches-app` — the second obligation below stage 5**, `mdl`/project/`from-stage 3`, artifact `docs/design-reaches-app.md` with a denominator, degradable. Owed by `mdl` rather than `review` deliberately: the agent that performed the port is the one that must show it landed. The reason the defect above escaped is not that the check was hard — it is that **nobody was owed an answer**, so every gate it passed through was green-by-absence. A project with no build yet discharges it by saying so (*"binding UNPROVEN, no build"*); silence does not — Maurits Visser, from a MOC/PSSR app replacement
- fix(project-bin/check-design-portability.sh): the falsified premise is **corrected in the header rather than quietly deleted**, with what it cost and a pointer to the new sibling. A header that states a wrong assumption confidently is worse than no header — Maurits Visser, from a MOC/PSSR app replacement
- learn(ui-preflight-pages): **hard rule B0 — before the FIRST page of the project, prove the design system reached the app.** `0 of N` on any of the three denominators means every class you are about to write resolves to nothing, and every later page inherits it. Placed at the consumer, in Step 2, because the ordering is the finding: the port happened at the stage that owed it and the pages were built on top of it unproven — Maurits Visser, from a MOC/PSSR app replacement
- process(install-manifest, skill-routing): `check-design-reaches-app.sh` installed into projects and routed **baseline** for architect/mdl/gate/review at stages 3 and 5, trigger-phrased on *"after the first build that follows any design-system port, and before any page is built on it"* — Maurits Visser, from a MOC/PSSR app replacement
- fix(bin/doctor.sh): the docker probe is bounded (`MXTK_DOCKER_PROBE_SECS`, default 8 s), announces itself before waiting, and can be skipped (`--no-docker` / `MXTK_DOCTOR_SKIP_DOCKER=1`). With Docker Desktop installed but stopped, `docker info` sat silent for minutes at the end of `init-project.sh` and the whole scaffold read as hung. A daemon that is down now gets the one start command for this platform (`open -a Docker`, the Windows `start` line, `systemctl start docker`) plus the re-run line; `--install` on macOS launches Docker Desktop itself. Fixture `tests/wave2/test-doctor-docker-probe.sh` (13 assertions: hanging, down, up, skipped) — a new builder's onboarding feedback, via Maurits Visser
- new(bug-logs): upstream draft for BUG-114 — `pending-github-issues/bug114-alter-page-replace-rescopes-to-outer-context.md`. One issue for the two symptoms of the same `mutator.go` walker defect: `ALTER PAGE REPLACE`/`INSERT` under a selection-driven data view re-scopes the binding to the outer entity (CE1613, invisible to `DESCRIBE PAGE`), and under a flow-sourced gallery template drops it (`<unbound>`, CE0402 — the former BUG-118 draft, now reproduction 2, not filed separately). Cites the exact functions (`findEntityContextInWidgets` → `widgetOwnEntity` → `EntityRef`-only readers) and that `Forms$ListenTargetSource` is written with no `EntityRef` — the toolkit merge queue, from the dashboard-publishing and language-learning conversions

## 2026-09-08
- new(obligations): **`workflow-count` — the first obligation below stage 5, and that gap is the finding.** Every existing row (look, sweep, journeys, coherence, skeleton) is `from-stage 5`, so `obligations.tsv` — the only mechanism here for *"a judgement pass owes a mark"* — did not reach the **design** stages at all, and `workflow-structure-rules.md` §12 is a ten-row count with a denominator per line that fired only if someone happened to run it. Measured on a requirements-driven app replacement: the agent read §11 for a construct question, filled §12 row 10, never ran rows 1–9, and shipped a build plan specifying a **role XPath** for four user tasks whose assignees are data on the record — which §6 calls *"not a near miss, it is a different application"*: one `Approver` role covered five populations, so every stage of every project would have landed in every approver's inbox. Found at Stage 4 by a human asking whether the workflow skills were being used, with the plan already written. New row is `project` scope, `denominator yes`, `from-stage 3`; `obligation-check.sh` ranks `from-stage` generically so **no code change was needed**. Deliberately not an `optin` row: an artifact a project must think of in order to owe is an artifact nobody owes. A workflow-free project discharges it in one line — *"0 workflows, nothing to count"* — the way §12's own rows 2/3/4/9 are written. Proven to go red both ways on a real project: PASS with the numbers, **PENDING** when the artifact is removed, **FAULT** on a mark carrying no denominator — Maurits Visser, from a no-code app replacement project
- fix(agents/architect-agent.md): **§12's ten rows are inlined, not cited.** The stub referenced `workflow-structure-rules.md` once and inlined nothing, against this repo's own authoring rule 2 — *"a skill only works when its text is in the acting agent's context; a citation is not a read."* The agent that owns Stage 3 now carries the count, the `workflow-count.md` artifact it writes, and the two rows that actually bite: row 5 (targeting mechanism named **with the sentence it came from quoted** — and §6's warning that a role XPath over a nominated assignee delivers the task to everyone holding the role) and row 10 (N constructs in the diagram, N rows in the plan). Added **outside** the `ROUTING:BEGIN/END` markers, so `render-routing.sh --check` stays in sync — Maurits Visser, from a no-code app replacement project
- fix(bug-logs): re-probed the two workflow blockers on mxcli **v0.21.0** (released 2026-09-06, Mendix 11.13.0) — **both still open**, and BUG-76's diagnosis in this log was wrong. BUG-121 (`PARALLEL SPLIT`): the same script executed through v0.20.0 and v0.21.0 on two throwaway copies produces a **byte-identical** workflow unit (zero diff from `bson dump` after blanking the `$ID` GUIDs), so the runtime behaviour cannot have changed — and the path contents *are* present in the stored BSON on both versions, so the entry's own title ("writes the paths but not their contents") names the symptom, not the mechanism, which is still unidentified. BUG-76 (`DECISION`): not unconditional storage corruption but a **pincer between two spellings** — a bare outcome passes `mxcli check` and makes the model unloadable (`StorageLoadException`, "not a valid EnumerationValueIdentifier"), while a qualified one is rejected by mxcli's own `MDL-WF03` and loads clean via `exec --no-check` with a single `CE0117` for the empty expression; mxcli mandates the spelling the loader rejects and rejects the one it accepts, which implies two independent fixes rather than one. The correction was made on the source project on 2026-09-02 and never promoted here; it is already filed upstream as mendixlabs/mxcli#1031, so it goes into the draft as a comment to post rather than a rewritten body — a topbar-titled portal project · at merge: the three "disputed" `DECISION` notes on master (`mxcli-bugs.md` v0.20.0 clearance table, `learned-workflow-patterns.md` §1 pointer, §8 version table, §21 verdict table) now cite the resolved pincer diagnosis and the v0.21.0 re-probe instead of "shape-dependent, not isolated" — the toolkit merge queue

## 2026-09-07
- new(bug-logs): seven mxcli defects harvested from a topbar-titled portal project's build (mxcli v0.20.0, Mendix 11.13.0), each with an upstream draft — BUG-120 (three write-path asymmetries; the serious one: `ALTER ENTITY … ADD ATTRIBUTE` reconciles only the widest access rule and silently skips narrower ones — a valid model with wrong security), BUG-121 (`PARALLEL SPLIT` writes its paths but drops their contents: loads, passes every gate, deadlocks at run time — `workflow-structure-rules.md` §11's *proven* row retracted, plus a new CE6681 forward-`JUMP TO` row and the `$WorkflowUserTask`-not-in-call-microflow limit), BUG-122 (`ALTER PAGE … SET PageSize` rejects what `CREATE` accepts on the same widget), BUG-123 (`SHOW CALLERS OF` indexes neither page-button actions nor `show page` targets), BUG-124 (`DESCRIBE MICROFLOW` omits `without events`, so a round-trip silently re-enables event handlers), BUG-125 (`check --references` cannot resolve any enumeration — four A/B probes), BUG-126 (`ALTER PAGE … SET Label` prints "Altered page" and discards the value while `SET Class` applies and `SET Caption` errors by name). Renumbered at merge from 113–119, all seven of which master had taken while the branch was open; `bug-submission-checklist.md` §0 now states the merge-queue rule (`BUG-DRAFT-<slug>` in PRs) instead of "grep the toolkit and take the next number", which this branch did and still collided — a topbar-titled portal project, merged by the toolkit queue
- fix(gate-check): the stage Surface check now also looks in `<project>/analysis/` — `source-sufficiency.sh report` and `triage-report.sh` write there by default, but the check only probed the project root and the knowledge-base parent, so every fresh project reported both Stage 0 surfaces MISSING while they existed (seen on the first Stage 0 gate of a MOC/PSSR rebuild POC, 2026-09-08; verified `Surface present` after the fix) — a MOC/PSSR rebuild POC

## 2026-09-04
- learn(mdl-preflight): STOP row 24 — `DesignProperties` on widgets in a `mxcli new` app: grep the theme's design-properties.json first; the scaffolded theme lacks Atlas's FormBase entries, so the property passes `check` and fails mxbuild CE6083 — greenfield pilot 2026-09-04
- new(bin): `status.sh <project-root> [--brief]` — one screen (or three lines): where the project is, what is done, what is overdue, and the ONE next action, condensed from gate-check, the obligation check, coherence-cadence, the doctor receipt, docs/BUILD-LOG.md and PROJECT.md. The NEXT line is an ordered lookup over instrument facts (each rule names its owning skill), never a new verdict. Wired as the session-start ritual's first read (init-project, sync-project, runbook §1b) and routed baseline. Motivation: gate-check answers "may stage N close?" in ~75 lines and, on a greenfield pilot at Stage 5, asked for source-sufficiency and a cutover row; nobody could answer "where am I" in one place. Field-run on the pilot (2 s) and on a stale workflow-PoC checkout (55 s — gate-check's cost, not status's; its NEXT correctly pointed at a stale blueprint render) — toolkit field review 2026-09-04
- new(bin): `doctor.sh --quick` — ~2 s (platform, mxcli/mxbuild/java EXECUTE with a 2 s bound, spawn speed on every platform, path hygiene, model layout incl. `app/`), writes a receipt whose second line is the environment fingerprint (mxcli version | mxbuild | arch). Doctor used to run once at install; the Git Bash spawn probe was gated to Git Bash; the project probe missed the two-tree layout — greenfield pilot 2026-09-04
- new(project-bin): `exec.sh` runs `doctor.sh --quick` itself when the receipt is missing, older than a day, or the fingerprint changed (warn-only, `SKIP_DOCTOR=1`), and prints its elapsed time with a "slower than a healthy run — run doctor" hint past 4 minutes. The build gate that reported 0 errors for several commits with a wrong-arch mxbuild (a workflow PoC, 2026-07) is the case — greenfield pilot 2026-09-04
- new(bin): `bug-lookup.sh <CE-code|BUG-n|keyword>` prints the matching ledger entries (headers only above 6 matches; `--index` lists all 115); routed **baseline** in place of the 32k-word ledger, which moves to on-demand. Always-on reading load measured 118,783 → 86,926 words (−27%) — toolkit field review 2026-09-04
- process(routing): `render-routing.sh --check` now fails when the baseline tier exceeds `MXTK_BASELINE_BUDGET_WORDS` (default 90,000, a ratchet: lower it, never raise it) and names the five largest baseline files; ROUTING.md said "keep baseline short" and nothing enforced it — toolkit field review 2026-09-04
- learn(ui-loop): the loop's cost, measured on the pilot: exec.sh 44 s with the watch's hot-apply overlapped inside it, journey + screenshot 3 s, ~47 s edit-to-screenshot; and a first-attempt instance of the skill's own "the theme, not the page" row — `Class: 'form-vertical'` passed every gate and changed nothing on screen. Also: `DesignProperties: ['Form orientation': 'Vertical']` on a DataView fails mxbuild CE6083 on a `mxcli new` (signal theme) app — the theme's design-properties.json does not carry Atlas's FormBase entries — greenfield pilot 2026-09-04
- new(skills): `walking-skeleton.md` — Stage 5 opens with one thin slice (entity, flow, screens, nav, demo user, journey with a failing control, screenshot) proven in the running app before the first module; routed baseline for mdl/gate/test, referenced from the runbook's Stage 5 and the build loop's head, and owed as the `skeleton` obligation (`docs/skeleton-proof.md`, denominator required). Field-run on a greenfield approval-request pilot in a cloud container the same day — the skill's own "Field run" section lists the seven defects it surfaced before any module existed — toolkit field review 2026-09-04
- fix(project-bin): `_common.sh` `find_mpr` also looks in `app/*.mpr` — the two-tree layout `cloud-dev-environment.md` prescribes made every project script (snapshot, exec, restore) say "no .mpr found" — greenfield pilot 2026-09-04
- fix(project-bin): `exec.sh` prints each mxbuild error's module/document/element and the expression parser's message, instead of `CE0117 Error(s) in expression.` alone — locating the failing activity previously needed a scratch-copy mxbuild run — greenfield pilot 2026-09-04

## 2026-09-03
- new(bug-logs): `pending-github-issues/render-paste-ready.sh` renders every NOT-YET-FILED draft into `paste-ready/<NN>-<slug>.md` (line 1 = title, rest = body, local header stripped mechanically) plus an `INDEX.md` in worst-first filing order with labels and the duplicate check done per issue; eight new drafts from the ledger's unfiled entries — BUG-102 (DataGrid `SET DataSource` silent no-op), BUG-104 (quoted `"$Param"` keeps the sigil), BUG-106 (widget names burned after rollback), BUG-107 (workflow `WITH` unquoted value segfault), BUG-109 (`JUMP TO` in boundary event), BUG-112 (`mxcli new` hangs on over-length path), BUG-100 (`docker init` without `COMPOSE_PROJECT_NAME`), STOP row 22 as an `exec` partial-application issue — and one feature request, `mxcli doctor`, written from `bin/doctor.sh`'s probe table. 22 issues paste-ready, 0 filed from this session (no `mendixlabs/*` access) — toolkit field review 2026-09-03
- process(routing): the always-on word budget now counts what an agent READS — skill documents — not the scripts it runs by their usage line: the tier held 97,769 words of which 21,216 were `.sh` (verify-module, source-sufficiency, source-ledger, status …) and 3,527 a `.js`, none of which ever enter a context window. Documents-only total 73,026; ratchet set to 80,000 and the check is **strict again** (the same-day advisory detour existed only because the number was wrong); `MXTK_BASELINE_BUDGET_ADVISORY=1` for a deliberate re-ratcheting pass. Tier-membership questions (`learned-stylegallery`, `design-spacing`, `ui-loop` baseline for every mdl session?) are left for the maintainers — this PR fixes the metric, not the roster — the toolkit merge queue
- fix(tests): both fixture suites green again — `run-tests.sh` 19/19 and all 30 wave2 fixtures. Bisected, not patched over: every failure was a fixture left behind by two deliberate 2026-08-20 exit-code changes (`40c63ac` PENDING → exit 3; `cec7a1e` Stage 0 advisory → exit 0) that updated only the fixtures their authors ran — `test-surface-column` now asserts the verdict word is *unchanged* rather than a literal FAIL, `test-bug04-subscripts` expects the documented codes and discriminates PASS/FAIL by the verdict word, `run-tests.sh`'s project now has an examined question file so the open-questions guard has something to examine, `test-triage-report` asserts `Stage 0 (Triage): FAIL` with exit 0 and the reworded reason. `test-bug05` T7 depended on a pre-fix gate copy that only existed on its author's Mac; it now materialises `778c3ff^:bin/gate-check.sh` from history. No gate behaviour changed. Also found by wiring them in: `init-project.sh` registers a scaffolded project's name in the *toolkit's* gitignored denylist, so fixtures scaffolding `t0`..`t10` had been appending `\bt0\b`… to the developer's real list on every run (and created one in CI, where the guard then flagged 200 files); `MXTK_LEAKGUARD_DENYFILE` now overrides the target, every scaffolding fixture points it at scratch, and `run-all.sh` refuses if a root denylist appears. **process(ci):** both suites now run in `checks.yml`, after the guard — they were red for nineteen days while every PR showed green because nothing ran them — the toolkit merge queue
- fix(bin/sync-project.sh, bin/lib/triage-template.sh): **`sync-project.sh` overwrote a signed-off `triage.md`, destroying 73 lines of Stage 0 decisions** — recovered only because the project had committed it. Two independent defects in `mxtk_triage_is_pristine()`, either sufficient: (1) the completion marker a filler writes, `MXTK-TRIAGE-STUB-FILLED`, is a **substring** of the `grep -q 'MXTK-TRIAGE-STUB'` guard, so the suffix that exists to say "done" said nothing — now anchored, and `-FILLED` is checked first as an explicit *not pristine* signal; (2) the header-strip `awk` needed `<!--` **and** the marker on one line, and the shipped template does put them there — but an agent filling the file in naturally *reformats* that header (wrapping it, adding a "filled in" note), after which `skip` never turns on and the header's own explanatory `{{PLACEHOLDER}}` counts as a live unfilled slot, so the file tests pristine forever. Skip now triggers on the marker alone. Plus the guard that does not depend on a regex being right: **sync refuses to overwrite a `triage.md` git reports as modified**, before the predicate is consulted. This is the third instance of one bug class in this function (the `{{DOUBLE_BRACE}}` false positive, ~33 KB of agent files; the unstripped one-line header, 2026-08-20; this) — each fix narrowed the inference, none replaced it, which is what the git guard is for. `tests/wave2/test-triage-report.sh` gains 7 assertions built by *reformatting the real template* rather than inventing a header: the suite already claimed to cover "sync must not clobber work" and its worked-on fixture carried **no marker at all**, so it was kept for the trivial reason and never reached the logic where the defect lived — hand-written input encoding the same assumption as the code. Scoped run: 62 passed / 2 failed, the same 2 failing before the change — Maurits Visser, from a no-code app replacement project
- fix(project-bin/build-plan-status.sh, bin/gate-check.sh): the **merged module brief** is now recognised. `brd-to-build-plan.md` prescribes merging the brief into `build-plan.md` under `## Module brief — <Module>` for single-module projects ("two documents at ~70% overlap is how one of them ends up unwritten"), and `exec.sh`'s advisory already read that shape — but the progress board tested only `architecture/modules/<Module>/module-brief.md`, so a project that followed the instruction showed `briefed: no` forever, and `build-ready` **failed**, with no way out except writing the duplicate the skill warns against. Board now reports `merged`; build-ready accepts either shape. Found on a real single-module project at its Stage 4 gate — Maurits Visser, from a no-code app replacement project **Detection is an alternation `(—|-)`, not a bracket class `[—-]`** — the class looked equivalent and could not work: a bracket expression matches one CHARACTER, the em dash is 3 bytes in UTF-8, and under a C/POSIX locale grep compares bytes, so it silently reported "no brief" on a file that had one. Caught on the first real project to run the code, minutes after it was written, and verified under both `LC_ALL=C` and `LC_ALL=C.UTF-8`.
- learn(platform-link): trap 6 — the project file's NAME is checked in three places and fixing one creates the next: git revision metadata (`pathspec 'App.mpr' did not match any file(s) known to git`, noisy but harmless), `mprcontents/mprname` in MPR v2 storage (`StorageMprNameDiscrepancyException` from `MprStorageManager.EnsureThatTheExistingMprContentsReferToThisMprFile` — fatal, and *created* by renaming the .mpr to silence the first), and `refs/notes/mx_metadata`, where a commit with no note makes Studio Pro fall back to the newest note in the ref — which is how 11.13 refused an 11.13.0 model insisting it was "on 11.14". Includes the plumbing to write mprname without disturbing a running app, and the SQLite `_MetaData` read for the model's real version. Also records the dead end: the fourth `_MetaData` column is not a filename hash (ten encodings tested) — ToeicBuddy, field run 2
- learn(platform-link): "do not flatten a two-tree repo" was too strong — replaced with the collision count that decides it. Measured: 218 colliding paths sounded fatal, 211 were byte-identical, 7 needed a real merge. Adds the three flatten traps that are not obvious — a symlink vs a directory of the same name is a type clash not a merge; an anchored `/app/...` .gitignore rule silently stops matching and in this case would have made a credentials file stageable; and whatever your tooling derives from the .mpr basename. Plus the size number the choice actually costs: 196 MB tree, 74 MB of it .mpr backups and 47 MB e2e video, cloned by every Studio Pro user — ToeicBuddy, field run 2
- learn(platform-link): flattened repos are one tree with two remotes — Team Server pushed first, the code host second, each confirmed with `ls-remote` before the next, because Team Server is the copy Studio Pro and the deployed environment read — ToeicBuddy, field run 2

## 2026-09-02
- fix(tests): `test-source-sufficiency-gate.sh` T1/T2 still asserted that Stage 0 exits non-zero on a missing or unrated rubric — gate-check loosened that to advisory on 2026-08-20 and the fixture was never updated, so it had failed on master for two weeks (found by running the scoped fixtures after the source-ledger work). Now pins what the 08-20 decision actually promises: rc 0, and the sufficiency line reads FAIL, never assessed — the toolkit merge queue
- fix(source-ledger): the name check lowercased raw bytes — ASCII-only, so a filename with Ä/Ü/ü never matched an artifact that plainly named it. Found by the fresh unattended rerun (session on the source-ledger branch, Stages P–3 in one afternoon): 18 of its 96 rows read "unverified", every one an umlaut name, and it diagnosed the cause in the script rather than in its own work. Now UTF-8-decoded, NFC-normalised and casefolded on both sides; fixture pins an umlaut module. Re-measured on the real corpus: the "33 modules named in no analysis file" from this morning's line is 15 — 18 were this bug. A coverage instrument produced a confident wrong number on the day the post-mortem about confident wrong numbers landed; the fresh run caught it because it measured the same thing a second way — the approval-workflow project, the rerun session
- process(post-mortem): `process/post-mortem-inverted-evidence-2026-09-02.md` — the build session's own failure analysis of the VBA migration, anonymised: the evidence hierarchy was inverted (a restorable `.bak` recorded as "unrecoverable" for five months while the analysis was derived from code and from BRDs derived from that code), absence recorded as a finding three times, presence counted instead of content read three times in one session, an intake claim propagated as fact, gates checking artifacts not truth, architecture derived from the lossy artifact, a ~130 that measured 19. Six toolkit changes, one per failure, each citing it: (F1) `query-the-model.md` gains the **evidence hierarchy** — production data > configuration tables > client statements > source code > our own earlier conclusions — and `source-formats.tsv` routes `.bak`/`.mdf`/`.bacpac`/`.dmp` to "restore and query", ranked top; (F2) `brd-validation.md` **check 7**: a negative claim names what was searched and carries an expiry, retired ≠ absent, a number carries its query or is marked estimate; (F3) **the ledger's verdict is hand-assigned**: `mark` now requires `--evidence "<finding → where>"`, and a pattern-covered file the artifact never names is a FAULT, not EXTRACTED-unverified — on the motivating corpus that turns 33 of 95 modules from a quiet count into 33 blocking rows, which is the honest state; (F6) `source-triage.md` anti-patterns "reading the interpreter as the specification", "conformance for correctness", "skipping the backup"; (fix 5) `bin/open-questions.sh` prints **RE-CHECK** — every source that arrived after the questions were answered and has no disposition yet, with the count of ANSWERED/ASSUMED/RAISED questions open against it (`recheck` in `--json`); (fix 6) `improvement-register.md` gains **Corrections**: a superseded claim, the measurement that overturned it, and where the old claim still stands. Fixture `test-source-ledger.sh` grows to 36 assertions — the approval-workflow project, the build session's post-mortem
- new(source-ledger): `bin/source-ledger.sh` + `bin/lib/source-inventory.py` + `bin/lib/source-formats.tsv` — **every source file must name the artifact that consumed it, or carry a person's waiver, before Stage 1 or 2 passes.** The incident: on a VBA migration the intake closed "documents not yet accounted for?" with *"the .pptx was already used by the triage pass"* — a claim about another document nobody checked; the triage was derived from the `.cls` exports, and the deck's 22 diagrams (the only statement of the workflow engine's X/M/E semantics) went unread for two months of BRDs, blueprint and build plan, reversing a CONFIRMED decision when finally opened. Three fixes, one shape each: (1) the Stage 0 inventory is now EVERY file under the source root minus a denylist — the extension allowlist that hid the deck had already been patched for `.yaml` (08-19) and code (08-31), and an allowlist is only ever as complete as the last incident; Office/PDF rows carry their embedded image and page counts, `init --refresh` appends files that arrive later; (2) a per-row disposition the gate greps — the artifact must exist, be non-empty and *name* the file, and for a container account for its pages and images — with `gate-check.sh --waive source/<file> --reason` as the one way to skip a file (the user's "if I agree to skip, skip"); (3) drift: files on disk with no inventory row block by name. Blocks Stages 1–2 (a file test, not a conversation test, so it clears skills-over-scripts); advises at Stage 0. Field-run on the motivating project (96 files): the intake's own claim replays as FAULT "never names the deck", the first extraction pass (3 of 22 images) replays as FAULT, the second passes, and 33 of 95 VBA modules turn out to be named in no analysis file at all. Fixture `tests/wave2/test-source-ledger.sh`, 32 assertions. Skill text: `source-triage.md` (Core Principle + anti-patterns), `interview-protocol.md` ("an answer that points at another document is a claim"), `conversion-runbook.md` Stage 0/1 rows, `document-discovery.md` completion rule — the approval-workflow project
- fix(commands): `mobile-dev-loop-prompt.md` no longer claims the build gate needs Studio Pro — the gate is `mxbuild`, a plain binary `exec.sh` invokes directly, and its only Studio Pro coupling is the `.mpr` lock check, a no-op where no Studio Pro exists; both mobile prompts, `testing-shape.md` and `e2e-harness-base.md` now know `mxcli run --local / --hub / --test-endpoint`, so a container stands the app up itself and publishes it at a public URL instead of hand-rolling a tunnel, and the harness `BASE_URL` no longer assumes `localhost:8080` (cherry-pick of 372e2ac, consistent with the cloud lane in `cloud-dev-environment.md` and the runbook's "Where you run this") — Maurits Visser's Mac parking branch, salvaged by the toolkit merge queue
- new(skills/existing-app-change.md): the entry mode the three existing ones did not cover — changing a live Mendix app that has no BRDs, no architecture document and no wireframes. The knowledge base comes from the model itself (**Path D**, `SHOW`/`DESCRIBE`, which cannot silently return zero the way a code extractor does), stages 2–4 run over the changed slice plus its **blast radius** (associations, writers, bound pages, module tangles, published services — the Stage 0 question with no equivalent elsewhere), the Track B regression baseline is the precondition rather than an option, every gate from Stage 5 on is also a regression gate, and the ledger denominator is the slice's leaves, said out loud. Routed as `ba,architect / P,0 / ondemand / spine`; `existing-app-assurance.md` now points here instead of back at the runbook — Maurits Visser's Mac parking branch, salvaged by the toolkit merge queue
- learn(workflow): `learned-workflow-patterns.md` §21 — a full v0.20.0 re-probe (2026-08-31, Mendix 11.13.0) rebuilt a 23-activity BPM-engine conversion workflow from pure MDL at 0 native errors: non-interrupting boundary timer and `wait for timer` cleared, `$Type` re-confirmed fixed, `DESCRIBE WORKFLOW` reads decisions/jumps/waits/boundary events back, `create or replace workflow` now refuses to delete an Event Sub-Process; four gaps remain (Event Sub-Process, End activities, body annotations, a three-fix `describe → exec` loop) and the `JUMP TO` trap is spelled out — mxcli names activities after the called microflow, a dangling target is stored as a self-reference, and `CE6681` names the wrong fault. Merged against master's newer state: the `DECISION` result is recorded as **disputed**, not cleared, because the toolkit's own same-day retest reproduced BUG-76 byte for byte with `decision '1 = 1'`; §8's version table, §10 step 5 and the Notes on scope updated accordingly — Maurits Visser's Mac parking branch, salvaged by the toolkit merge queue
- learn(preflight): STOP #24 — a `JUMP TO` target must be a real activity name and mxcli will not check it (passes `check --references` and `exec`, stored as a jump pointing at itself, `CE6681` at native check; `DESCRIBE` output walks you into it); the v0.16.0 `WITH`-clause gotcha is stamped as v0.16.0-only after 14/14 calls stored correctly on v0.20.0 — Maurits Visser's Mac parking branch, salvaged by the toolkit merge queue
- new(bug-logs): BUG-140 — a full page regeneration never wires a parameterized microflow datasource on a top-level `dataview` (CE1571 on 15 of 15 pages, even byte-identical to a working original; explicit `Params:` is accepted and dropped, enum literals are rejected outright); only implicit binding through one level of nesting works, proven on a throwaway page. Number 140 assigned at merge, clear of the 127+ block another branch was taking. Plus addenda where the branch's findings matched existing entries instead of new numbers: BUG-109 gets the general dangling-`jump to` case and its upstream issue #1005, BUG-110 its #1006 link and the second emitter defect #1007 (`annotation` statements `MDL-WF04` rejects), BUG-118 an earlier v0.18.0 two-entry `ContentParams` observation from a second project and the full-page-rewrite workaround; a "Cleared on v0.20.0" section records the workflow re-probe, with BUG-76 marked disputed against the same-day retest — Maurits Visser's Mac parking branch, salvaged by the toolkit merge queue
- fix(checkpoints): the last three `project-profile.md` references — a file no project has — now point at `PROJECT.md` → `## Decisions` (`checkpoint-design.md` Figma/brand answers, `checkpoint-scope.md` external refs, `checkpoint-architecture.md` NFRs; the third was not on the branch, found by grep at merge) — Maurits Visser's Mac parking branch, salvaged by the toolkit merge queue
- process(CLAUDE.md): "Testing this toolkit" now says there are two suites — `tests/run-tests.sh` is self-contained, each `tests/wave2/test-*.sh` takes its subject as `$1` and several take something other than `gate-check.sh` — so a permitted single-fixture run is invoked correctly the first time (checked still accurate: 24 fixtures, `run-all.sh` resolves the subject from the same `usage:` line) — Maurits Visser's Mac parking branch, salvaged by the toolkit merge queue
- new(evals): `evals/` — an agent-behaviour eval skeleton (does the agent ask the gate questions, post the live checklist, write CONFIRMED rows to PROJECT.md): one full Stage 0 scope scenario with fixture, expected questions, checklist and PROJECT.md schema, two grep-based assertion scripts, three stub rubrics, no runner yet. Written 2026-08-12 against that day's `checkpoint-scope.md`; the expected questions must be re-validated against the current checkpoint before a result is trusted — a parked branch, salvaged by the toolkit merge queue
- fix(gate-check): the source-ledger block for Stages 1–2 now respects a stage declared not required — `--adopt` at a later stage, `Waived stage N`, or an entry mode that skips it — instead of blocking before the verdict table was consulted (obligation and artifact checks already did); and the `source-ledger` manifest row's absence class is `report`, not `gate`, because nothing mandatory produces `analysis/source-ledger.html` (gate-check runs `check`, never `report`) and a row claiming to hold a gate shut over a file no step writes would sit PENDING on every migration project — the toolkit merge queue, reviewing the intake-gates branch
- new(gate-check): `--closeout <root> <stage>` — the stage close-out / next-stage-open block (`bin/lib/closeout.sh`), generated from the register, the artifact manifest and the routing table, pasted as-is at every gate (runbook §1b rule 7; CAC-3 and CAC-5 open with it): artifacts produced, decisions made in the stage (first 15, every non-CONFIRMED row always), carried forward (open questions with the status word that keeps them open, ASSUMED rows, UNSYNCED markers), the gate line verbatim **and in plain words**, then what the next stage does, how it is worked and tested, its checkpoints, the top-5 governing skills from `skill-routing.tsv`, and the optional artifacts on offer. Field run on TFC-TCXGraphPOC Stages 2–4: surfaced a PENDING coverage ledger nothing had flagged, 2 UNSYNCED markers, 5 open questions with status words outside the closed vocabulary ("TO", "REVERSED", "PARKED"), and the hand-written `architecture/workflow-definition.md` as an opt-in-by-deed; fixture `tests/wave2/test-closeout.sh` (32 assertions) — TFC-TCXGraphPOC
- new(gate-check): plain-words paragraph under every stage-query outcome (PASS, PENDING, FAIL, MANUAL, drift-blocked) — status in one sentence, what the gate needs, why specifically, what is still missing in plain names; asked for as "explain why a gate is blocked or what is pending, in human-understandable language" — TFC-TCXGraphPOC
- new(artifact-manifest): opt-in rows (absence `optin`): `workflow-design`, `swimlanes`, `sequence-diagrams`, `erd`, `nfr-sheet`, `integration-contracts` (Stage 3), `test-plan`, `acceptance-criteria` (Stage 4), `demo-script` (Stage 5) — offered at each stage open, owed only after a `Opt-in artifact <id>: <why>` register line or once the file exists; untaken ones summarised on one line per run, never listed as missing; the Offer step added to `checkpoint-template.md`, `checkpoint-architecture.md` and `checkpoint-build.md` — TFC-TCXGraphPOC
- fix(artifact-check): `_art_field` is one awk pass instead of a bash loop spawning `tr`+`sed` per register line — on the TFC register (~300 lines, 2 kB cells) a stage query took ~90 s and read as "Stage 2 hangs"; now ~1 s, same contract — TFC-TCXGraphPOC
- process(merge-queue): the toolkit-cli-improvements branch landed with six merge-review fixes — three duplicate upstream drafts (bug107/109/112, already FILED on master as #1023/#1024/#1026) and the committed `paste-ready/` render (15 of its 22 issues already filed) removed, the dir gitignored; `bug-lookup.sh`'s `mapfile` replaced (first real bash-4 call in `bin/`; `check-portability.sh` now flags `mapfile`/`readarray`); the `skeleton` obligation given a producer (`walking-skeleton.md` step 6 writes `docs/skeleton-proof.md`, the file the row reads — it would have sat PENDING forever); `walking-skeleton` routed ondemand and the baseline word budget made **advisory** in `--check` (`MXTK_BASELINE_BUDGET_STRICT=1` to fail): three consecutive merge commits went red on it while every one was an ordinary edit, and two of the five largest baseline entries are scripts an agent runs rather than reads — the report stays, the tier needs curating before it can block; CLAUDE.md's always-on list names `bug-lookup.sh` in place of the ledger; sync-project's ritual numbering aligned with init-project's; and `status.sh` calls gate-check with `--no-html`, because a status read that rewrites `index.html` leaves every wired project dirty (caught on the TFC second run) — the toolkit merge queue
- fix(gate-check --closeout): an early exit between the output capture and the emit point (nonexistent project dir, ambiguous register, --adopt/--waive misuse) used to vanish into the temp log — exit 1 with nothing on screen and the file left behind; an EXIT trap now restores the fds, prints what was captured, removes the file and keeps the exit code (probed: bad project path → message shown, rc=1, no temp file). Stage 7 no longer carries a hard-gate mark the runbook header does not — the toolkit merge queue, reviewing the stage-feedback-transition branch
- process(bug-logs): filed the BUG-120..126 harvest upstream — #1067 (write-path asymmetries), #1068 (parallel split writes paths without contents), #1069 (`ALTER PAGE SET PageSize`, citing #414 as the general case), #1070 (`DESCRIBE MICROFLOW` omits `without events`), #1071 (`check --references` cannot resolve enumerations). Dedupe redirected two: BUG-126 is mendixlabs/mxcli#292, open since April and the same bug, and BUG-123 is #773, closed as completed 2026-08-12 and still reproducing — both posted as reproductions on the existing issues rather than re-filed. Every draft carried a Mendix version but no mxcli version; v0.20.0 (from `mxcli-bugs.md`) is now in each Environment section, in the drafts as well as upstream. BUG-117 stays held pending its v0.20.0 retest, BUG-118 pending BUG-114 having an upstream issue at all — it has none — Maurits Visser, via the toolkit merge queue

- learn(bug-logs): BUG-133 — **MDL cannot author a microflow that applies entity access**, and because CE0114 restricts the *caller*, that means no MDL-authored flow can call one that does, from anywhere: not a startup flow, not a button handler. Whole marketplace features become unreachable headlessly — the field case is Excel Importer 11.2.2's own `_USE_ME` startup check, which re-syncs Model Reflection and validates every import template, and which took two apply-and-restore cycles to establish was a wall rather than a wiring mistake. Ask: a grammar property plus rendering it in DESCRIBE, or failing that raise CE0114 in `check --references` where the callee's flag is readable. Plus BUG-131's fourth and fifth cases, whose new information is that the snapshot restore is **partial across document types** — two failed applies left the page edits and the startup-microflow edit while none of the three microflows the same script created survived, leaving a model more broken than either endpoint; "2122 units verified" is the one measure that cannot see it — a sales-coaching build (renumbered from BUG-120/118 at merge; master had taken 113–126 while the branch was open)
- learn(skills/bug-logs): control-bar buttons bind the grid selection, not `$currentObject` — `learned-page-patterns.md` gains the rule with both failure codes (CE0117 for `$currentObject`, CE1571 for omitting the argument) and the tell that costs an hour: a module's OWN control-bar button describes with no argument at all while its microflow takes a parameter, because `DESCRIBE PAGE` does not render the stored argument — so copy the `-- Context: … $grid1 (selection)` comment, not the neighbouring button. Plus BUG-132: `mxcli fix design-properties` aborts on a duplicate GUID shipped inside a marketplace package (Mx Model Reflection 9.1.0), losing the rename pass for the other 2,467 units — and the half that matters, mxbuild reports 0 errors on the same model, so FAILED there is not "the install is broken" — a sales-coaching build (renumbered from BUG-119 at merge)
- fix(skills/conversion-runbook.md): the Stage-0 applicability table said the reuse-vs-build extraction call and the coverage matrix are **`N/A` for requirements-driven**, flatly — while `source-triage.md` and the shipped `triage.md` header say `N/A` only where the corpus holds no extractable structure, "on evidence, never on the entry-mode label". The runbook is the file every session must read first, so the unconditional version won: on a requirements-driven project whose corpus included a platform data export, following it literally would have left that export unparsed — and that parser went on to correct the documents written about the app (53 stored columns vs 128–175 documented, four spellings of one foreign key across four sheets, 15 sheets vs 17). Same shape as the `toolkit-guide.html` first-touch incident: conditional in its owning file, unconditional in the front door. Runbook now defers to `source-triage.md`, and gains a `document-discovery.md` row that was named in the entry-mode prose but missing from the table — Maurits Visser, from a no-code app replacement project
- learn(skills/source-triage.md): **document discovery is unconditional, extractors are conditional** — for a requirements-driven project the two are not alternatives: `document-discovery.md` runs over the whole corpus first (it is the inventory nothing else provides), then the extraction call runs once per extractable structure it found (code, DB schema, table dump, ORM model, OpenAPI contract, platform data export, entity tables inside a spec), and prose is read into BRDs via `kb-generation.md` Path B. `N/A` on the extraction rows is a finding about the folder, never a property of the mode — Maurits Visser, from a no-code app replacement project
- learn(skills/source-triage.md): extraction is decided on **two axes, not one** — medium (can the agent open the file at all) before content type. Text-native prose (`.md`, `.html`, `.txt`) is read, never classified by a script; opaque media (`.pptx`, `.docx`, `.xlsx`/`.ods`, `.pdf`, images, screenshots) need extraction even for their prose, because the alternative is not reading the source at all; structure is parsed either way. The Core Principle is untouched — a codebase, schema or export still gets an extractor, and this is not the manual-only path it forbids. Field evidence from a no-code app replacement: the data-export parser corrected its own source documents (53 stored columns vs 128–175 documented, four spellings of one foreign key across four sheets, 15 sheets vs 17), while a regex requirement-classifier over the same corpus's 3.5 MB HTML spec scored ~50% precision and ~50% recall against a user-confirmed sample and was deleted the same day — it read "what the recording covered" bullets as requirements, truncated sentences at inline tags, and matched no negative-form statement ("there is no role model"), where the agent's plain read of the same file was better in one pass. Also adds the denominator rule for large text-native files: emit the section list so the read has a bound — Maurits Visser, from a no-code app replacement project
- learn(platform-link): the single-tree adoption form — when the `.mpr` is already at the repo root there is nothing to `subtree split`; `git merge --allow-unrelated-histories -X ours` on a side branch gives the same fast-forward with `git diff --stat main ts-align` as the must-be-empty gate — and the `comm -23` unique-content count that belongs *before* any force-vs-merge choice: divergence says the histories differ, the number of paths on Team Server and nowhere locally says what a force would actually destroy (0 on a fresh `Initial app upload.`, 1042 paths all present locally). Field run on a Mendix 11.13.0 app: adoption built, pushed and confirmed by `git ls-remote` (fast-forward from the `Initial app upload.` commit, `refs/notes/mx_metadata` untouched) — a Mendix workflow-and-agents POC project
- learn(skills): **a correct rule with a popup-shaped name missed four full-page buttons.** `learned-popup-feedback-pattern.md` has said "never leave the user with no visible state change" since 2026-08-14, but its title, applies-to and every worked example spoke of popups only, so on a Mendix Workflow project it never reached the full-page workflow user-task screens. Measured 2026-09-08 by `DESCRIBE MICROFLOW` on all five task-decision buttons: **one of five** showed a message and closed the page, and **none of five** hid the decision controls once the task completed. The workflow advanced correctly every time — `set task outcome` fired, the DB state moved — so the standard e2e assertion ("the state moved") passed over the whole defect; the user's report was "it was for me unclear to see that the flow was completed". The rule is now stated as three legs (say it / move on / stop offering), scoped to any microflow behind a committing or task-completing button rather than to popups, with the workflow-task specifics: key visibility on `$WorkflowUserTask/State`, never on a domain attribute (two of three pages had no attribute for their outcome), and keep `set task outcome`+`close page` inside the success branch so a refusal leaves the task retryable. Its routing row is rewritten as a trigger condition per `CLAUDE.md` authoring rule 1, and the file now carries the DOM-side e2e assertion the defect class needs. — TFC-TCXGraphPOC (Mendix workflow + agents POC)
- learn(platform-link): trap 5 — a ~40 MB first import push dying with `remote: fatal: write error: Bad file descriptor` / `index-pack abnormal exit` is the HTTP transport, not the pack: same pack, branch and credential landed on the first retry with `http.postBuffer=524288000`, `http.version=HTTP/1.1` and git's low-speed abort disabled; the wording routes you to three wrong fixes (shrink, repack, force-push) and git has reported client-side success while the ref never moved, so confirm with `ls-remote`. Git username pinned down: `pat` is **case-sensitive** (`PAT` returns the same 401 as a wrong token — a username sweep that tries the uppercase form stops one keystroke from the answer), and the earlier "an email is rejected" claim becomes a measured table: the owning account's address is accepted, a non-owning one is not — standardise on `pat`, which does not depend on which address owns the app; Basic auth only, probe with `service=git-receive-pack` — an exam-prep app project
- new(bug-logs): BUG-117 (the widget-property writer silently drops any property name the target widget's schema lacks — LISTVIEW accepting DATAGRID paging names, GALLERY shorthand names that differ from its own schema keys — with no MDL-WIDGET07, clean `mx check`, and only the running app showing it) and BUG-118 (`ALTER PAGE REPLACE` inside a gallery template writes `ContentParams: [{1} = <unbound>]`, CE0402), both with upstream drafts. At merge, both verdicts settled from the mxcli source rather than the symptoms: **BUG-118 is BUG-114** — the enclosing-entity walker takes an entity only from a `DataSource` doc with an `EntityRef`, which a listen-to-widget data view never has and a flow-sourced pluggable list does not have either, so the target's scope is skipped and the walk keeps the outer data view's entity (re-scope, BUG-114) or nothing (`<unbound>`, BUG-118); one upstream issue, two reproductions. BUG-117's root cause is already acknowledged in mxcli's 0.19.0 notes (#928: a widget-type-agnostic allow-list, fixed for two properties only) — file the general case citing it, after a v0.20.0 retest — an exam-prep app project, verdicts by the toolkit merge queue
- fix(routing): `platform-link.md` moved from stage 7 to **P,5,6,7** — the platform home (Team Server app, PAT scopes, Free-vs-licensed) is a project-birth decision per `handoff-to-studio-pro.md` §10, and a build-stage agent asked "how do I get this onto Team Server" was not going to find a cutover-only row; trigger text front-loaded on the birth moment — Maurits Visser, via the toolkit merge queue
- fix(project-bin): three instrument defects found by running the module review on a real five-BRD module — `conformance-check.sh`'s watchdog slept the full timeout on EVERY probe (a subshell in `sleep` ignores SIGTERM until it returns: 24s per 0.144s DESCRIBE, ~12 min for ~5 s of work; now polls 1s and exits with the command), `graph-sweep.sh` looked for `.mxcli/catalog.db` at the project root on a two-tree checkout (third time this directory shipped the both-layouts assumption — F-020, F-042, now this), and `review-module.sh` measured ONE BRD of five and printed it as the module's coverage — it now states `1 of N`, and new `coverage-check-all.sh` runs every BRD against its own ledger in `coverage-ledger/<BRDID>.md`. At merge: the all-BRDs mode takes the explicit list the module's ledgers name rather than a glob over the shared BRD directory (a multi-module project would FAULT on the other modules' BRDs); the project name the author's own leak-guard denylist tripped on genericised in four headers; `ts-sync.sh` and `handoff-to-studio-pro.md` §10 now point at `platform-link.md` §3, which supersedes the content transplant for GitHub-born projects — a workflow-migration project, merged by the toolkit queue
- learn(skills/platform-link.md): day-one layout rule — one repository, `.mpr` at the root, artefacts beside it, Team Server as origin from the first commit; the subtree procedure is now labelled as the retrofit path for a project that grew up two-tree. Learned when the app's owner opened the Team Server clone and found none of the tests, docs or MDL scripts there ("let's do that from the start next time") — a dashboard-publishing migration project
- new(bug-logs): BUG-113 -- OQL `!= NULL` against a reference association is unreliable: `= NULL` returns 0 even with confirmed live orphans, `!= NULL` returns a number that happens to match a second, unrelated code path's rollup by coincidence rather than by implementing real IS/IS NOT NULL semantics for a reference. Found chasing a test-assertion failure that looked like a real cascade-delete bug; the live delete cascade was independently proven correct with a probe (create, delete through the app, recount), which is what exposed the OQL comparison as the thing that was wrong, not the app -- a dashboard-publishing migration project
- new(bug-logs): BUG-114 -- `ALTER PAGE ... REPLACE widget WITH {...}` re-scopes an inherited `ContentParams` reference to the page's OUTER data context instead of the widget's own enclosing scope, confirmed CE1613 at mxbuild -- and confirmed **twice, independently**, on two different widgets on two different pages of the same project, a day apart. `mxcli check`, the exec itself, and `DESCRIBE PAGE` all report success; only mxbuild/Studio Pro catch it, by which point the running app is down. Workaround documented (SET Content alone when only rewording, never REPLACE); no narrower ALTER PAGE route exists today for a genuine attribute rebind inside a nested data-source scope -- needs Studio Pro -- a dashboard-publishing migration project
- new(bug-logs): BUG-115 -- the file-upload widget cannot be authored in MDL at all: `filemanager` is a PARSE error (no such keyword in the grammar), and the pluggable `FileUploader` parses then fails at exec with "no definition for widget" because it is Studio-Pro-bundled rather than shipped as an `.mpk`, so `widget init` cannot extract it and no user-side workaround exists. Once a human adds it in Studio Pro, `DESCRIBE PAGE` correctly warns that the widget is not re-executable -- but the page is then permanently non-round-trippable, forcing a standing "ALTER PAGE only, never create or replace" rule on that page with silent data loss as the failure mode if a future session forgets it. Re-verified on v0.20.0 rather than carried forward -- a dashboard-publishing migration project
- new(skills/platform-link.md): linking an mxcli project to the Mendix platform — including the Free App section that decides whether ANY of it is automatable (deployment is Studio-Pro-only there, runtime settings and constants are unavailable so OpenTelemetry cannot be configured at all, and logs are live-only with no levels and no history) — create the app, adopt an existing model into Team Server without rewriting history, deploy. Written from a field run that adopted 144 commits of model work into a new Team Server app and deployed it. Four traps carry the file, because each reads as something it is not: the Platform SDK returns 403 on EVERY call from a proxied container (Node ignores HTTPS_PROXY — `curl` succeeds with the same token, so it was never auth); the Deploy API cannot deploy from a PAT while the Pipelines API can (the API with "deploy" in the name is the dead end); a first cloud environment cannot be created from any API at all; and Chromium cannot reach external hosts through an agent proxy, which reads as "the app is down". Also carries the 405-vs-401 rule that settles "wrong credential" vs "wrong URL" in one request, the literal-`pat` git username, and the subtree procedure with the two assertions that must hold before pushing so Team Server is never force-pushed — a dashboard-publishing migration project
- new(bug-logs): BUG-116 -- PAT-authenticated deploy exists, and NOTHING points you at it: it is the Pipelines API (`pipeline-portal.home.mendix.com/api/v1`, `POST /apps/{appId}/runs`, scope `mx:pipelines:write`, verified live -- a zeros UUID returns 404 rather than 401, so auth passed), while the Deploy API -- the one with "deploy" in the name, the one every search points at -- is a dead end: v4 is read-only (404 on all eight sub-resources tried) and v1 takes only legacy `Mendix-ApiKey`. Two wrong conclusions were reached and are both recorded rather than tidied away, because the route out was not an API at all but the PAT SCOPE LIST on a settings page naming `mx:pipelines:write`. Residual human step shrinks from every deploy to one pipeline setup, since `startRun` needs a pipeline design's UUID and the spec has no endpoint to create or even list one -- creating the app and pushing the model to Team Server is fully automatable with only a PAT (measured end to end from a headless container), but provisioning its FIRST environment is not -- Deploy API v4 answers `404` for an app with no environment and `405` on `POST /apps`, and v1, which is believed to carry the create-sandbox endpoint, rejects PATs for the legacy `Mendix-Username`/`Mendix-ApiKey` scheme. Includes the trap that the Platform SDK returns `403` on every call from a proxied container because Node ignores `HTTPS_PROXY` (fix: `NODE_USE_ENV_PROXY=1`) -- which reads exactly like a missing token scope and is not one -- a dashboard-publishing migration project
- new(bug-logs): seven mxcli defects harvested from a topbar-titled portal project's build (mxcli v0.20.0, Mendix 11.13.0), each with an upstream draft — BUG-120 (three write-path asymmetries; the serious one: `ALTER ENTITY … ADD ATTRIBUTE` reconciles only the widest access rule and silently skips narrower ones — a valid model with wrong security), BUG-121 (`PARALLEL SPLIT` writes its paths but drops their contents: loads, passes every gate, deadlocks at run time — `workflow-structure-rules.md` §11's *proven* row retracted, plus a new CE6681 forward-`JUMP TO` row and the `$WorkflowUserTask`-not-in-call-microflow limit), BUG-122 (`ALTER PAGE … SET PageSize` rejects what `CREATE` accepts on the same widget), BUG-123 (`SHOW CALLERS OF` indexes neither page-button actions nor `show page` targets), BUG-124 (`DESCRIBE MICROFLOW` omits `without events`, so a round-trip silently re-enables event handlers), BUG-125 (`check --references` cannot resolve any enumeration — four A/B probes), BUG-126 (`ALTER PAGE … SET Label` prints "Altered page" and discards the value while `SET Class` applies and `SET Caption` errors by name). Renumbered at merge from 113–119, all seven of which master had taken while the branch was open; `bug-submission-checklist.md` §0 now states the merge-queue rule (`BUG-DRAFT-<slug>` in PRs) instead of "grep the toolkit and take the next number", which this branch did and still collided — a topbar-titled portal project, merged by the toolkit queue

## 2026-09-05
- learn(bug-logs/microflow-patterns): BUG-130 — **`exec.sh`'s mxbuild gate passes relative to a broken baseline**: with a pre-existing error it prints "Gate passed (mxbuild-clean)" two lines after "the model still will not deploy"; the gate should fail on any post-apply error. BUG-131 — the snapshot restore after a failed apply **leaves page edits in place** (further field cases of BUG-106's family): the next apply trips on its own widget names, and a leftover microflow error becomes BUG-130's baseline. Plus three Mendix facts in `learned-microflow-patterns.md` from the same afternoon: no custom error handling inside a loop body (CE0644, wrap the call), `on error { }` is *with* rollback of the whole outer transaction (use `without rollback` for per-row isolation), and `substring` throws on strings shorter than the range — a sales-coaching build (renumbered from BUG-117/118 at merge)
- learn(bug-logs): BUG-115 addendum + its upstream draft (`bug115-file-manager-widget-not-authorable.md`) — a second project independently found that MDL cannot author a **File Manager** widget (six spellings all `mismatched input … expecting '}'`), and added the costly half: `DESCRIBE PAGE` of a page that HAS one emits it as a `-- NOT re-executable` comment, and that script **passes `check --references` clean** — so the documented describe → edit → `CREATE OR REPLACE PAGE` loop deletes the widget with no error at any gate. Verified against `AgentCommons.AgentImportExportFile_NewEdit`, which ships one. The comment is good behaviour and the draft says so; the ask is a grammar production, or failing that a `check` warning on the marker, since a pipeline reads the gate and not the comment — a sales-coaching build (logged on its branch as BUG-116; folded into master's BUG-115 at merge, the same defect)
- learn(bug-logs): BUG-127 (`ALTER PAGE … REPLACE` of a **pluggable** widget silently drops properties you did write — a Combobox came back with no `Attribute`, quoted and unquoted alike, and still rendered a normal-looking dropdown bound to nothing; `check --references` and mxbuild both clean, only `DESCRIBE PAGE` shows it), BUG-128 (`CREATE MODULE ROLE` has no `OR MODIFY` / `IF NOT EXISTS`, so a re-run aborts on it and every statement *after* it silently does not run — keep non-idempotent statements in their own once-applied script), BUG-129 (`exec` logged `Created microflow:` ×7 plus a page, passed the mxbuild gate and exited 0, and the model afterwards held the role and all six grants from the same script and **none** of the documents; root cause not established, logged as observed rather than diagnosed). The practice BUG-129 forces is worth having regardless of the bug: **read the model back with `SHOW` after every exec — exit 0 plus a log of `Created …` lines is a claim about what the tool tried, not a fact about the model** — a sales-coaching build (renumbered from BUG-113/114/115 at merge; those numbers, and 107–109 before them, were taken on master while the branch was open)
- new(guide/README): "Where you run this" visual — three lanes (cloud, devcontainer, local) on one shared project, the Windows Docker-on-WSL2 vs WSL-as-shell distinction, the build-headless/polish-in-Studio-Pro rhythm with the one-writer rule, and a side-by-side "why a Dev Container instead of local" table; SVG in `toolkit-guide.html` §5 (later sections renumbered), mermaid + prose in `README.md`; `doctor.sh`'s devcontainer note now says SP modes are unavailable *here*, not unavailable — same onboarding thread as the line below — Clinton Chee, via Maurits Visser
- new(guide/README): "Where you run this" visual — three lanes (cloud, devcontainer, local) on one shared project, the Windows Docker-on-WSL2 vs WSL-as-shell distinction, the build-headless/polish-in-Studio-Pro rhythm with the one-writer rule, and a side-by-side "why a Dev Container instead of local" table; SVG in `toolkit-guide.html` §5 (later sections renumbered), the same SVG exported to `docs/where-you-run-this.svg` and embedded in `README.md` (replaces the mermaid draft) + prose; `doctor.sh`'s devcontainer note now says SP modes are unavailable *here*, not unavailable — same onboarding thread as the line below — Clinton Chee, via Maurits Visser
- fix(docs/README): the "Git Bash, not WSL" warning now says explicitly that Docker Desktop's WSL2 backend (the "install Docker on WSL" prompt from `mxcli new`'s Dev Container) is fine and is the `devcontainer` lane, and that the lanes mix freely on one project with one writer on the `.mpr` at a time — a colleague onboarding via `mxcli new` read the warning as "say No to Docker on WSL" — Clinton Chee, via Maurits Visser

- fix(privacy): genericised a personal project code on CONTRIBUTING.md:97 that the local denylist flags but CI's did not, and gitignored `.claude/park/` — parked cycle notes name the live engagement and local paths by nature, and the leak guard scans untracked files, so they blocked every commit on the author's machine (the batch above had to land with `--no-verify`) — Maurits Visser, via the toolkit merge queue
- process(bug-logs): filed the pending upstream batch on mendixlabs/mxcli — 14 bugs + 1 feature request as #1023–#1037 (BUG-107, 109, 111, 112, 63, 67, 70/95, 73, 76, 84, 86, 87, Gallery CE0463, `widget init` docs, `mxcli new` Team Server app), bodies verbatim from `pending-github-issues/`, each draft's Status set to FILED. Dedupe before filing caught two drafts already up from an earlier session and never marked — BUG-82 is #917, BUG-110 is #1006 — marked, not re-filed; `create-association-corrupts-mpr` held back (unfilled version placeholder, open same-module question). BUG-107's PR waits on issue #1023 being approved — Maurits Visser, via the toolkit merge queue
- fix(skills): **`show message` is not a gap — two learned files said so for three weeks, in opposite directions.** `learned-popup-feedback-pattern.md` asserted "there is no MDL syntax for a show message/toast microflow activity anywhere" (concluded 2026-08-14 by reading two *skill files'* statement lists, never by probing the parser); `learned-microflow-patterns.md` at the same time carried the richer, half-right rule that it exists but generates CE0720. Retested 2026-09-04 on mxcli `4b58b89` / Mendix 11.13.0: `show message 'text' [type Information|Warning|Error];` parses, execs, and passes a **real mxbuild with 0 errors** — bare literal, concatenation and all three levels, in a probe microflow, plus a production microflow shipped the same day. CE0720 **does not reproduce**; the nanoflow workaround is now history, kept and stamped for projects on older binaries. The level-first form `SHOW MESSAGE WARNING 'text'` does **not** parse in either microflows or nanoflows — even though mxcli's own bundled `.ai-context/skills/write-nanoflows.md` uses it seven times and the binary embeds examples of it, while `mxcli syntax` documents the activity nowhere. The toast becomes Option C in the popup-feedback pattern — a Mendix workflow-and-agents POC project
- new(skills): `retesting-learned-rules.md` (baseline routing) — the discipline the above was missing. Fires before obeying any `learned-*` STOP or workaround **that costs a detour**, on a binary newer than the rule's stamp: version → `mxcli check` → exec and read the *mxbuild* verdict → drop the probe. Never conclude from documentation, from `strings` on the binary, or from one green `mxcli check`. Writing the result down takes four things — both version stamps, the verdict word in the first sentence, the evidence with its denominator, and the old rule kept underneath as history — and completion includes `grep -ril` across `skills/`+`bug-logs/` so a second file cannot keep contradicting the first — a Mendix workflow-and-agents POC project

- new(bug-logs): first upstream code contribution prepared — `pending-github-issues/bug107-fix.patch` (+ README): nil-safe mapping-expression accessor for both workflow `with (…)` builders, the not-mapped hint corrected to the quoted form, and a regression test; verified against mendixlabs/mxcli HEAD in a cloud container (baseline panic captured at both sites, `go test ./mdl/...` 35/35 after, real binary prints the syntax error instead of SIGSEGV). Held until the issue is approved, per mxcli CONTRIBUTING — Maurits Visser, via the toolkit merge queue
- new(bug-logs): five ready-to-file upstream issue drafts in `pending-github-issues/` for the 11.14 workflow-probe cluster — BUG-107 (unquoted `WITH` value segfaults `buildWorkflowCallMicroflow`), BUG-109 (`JUMP TO` in a boundary-event body writes a target-less Jump named after its target), BUG-110 (`DESCRIBE WORKFLOW` drops XPath quote-escaping, breaks round-trip), BUG-111 (`mxcli syntax` drill-down rejects its own help example), BUG-112 (`mxcli new` warns on an over-length path then hangs in the first build); BUG-108 not drafted, it is folded into BUG-76 — the toolkit merge queue, from a PLM approval-migration project's probe
- fix(privacy): genericised a client project code that had survived on master in two `project-bin/` headers and two changelog credit lines — surfaced by two contributors (PRs #8 and #9) whose local leak-guard denylist refused to commit beside it, so both had to `--no-verify`; a denylist hit on content you did not write is a report about master, not about your PR — the toolkit merge queue
- **Skills — `workflow-structure-rules.md` §11 + §6.** Probed the user-task *On created* handler (`onCreatedEvent`), the one construct §6 recommended while §11 had no row for it: **not expressible in MDL**, and grammar-level rather than a docs gap — after `PARAMETER` the parser accepts only `{BEGIN, EXPORT, DUE, OVERVIEW, DESCRIPTION, DISPLAY}` and four candidate spellings were rejected at parse. It **is** on the MCP write path (`json:"onCreatedEvent"` on the user-task payload, beside `taskPage`/`outcomes`/`boundaryEvents`, no `omitempty`), so it is a **per-task** property, not one workflow-level handler; end-to-end MCP write unverified, needing a live Studio Pro. Also **retracted the "decision on a boolean or free-text outcome is proven" row** — BUG-76's v0.20.0 retest corrupts on a literal `1 = 1`, so the condition never mattered; left struck through rather than deleted, because "a boolean decision is the safe kind" is the belief the table has to kill. From the PLM approval-migration probe project.
- **Skills — `workflow-structure-rules.md` §6.** Added the *targeting microflow returning a one-element list* shape for assignment-carried-in-data, alongside the Mendix MCP team's *On created* handler and marked as the usual preference: it is proven in MDL (§11) where the `onWorkflowEvent` slot has no §11 row at all — neither proven nor refused, because nobody has probed it. A fallback chain (nominee → eligibility list → logged backstop) also satisfies §6's empty-targeting rule structurally, without a separate error handler, and logs when it degrades where an XPath cannot. Fed back from the PLM approval-migration probe project, which hit this on a 19-station process with per-station nominations and two stations holding fewer than ten eligible users.
- fix(skills/workflow-structure-rules): withdraw the targeting-XPath conflict — §11 recorded the `[%UserRole_X%]` token form as proven while the header bullet and §6's tail note still told the reader it was unprobed and to prefer the three-segment path. Both forms are proven on v0.20.0 / 11.14.0; the MCP team's spelling was not wrong. Found by reading the file as a build input on a live approval rebuild — the exact use it is written for — a PLM approval-migration project
- process(stages): the workflow skill is now **read, not merely cited**, wherever a workflow definition is decided — `architecture-blueprint.md` Step 3b (read §1–§7/§9 before drawing; boundary events, event sub-processes and parallel splits drawn explicitly, targeting named per task with the sentence it came from, §12 counts recorded under the diagram), `brd-to-build-plan.md` (new "a module carrying a workflow gets a construct row" — every hand-add becomes its own numbered `RUN` row, never a footnote), `iterative-build-loop.md` pre-module checklist, `agents/mdl-agent.md` step 6a; routing row widened to stages **3**,4,5,6, `render-routing.sh --check` clean. `checkpoint-architecture.md` CAC-3 Q3 already asks the workflow-scope question, so both checkpoints were left alone as instructed — a PLM approval-migration project
- new(bug-logs): BUG-107 … BUG-112 from that probe. **BUG-108 is critical** — an enum `DECISION` accepts only the bare value form, which writes an `.mpr` the Mendix loader cannot open (`StorageLoadException`), while rejecting both forms Mendix requires; recovery is `DROP WORKFLOW`, since mxcli can still read what mxbuild cannot load. **BUG-107**: an unquoted `WITH` value segfaults the binary, and the "parameter is not mapped" hint prints the crashing spelling. Also BUG-109 (`JUMP TO` in a boundary body writes a targetless, name-colliding Jump), BUG-110 (`DESCRIBE WORKFLOW` emits unparseable MDL for a quoted targeting XPath), BUG-111 (`mxcli syntax` rejects the drill-down example in its own help), BUG-112 (`mxcli new` warns the path is too long, then hangs forever instead of failing — cost an unattended session ~10h) — a PLM approval-migration project
- learn(skills/workflow-structure-rules §11 + learned-workflow-patterns §6/§19 + build/workflow-example.mdl): **12-row construct probe run on mxcli v0.20.0 / Mendix 11.14.0**, one construct per script, three checks each. Newly **proven**: multi-user task (the activity — its decision method and completion timing have no grammar), `JUMP TO` in an outcome, non-interrupting boundary timer, `WAIT FOR TIMER`, `WAIT FOR NOTIFICATION` (takes no name), and the `[%UserRole_X%]` **token XPath form** — retiring §6's divergence. Newly **hand-add in Studio Pro**: event sub-processes (all four kinds), notification boundary events, interrupting boundary events, enum decisions. **`learned §19` corrected**: `boundary event timer 'P3D'` was recorded as proven on a `mxcli check` alone and fails native `mx check` with CE0117 — the duration is an expression, not an ISO period. Headline: **`mxcli check` was wrong on 5 of 12 rows**, three of them passing check, exec *and* the DESCRIBE round-trip while broken under `mx check` — a PLM approval-migration project
- fix(skills/workflow-structure-rules): validated against CLAUDE.md's authoring rules and reconciled with the Mendix MCP team's source. Six edits: a new **§12 "count it" checklist** (the file had no completion criteria at all — every row now ends on a denominator taken from the workflow itself); §5's enum-outcome rule extended to call-microflow-returning-enum and AI agent task and **separated from the dead-branch smell it was being conflated with** (`learned §16` is about empty outcome *bodies*, never licence to omit an outcome); §6 gains the **assignment-carried-in-data** mechanism (no targeting + an *On created* handler) which none of the four existing mechanisms covered; §8 gains the verbatim wrong expressions (`$currentUser/Name`, `$Order/Amount`) per authoring rule 7; §9 gains the operator-action → sub-process effects table; CE1834 now names `System.WorkflowUserTask` (`System.UserTask` does not exist and loops forever). Three conflicts with the source resolved **toward the probe, not the newer text**, and recorded in the file header: due dates (field §19 wins — no SLA, no timer), targeting XPath form, Empty-outcome scope — the Mendix MCP team, via a PLM approval-migration project
- **fix(gate-check): the Surface check now resolves bare filenames against `design/`, `architecture/` and `docs/`, not only the project root.** §2's Surface cells are inconsistent about paths — some carry one, some name the file bare — so `design-system.html`, `build-plan.html` and `wireframes/*.html` reported MISSING while sitting exactly where their own skills put them. That is the worst direction for this check to fail in: the Surface column exists because "gate PASS" was being read as "stage done" when the reviewable artifact had never been generated, and a checker that cries missing on present files trains the reader to ignore the line. Fixed in the reader rather than by rewriting the spec's prose cells. Found on a PLM approval-migration project whose Stage 3 and Stage 4 surfaces were both present and both reported missing.
- process(front-door): "Where you run this — detected, not asked" added to `CONVERSION-RUNBOOK.md` (one table: cloud container / devcontainer / local with Studio Pro, keyed on where the chat started; `doctor.sh` now detects the lane from `CLAUDE_CODE_REMOTE` / container markers / platform and prints what it changes, and the agent records `Environment: <lane>` in PROJECT.md — no up-front question), with pointers from README Quickstart, the README write-modes section and the runbook's Prerequisites. The cloud lane was field-proven since 2026-08-31 in `cloud-dev-environment.md` but the front door read local-first, so a fresh cloud session announced Stages 5–7 impossible in the container before probing anything — the front door now names the lane and the agent rule ("never conclude no-Studio-Pro = no build; probe"). Also: `cloud-dev-environment.md` gains the build-from-source fallback for containers whose policy 403s GitHub Releases while serving git clone and the Mendix CDN (verified on a Claude Code web container: `make -C mdl/grammar bootstrap/generate`, `make sync-all`, `go build` → working binary). At merge: lane detection re-verified on a second cloud container (all three Linux branches), doctor's cloud note now carries the `run --local` split-model-collapse warning it pointed past, and the runbook table cross-references the build-from-source fallback instead of calling Releases mandatory — a MOC/PSSR rebuild POC, merged by the toolkit queue

- new(skills): `workflow-structure-rules.md` — the Mendix engine's workflow constraints stated for an MDL author: every path ends once (CE6689 unreachable after a terminal), boundary-event type decides its terminator, event sub-processes live beside the main flow with recurrence limits and jump rules, parallel branches never end the instance (CE1844/CE1845/MW0012), outcome minimums and the mandatory Empty path on enum decisions, targeting derived from the sentence with empty-result failure handling, multi-user decision methods, and which edits break running instances. Translated from the Mendix MCP team's `workflow-common`/`workflow-update` skills; their model-SDK mechanics dropped on purpose (MDL and the CLI stay the writing path), one XPath-form divergence reconciled toward the field-proven form, and §11 marks which constructs are proven in MDL versus unprobed. Routed build/workflow; `learned-workflow-patterns.md` scope note points here — the Mendix MCP team, via Maurits Visser
- fix(check-pr-discipline): the BUG-number collision check now excludes headings that already existed at the PR's merge base — a retest or archive restructure rewrites an existing `## BUG-<N>:` line, which shows as added in the diff and read as "number taken". First field case: the v0.20.0 retest PR re-stamped BUG-97 and the check refused it. A collision is now only "absent at the merge base AND present on the base tip" — two sessions inventing the same number in parallel, the failure the check was born for — the toolkit merge queue
- learn(cloud-dev-environment): two gaps found on the first Claude Code web scaffold — the no-Docker build-gate route (`doctor.sh --install --yes`, which doctor prints but the skill never named) and the CRITICAL `mxcli run --local` split-model collapse (logged 2026-09-01, never cross-referenced from the one skill that mandates that command): snapshot before every local run, restore split format and `git status` before the next commit — a MOC/PSSR rebuild POC
- process(contrib): two halves of one proposal queued in `contrib/inbox/` — **the judged review's trigger** (`judged-review-verdict.md` + `bin/review-verdict.sh`: the verdict moves off the runner and onto the artifact as a `<meta name="review-verdict">` stamp, audited by a sweep that dates from filenames because a checkout rewrites every mtime, separates the pre-convention backlog from the finding, and warns without ever blocking the review that clears it) and **the UI measurement run** it certifies (`ui-measurement-run.md`: resolve the real scroll root before measuring or capturing, or an Atlas app silently crops every page to the viewport). Field evidence, one project, one day: 48 HTML reports under `docs/` and exactly 1 ever judged — the rule was known, it lived in prose and was wired into a single runner; and a measurement run reported as done had captured the top 900px of pages up to 2907px tall, so ~two-thirds of every page had never been in the evidence. Re-run with the scroll root resolved: 12 captures / 10 distinct pages / 3 personas, four P1s and twelve P2s over evidence that had previously read as clean. Carries its own correction — the first report accused the instrument of fabricating the height; it was reading `document.documentElement` correctly and the app scrolls an inner container — a Mendix workflow-and-agents POC project

## 2026-09-01
- new(skills): `preview-over-hub-tunnel.md` — the three traps between `mxcli run --hub` and a working public preview, each from a live debugging round: `--db-name` defaults to the .mpr name (boots against a database that does not exist), the Mendix runtime's REST client ignores JVM proxy properties so a GenAI call 403s with "Host not in allowlist" while the host was reachable the whole time (fix: `--runtime-setting http.proxyHost/Port`), and a leftover runtime on :8080 serves a stale build at a fresh URL — a deal-management PoC
- new(bin): `run-hub.sh` — wraps `mxcli run --hub` with the db-name and runtime proxy settings the skill documents; routing row + changelog added at merge (the branch shipped it README-only), and three merge-review fixes: `readlink -f` and `mapfile` replaced with bash-3.2/macOS-portable forms (the script's audience IS Mac users), and the hard-coded db password default replaced by reading the project's own `.docker/.env` (`RUNTIME_PARAMS_DATABASEPASSWORD`) — both caught by check-portability and the leak guard at merge, symlink-dedupe re-verified on a fixture — a deal-management PoC, merged by the toolkit queue
- fix(doctor): the machine-local receipt (`.claude/.doctor-receipt`) is now auto-gitignored by the script that writes it — one machine's receipt must never satisfy gate-check's doctor-ran probe elsewhere, and every wired project was tripping clean-tree hooks on an untracked file doctor itself created (field case: a stop hook demanding it be committed, TFC-TCXGraphPOC 2026-09-02); idempotent, verified twice-run on a scratch repo — the toolkit merge queue
- new(bug-logs): `pending-github-issues/feature-mxcli-new-teamserver-app.md` — feature request draft: `mxcli new --platform` should create the app on the Mendix platform (Team Server repo included) via the stored PAT, closing the one manual Portal click in the otherwise scriptable Team-Server-first birth (`handoff-to-studio-pro.md` §10); probed absent on v0.20.0, and the platform capability exists (Platform SDK `createNewApp`) — Maurits Visser & the toolkit merge queue
- learn(handoff-to-studio-pro): §10 rewritten around the simpler topology the maintainer proposed — create the app on Team Server FIRST, mirror to GitHub by clone-and-push, and the two remotes share ONE history, making every later sync ordinary git (branch, merge, push — no tooling); the model-token rule stays as the safety content since model units still never text-merge. `ts-sync.sh`'s content transplant demotes to the fallback for GitHub-born projects whose foreign history Team Server refuses. Probed mxcli v0.20.0 for the birth step: `mxcli new` is local-only, no platform-app creation exists in the CLI, so the Portal click stays — Maurits Visser & the toolkit merge queue
- learn(handoff-to-studio-pro): five additions from ideation grounded in this week's field work — §0 recipient runs `doctor.sh --install` before touching the model (environment failure dresses up as project failure); §2 now names `mxcli run --local` as the second consolidation trigger (the routine one); §8 demo data as committed Excel fixtures (the one seed route where data travels in git; Seed button shrinks to running import templates); §9 the round-trip loop (one model writer at a time, mxbuild gate on every re-entry, SP git-crash pointer); §10 GitHub-workshop/Team-Server-showroom topology — sync content, never histories — the toolkit merge queue & a deal-management PoC
- new(project-bin): `ts-sync.sh` — content-transplant bridge between a GitHub-side project and a Mendix Team Server clone (`status`/`push`/`pull`): snapshot commits with source-revision provenance, refuses consolidated sources and dirty trees, never pushes for you, never merges histories. **UNPROVEN against a real Team Server** — six fixture scenarios pass (push, pull, no-change, dirty refusal, consolidation refusal, status; test 5 caught a pipefail bug that turned the consolidation refusal into a silent death), first field run owed from a machine holding both clones; routed experimental until then — the toolkit merge queue
- fix(gate-check): `has_confirmed_decision` accepts "CONFIRMED <date>" — word-anchored at the start of the status field, not string-exact, so a register writing status-plus-date in one cell (eight real decisions read as "no CONFIRMED decision", logged as TD-07) now passes, while a Notes cell merely mentioning the word, or "NOT CONFIRMED", still does not; boundary cases replicated at merge — TFC-TCXGraphPOC, cherry-picked from the mandixie-workshop branch
- process(merge-queue): PR strategy codified after the first five-PR day (two bug clusters both numbered 96–100, three PRs missing changelog lines, two conflicted PRs with no CI signal, one half-redacted secret a plain merge would have published) — `CONTRIBUTING.md` gains the five merge-queue rules (fast merges, numbers assigned at merge via `BUG-DRAFT-<slug>`, changelog CI-checked, squash default on this public repo, a claim ships with its probe), and new `bin/check-pr-discipline.sh` enforces the two mechanical ones on every PR (wired into checks.yml, PR template updated). Field-proven against the day's own history: both rules fire on PR #4's original head (missing changelog + five taken numbers), changelog-only on PR #5's, clean on the fixed heads — the toolkit merge queue itself
- new(skills): `handoff-to-studio-pro.md` — taking a model built headlessly in a container to a person who can open, run and demo it. The organizing rule: the model travels, the demo data and runtime configuration do not. Every section is an incident: `mxcli docker build` consolidating 2049 units into one 43 MB `.mpr` (a recipient's first `git status` reads as catastrophic damage), seeding as an action not an AfterStartupMicroflow, wipes that must not delete Administration accounts, env-var config needing UI fallbacks, an agent with 9 available models and zero bindings answering nothing, and database dumps whose secrets are encrypted against a gitignored key — the dump and the key travel as a pair or not at all — a deal-management PoC
- fix(mendix-agent-setup): three amendments paid for during a live build, plus the OQL truncation trap — a deal-management PoC
- fix(page-fidelity): merged the annotation-chrome vocabulary from two parallel fixes — the generic `wf-*` prefix rule now also matches `<p>` carriers (wf-note rides on `<p>` at least as often as `<div>`), and the non-`wf-` apparatus classes (`section-h`, `anno-wrap`, `rail-note`, `annot`) are dropped from the denominator too — a deal-management PoC & a dashboard-publishing migration project
- new(skills): `mendix-agent-setup.md` — MxCloud key import, knowledge-base indexing, agent import and model binding via UI automation; no MDL/SQL path exists, so the Playwright-only route is documented end to end, closing with an agent-answers-a-question verification — TFC-TCXGraphPOC
- learn(agent-ui/testing-shape): how page context reaches (or silently misses) a bound agent, plus two register entries — a trial-license session-leak false RED and a clipped-transcript false GREEN (an assertion that passes on a truncated reply because it checked presence, never shape) — TFC-TCXGraphPOC
- learn(bug-logs): BUG-102 (`ALTER PAGE SET DataSource` silently no-ops on a native DataGrid — success reported, gate passes, XPath never changes; CREATE OR REPLACE PAGE is the reliable path), BUG-103 (`DESCRIBE MICROFLOW` doubled-quote log strings fail `check` round-trip), BUG-104 (quoted `"$Name"` parameter keeps the `$`, CE1613 at mxbuild), BUG-105 (duplicate widget-name registration via REPLACE/multi-root INSERT), BUG-106 (widget names stay burned after a rolled-back exec) — renumbered from 96–100 at merge to clear the collision with master's — TFC-TCXGraphPOC
- learn(ui-preflight/testing-shape): the silent-SUCCESS rule, symmetric to the existing silent-failure row — every commit/delete/mutate button must resolve to a navigation, a confirmation message, or a provably re-scanned data source; a journey step that verifies a write by reloading-then-querying is a correct persistence check and a total blind spot for the button's own response ("after clicking new dashboard I don't see anything happening") — a dashboard-publishing migration project
- learn(bug-logs): second trigger for the CRITICAL split-model collapse — plain `mxcli run --local` (not just `marketplace install`) collapsed a 78 KB `.mpr` + 438 tracked units to a 15 MB monolith on one app start; rule added: snapshot before every local run, `git status` before any commit that follows one — a dashboard-publishing migration project
- new(bug-logs): BUG-101 — an mxcli-authored Gallery widget fails headless mxbuild CE0463 and survives full page regeneration and a fresh element ID; `mxcli check`, `mx check` and `widget sync` all report clean on the exact defect. Filed as a draft upstream issue in `pending-github-issues/` (cross-owner `add_repo` blocked direct filing) — a dashboard-publishing migration project
- new(skills): `record-demo-video.md` — nine rules for a narrated screen-capture demo a human will actually watch, distilled from comparing three generations of the same project's recordings. The load-bearing finding: **Playwright's recorded video timeline does not track wall-clock time** — a lead-in measured at 13.0s of wall clock landed ~28s into the recording, so every clock-derived trim (four attempts) left a blank frame 0. Replaced with a clapper-board technique — flash a marker colour into the page and locate it in the decoded video — which surfaced two further gotchas of its own: Chromium's screencast submits frames on compositor activity, not on a timer, so an idle marker with no forced raster (`page.screenshot()`) never reached the recording at all; and a stream-copy `-ss` can only land on a keyframe, so a naive cut on the raw file put the clapper flash itself back at frame 0 — the precise cut has to happen on the re-encode pass. Also covers two-beat narration (an "intent" caption before a click, a "landing" caption after), lifting the app's own bottom chrome by the caption strip's measured height instead of covering it, and captioning environment-caused defects (a video player that only got as far as opening one domain of several an embedded YouTube player needs) rather than editing around them — a BJJ-training mobile app project
- fix(mdl-cannot-express): the pluggable-widget row had the right verdict for the wrong reason, and the wrong reason would have misled the next reader. Corrected against v0.20.0: a pluggable widget takes **scalar properties in parens and no body at all**, and every body form is rejected with `mismatched input '<keyword>' expecting '}'` — object-list keywords (`attribute`, `event`) and child-slot keywords (`tagcontentcontainer`) alike. The old text claimed `item a (…)` parses and fails at exec with a navigationlist message; that came from a probe with a malformed widget header and a wrong widget id — a dashboard-publishing migration project
- new(bug-logs): `pending-github-issues/widget-init-docs-do-not-parse.md` — **`mxcli widget init` generates MDL examples the parser rejects.** For every pluggable widget it writes an "MDL Example", a "Child Slots" table and an "Object Lists" table, explicitly for an agent to follow, and on v0.20.0 not one of the documented body keywords parses. Verified with four read-only `mxcli diff` probes; baseline (properties in parens, no body) parses, all three documented body forms fail identically. It cost a real feature — a sandboxed iframe needs `sandbox` and `srcdoc`, both entries in an object list, so the screen ships a labelled gap; the only parseable alternative, `tagContentMode: 'innerHTML'`, executes the uploaded document same-origin and was refused. **A generated document that does not parse is worse than no document**: it is authoritative-looking, written for an agent, and on disk from the first `widget init`. The skill now carries "the generated widget docs will tell you the opposite — do not believe them, probe" — a dashboard-publishing migration project
- learn(mdl-preflight): **STOP row 23 — `diff` before `exec`, and it is the cheapest guard in the file.** A new script declared `create or modify non-persistent entity ..."ViewerContext"` with one attribute, believing the name unused. It was not: an earlier page script owned it, carrying four attributes that are the entire datasource of the app's viewing page. `mxcli diff -p <app>.mpr <script>.mdl --format struct` — read-only, one second — reported `+ Attribute SharedCount` and **four `- Attribute` lines**: `create or modify` on an entity does not merge into the attribute set, it **replaces** it. Renaming took ten seconds; the alternative was a page rendering nothing on a model that passes `mx check` and mxbuild clean, because an entity that has lost four attributes is a perfectly valid model. Rules 21 and 22 say write `create or modify` and give each document one owner; this is the check that the name you picked is actually free — a dashboard-publishing migration project
- learn(mdl-preflight): the same field run adds the ordering half of rule 21. `create or replace page` is re-runnable but **not order-independent**: it discards every later modification to that document. A cutover script named its layout inline and was re-run after the script that assigned the brand layout, silently reverting one page to the stock Atlas bar. **The from-empty replay could not detect it** — in numeric order the cutover script runs first, so the replay produced the correct five pages while the live model had four. A replay proof only ever exercises one order, so it proves the scripts are complete, never that the live model matches them. The structural fix is to create shared documents (layouts especially) in the FIRST script so later scripts can name them directly — a dashboard-publishing migration project
- fix(gate-check): the run summary — the one line anybody quotes — counted STAGES only, so the last thing a reader saw was `Summary: 8 passed · 0 need attention` while three obligations sat PENDING and three artifacts sat owed-and-absent two screens above. That is how an `Obligation look FAULT` survived **days** of daily gate runs on a real migration: the guard fired correctly every single time, into the middle of a long report, and every summary quoted afterwards said only good news. The summary now carries `Obligations: N pending · N fault    Artifacts: N owed and absent · N fault` plus a pointer to the rows. It stays a POINTER and never touches the exit code — these are informational by deliberate decision, and what was broken was not that they failed to block, it was that they could be true and invisible at the same time. A checker that did not run reads `NOT CHECKED`, never `0`, because "0 pending" over a check nobody performed is the green-by-absence the whole mechanism exists to retire — a dashboard-publishing migration project
- fix(artifact-check): `_art_find` had `_ob_find`'s bug — first glob match, alphabetical, therefore OLDEST. It never changed a verdict (this check is file presence only) but it named a superseded file as the artifact satisfying the row: `Artifact ui-reviews PRESENT — ui-review-2026-08-31.html` on a project whose current review was from 09-01, so a reader opening it to see what the stage produced read last month's work. Now prefers the newest match within a pattern, while the earlier pattern still wins across patterns — the `|` list is a preference order the manifest author wrote deliberately, and recency must not reorder it — a dashboard-publishing migration project
- fix(agents): the placeholder rule at the top of all six agent templates was **self-falsifying**. It reads "if any `{{DOUBLE_BRACE}}` placeholder remains, refuse to proceed" — and that sentence contains the pattern, so a dispatched agent checking itself with the obvious `grep '{{'` matches the rule text and refuses on a stub that is perfectly complete. Caught live: dispatching a test-agent and an architect-agent for the first time on a real project, `grep -c '{{'` returned exactly 1 for all six generated stubs, which reads as "all six incomplete". The mechanical checkers were never wrong — `sync-project.sh:81` and `gate-check.sh:1125` both exclude the token and both document the exclusion as load-bearing — but the prose the *agent* reads never passed that knowledge on. Each rule now names the working command (`grep -o '{{[A-Z_]*[^}]*}}' <file> | grep -v DOUBLE_BRACE`) and says why the naive one is wrong. Verified across all six templates (placeholders correctly listed) and all six generated stubs in a real project (clean) — a dashboard-publishing migration project
- fix(obligation-check): the obligation checker read the **oldest** matching artifact, so a pass that was performed again — properly — could not clear its own finding. `_ob_find`'s `match=names` branch returned the first glob hit, and glob order is alphabetical, which for the `ui-review-<YYYY-MM-DD>*.html` schema means oldest first. A migration re-reviewed its module with five pages, five `PROOF-OF-LOOK:` citations and five committed captures, and `gate-check.sh` still printed `Obligation look FAULT — 0 of 1 discharged; NO PROOF-OF-LOOK`, because a superseded report from the previous day was the only file ever opened. **That is the worst shape a guard can take: the remedy it names does not clear it**, so the author adds more proof, watches nothing change, and learns the check is broken. `_ob_find` now returns **every** match newest-first and the consumer walks them, taking the first that satisfies all three disqualifiers (denominator, LOOK proof, staleness) — which also makes tie order harmless, since a tie no longer discards anyone. An older report can still only win if `_ob_stale` clears it, i.e. the model has not been built past it, so nothing is gamed by keeping old reports around — a dashboard-publishing migration project
- new(tests): `tests/retests/retest-obligation-newest-report.sh` — seven assertions over `_ob_find` (newest date beats a newer mtime, same-date pairs are both offered, ordering is deterministic, dateless falls back to mtime, the unit filter beats recency, empty files are skipped, `match=path` is untouched). Written after the first attempt at this fix shipped a call to `_ob_mtime`, **a function that does not exist** — masked by its own `2>/dev/null || echo 0`, so every file tied at 0 and only the date in the filename was really sorting. A green run over a fixture I hand-built would have said PASS; what caught it was grepping for the callee. The second attempt then sorted on sub-second mtime, which this container's ext filesystem stamps identically for files written milliseconds apart — the reason the fix above stopped trying to resolve ties at all — a dashboard-publishing migration project
- fix(tests): `retest-bug22-settings-writes.sh` resolved the java symlink with `readlink -f`, which is GNU-only and absent on macOS — caught by `check-portability.sh`, which CI runs on every PR, in the toolkit's own test tree. Replaced with a bounded symlink walk, verified to produce byte-identical output to `readlink -f` on a real two-hop `/usr/bin/java` — a dashboard-publishing migration project
- fix(mdl-preflight): **STOP rule 2 is retired** — `alter project security level`, `alter settings configuration` and `alter settings model` do NOT corrupt the Settings unit on mxcli v0.20.0 / Mendix 11.12.1, and every project has been sent to the Studio Pro GUI for a statement mxcli implements correctly. The rule dates from v0.13.0 / 11.12.0 Beta (BUG-22) and the toolkit's own consolidated log flagged it as possibly stale on 2026-08-06 — item 4, `BUG-LOCAL-05` passing on both forks — and nobody reconciled it for a month. Falsified with the bug report's OWN stated detector: it says the model fails to load "on the next SP open **or `mx check`**", and `mx check` runs headlessly. **18 executions, 0 errors on every one** — six level toggles through all three levels, both sibling statements, ten consecutive toggles on a model carrying 23 scripts of content, and a full `mxbuild --target=deploy` (BUILD SUCCEEDED), with the written values asserted to round-trip because a clean check over a lost write is not a pass. Honest limit: no Studio Pro in a Linux container, so the SP-open path is not independently exercised. Reported by the user of a dashboard-publishing migration, who was right — a dashboard-publishing migration project
- new(tests): `tests/retests/retest-bug22-settings-writes.sh` — the retest above, committed and re-runnable (`<mxcli> [content.mpr]`), so the next person confirms or reopens the rule in a minute instead of re-flagging it in a year. **The generalisable ask: every STOP row should name the command that would falsify it.** A "may already be fixed, unreconciled" note is a claim with no owner — too weak to change behaviour, too strong to ignore, so it survives — a dashboard-publishing migration project

## 2026-08-31
- learn(build-plan): PROVE-row note — `.test.mdl` suites are falsifiable only on mxcli ≥ v0.19.0 (`@setup`/`@verify`/`@cleanup rollback` were parsed-and-ignored before, so no `@verify` could ever fail); prefer them over journeys for data-shaped assertions, and read sudden failures of an old suite on a new binary as surfaced findings, not regressions — v0.20.0 container retest session
- learn(build-plan): BUG-22 retested and RESOLVED on v0.20.0 (security level / demo users / guest access / settings model / settings configuration all land, read back, cold-load clean) — preflight STOP rule 2 retired at ≥v0.20, `brd-to-build-plan.md` Step 5 gains a **Script 00 project-scaffold row** (layout, theme, PRODUCTION security level, guest/demo, navigation profiles — all scriptable at project start instead of end-of-build Studio Pro chores), runbook STOP diagram example updated; BUG-22 archived — v0.20.0 container retest session
- learn(skills): seven skills updated for v0.19/v0.20 mxcli capabilities — `learned-workflow-patterns.md` (CALL WORKFLOW describe round-trip fixed in v0.20 + positional form valid, both probe-verified; catalog blindness measured still present and kept as a warning), `bootstrap-project.md` (directory-shaped bundled skills, guarded re-init worth doing at ≥v0.20), `design-artifacts.md` (`--mxt-*` token export feeds `mxcli theme create --from <design-file>`, replacing the hand-built custom-variables bridge; dark-mode selector reconciled with mxcli's `:root.theme-dark` system), `ui-preflight-pages.md` (shell mismatch now a one-script repair via CREATE LAYOUT / ALTER PAGES SET LAYOUT), `learned-stylegallery.md` (project-owned App_Default), `existing-app-assurance.md` (marketplace drift-diff row), `iterative-build-loop.md` (v0.19 exec pre-flight refusal + MDL-ORDER01 is the gate working) — v0.20.0 container retest session
- new(bug-logs): upstream issue drafts for all 8 bugs confirmed still open on v0.20.0 — BUG-63/67/70+95/73/76/84/86/87 each get a ready-to-file draft in `bug-logs/pending-github-issues/` with v0.20.0 repro evidence; duplicate-checked against mendixlabs/mxcli issues (none filed yet; #828 cited as the microflow-only predecessor of BUG-86, #855 as BUG-84's non-covering sibling, #122 ruled out as a different mechanism)
- process(bug-logs): second archive pass, older resolutions — 8 entries resolved earlier than the v0.20.0 retest (BUG-20/21/27 and BUG-WF03/WF06 fixed in v0.17.0 upstream #854/#838/#836/#845/#846, the #835 DataGrid2-parameter-binding entry fixed in v0.17.0, BUG-24 superseded/misdiagnosed by BUG-WF06, BUG-28 reclassified as an enhancement request) appended to `bug-logs/archive-resolved-2026-08-31.md` under its own "older resolutions" section, stubs left in `mxcli-bugs.md`; still-open, PARTIAL, and fork-scoped-remainder entries (BUG-47/50/WF02/WF05, the Engalar-only sections, the cross-module half of the inline-association-set entry) left untouched
- process(bug-logs): archive consolidation for the v0.20.0 retest — the 17 resolved / not-reproduced entries (BUG-01/58/64/66/69/71/74/79/80/81/82/85/90/91/97, the GRANT multi-rule entry, the uppercase-`AND`+`!=` entry) moved wholesale to `bug-logs/archive-resolved-2026-08-31.md`, leaving the standard header+pointer stubs; `mxcli-bugs.md` slims from ~4.8k to ~3.8k lines, still-open and PARTIAL entries (incl. BUG-75) untouched
- learn(bug-logs): mxcli v0.20.0 retest (`bug-logs/mxlabs-v0.20.0-retest-2026-08-31.md`) — 27 log entries stamped against tag v0.20.0 on real Linux mxbuild 11.13: 13 resolved (BUG-01/58/64/66/69/71/74/80/82/85/90/91, GRANT additive/multi-rule via v0.19 #936), BUG-81 and BUG-97 not reproduced (write-count cap downgraded), 8 confirmed still open incl. BUG-76 workflow-DECISION corruption (byte-exact signature) and the BUG-70/95 show_page-args gap that v0.19's MDL-PAGEARG01 misses on page-level buttons; `learned-mdl-preflight.md` gains a v0.20 delta block (braced ALTER PAGE form, DataGrid2 derived column names, rule 17/1c retired at ≥v0.20) — retested in a disposable `mxcli new` 11.13 container project
- new(skills): `learned-mdl-cannot-express.md` — the short list of things MDL cannot write AT ALL (a file widget; any pluggable widget property that is an object LIST; a page variable a button writes), plus the four-minute Stage-3 probe that finds them before a wireframe promises one. Written because a migration shipped two of its three central screens with labelled gaps, both discovered at BUILD time — after the wireframes were signed off and the microflows behind them were finished and gate-clean. Carries the `item a (…)` case, which parses, passes `check --references`, and fails at exec with "item must be a direct child of navigationlist"; and the rule for the blocked case — a labelled gap on screen, a BLOCKED (not failed, not passed) step in the journey harness, and a statement of what IS built. Includes the one refusal worth naming: `tagContentMode: 'innerHTML'` would have rendered the blocked sandboxed frame the same afternoon, same-origin, undoing the security decision the frame existed for — visibly incomplete beats invisibly unsafe — a dashboard-publishing migration project
- fix(render-routing): a routing row whose `tier` is not one the renderer knows was routed into **zero** surfaces, silently, and `--check` reported "all skills routed or exempted" over it. The coverage audit could not see it — the skill HAS a row, so it is not an orphan — and every surface rendered "in sync" because none was ever asked to include it. One typo (`situational` for `ondemand`) makes a skill unreachable, which is exactly the failure this repo's own authoring rule 8 names. `--check` now validates the tier column and names the row; verified by reintroducing the typo and watching it fail — a dashboard-publishing migration project
- learn(detection-gaps): five rows from a real build, all in the "green through the gate, broken in the app" family — (1) `bin/exec.sh`'s mxbuild gate does NOT compile Java actions, so `cannot find symbol: variable Core` reported 0 errors on every exec and failed on the first `mxcli run`; (2) a Java action's generated import list follows its SIGNATURE, so an action taking only primitives gets `IContext` and `UserAction` and nothing else; (3) `String.valueOf(obj.getValue(…))` lets javac infer `T = char[]`, **compiles clean under a real build with Java compilation**, and throws `String cannot be cast to [C` on the first click; (4) a non-idempotent statement makes mxcli skip every statement below it while the gate stays green; (5) a bare `commit`/`delete` on a page with a microflow datasource — the register's mirror image, where the UI reports failure over a correct database — a dashboard-publishing migration project
- learn(mdl-preflight): STOP row 22 — any non-idempotent statement (`alter entity … add attribute`, bare `CREATE ASSOCIATION`/`CREATE MICROFLOW`) plus mxcli's non-transactional exec means the first failure **silently skips the rest of the script** while mxbuild reports 0 errors. Surfaced days later as a microflow quietly missing a call it had had an hour earlier, found by `DESCRIBE MICROFLOW` on a hunch. The fix is the same one rule 21 needs: one owner per document, `create or modify` throughout, and a from-empty replay to prove it (23/23 scripts, BUILD SUCCEEDED, model identical element for element) — a dashboard-publishing migration project
- learn(microflow-patterns): the `refresh` section gains its measurement — the same omission produced BOTH "the screen says nothing happened while the row is in the database" and "the screen still lists the row the database has deleted", on one screen, in one session; `delete $X refresh;` is the fix for the second. The point of the table is that a browser test alone calls both features broken and a database assertion alone calls both working: neither instrument is the check, the pair is — a dashboard-publishing migration project
- fix(check-page-shell): the `RenderMode:` match was case-sensitive while MDL is not, so a page whose title line read `rendermode: H2` was reported as having **no** title block — a false positive on a correct page, and the identical defect the file's own comment records having fixed once already for the CREATE/PAGE match. Same `-i`, same reason: a guard that cries wolf gets switched off — a dashboard-publishing migration project
- fix(page-fidelity): two scoring defects, both found by scoring a real first-build page. (1) Only three specific `wf-*` annotation divs were stripped, so a wireframe whose binding tables live in `wf-bind` had its own build checklist scored as page content the page had failed to reproduce — "heading: Binding annotations", "content: Mendix widget". Now any `wf-*` div is dropped, matching the class convention rather than a list of three. (2) `chromeSet` dropped the class `topbar` from the denominator but not the bar's CONTENTS, so `brand`, `logo`, `right` and `lang` scored as classes the page had failed to declare; the subtree is now dropped, **guarded** by "only when it contains no h1/h2" so a design that titles its pages from the top bar keeps being scored on that. Plus: the run-log root walk found no `.mpr` above `design/wireframes/` on a two-tree checkout and printed "score NOT logged" on the very project the score was for — it now honours `MPR_FILE` and falls back to an ancestor holding a subdirectory that holds the model. Measured on one unchanged page script: 39% -> 57%, classes 16/22 -> 18/18 — a dashboard-publishing migration project
- fix(project-bin): **the mid-session crash-net never existed on a two-tree checkout.** `snapshot-mpr.sh` globbed `$PROJECT_ROOT/*.mpr` and copied `$PROJECT_ROOT/mprcontents`; where the app lives under `app/` (the layout the cloud-run prompt prescribes) it matched nothing, created an empty timestamped directory, printed **`mpr snapshot ok`**, and pruned the older — equally empty — snapshots. Twelve model writes ran behind a net holding zero bytes. It surfaced only when a gate finally failed: `exec.sh` announced "restoring snapshot", found no `mprcontents/`, silently skipped the restore, then ran its whose-error-is-it re-check against the still-broken model and reported the failure as **PRE-EXISTING, "This script is NOT the cause"** — the opposite of the truth. New `find_model_dir()` in `_common.sh` resolves the model tree from the `.mpr` itself (honouring `MPR_FILE`); `exec.sh` and `restore-mpr.sh` use it for every `mprcontents/` path, which also retires the bogus "PROJECT IS IN v1 SINGLE-FILE FORMAT — Studio Pro WILL crash on open" banner that fired after every successful exec on a healthy model. `snapshot-mpr.sh` now **exits 1 rather than record a snapshot with 0 `.mpr` or 0 `.mxunit`** — an empty snapshot is worse than none, because the caller proceeds believing it has one. Field-run on a dashboard-publishing migration project: `1 .mpr + 394 units`; both layouts probed for snapshot and restore round-trip; `tests/wave2/test-bug07-08.sh` 28/28 — a dashboard-publishing migration project
- fix(open-questions): the literal status `UNRAISED` — the canonical vocabulary word and the documented default — fell through to rule 11 and reported UNRECOGNISED, the identical defect fixed for `ANSWERED` on 2026-08-12 two rules above. Both states block, so no gate ever wrongly opened; what broke was the feedback to the author (a correctly written status read as unparseable) and the counts (UNRAISED=0 beside a genuinely unraised question). Found on a hand-authored BRD — a dashboard-publishing migration project
- new(pipelines/node-express-react): layout is now CONFIGURED, not hardcoded — `config.json`'s `layout` block (defaults = the original RWA shape, existing runs unchanged) plus four capabilities a second real source needed: `export type X = {…}` aliases beside interfaces, SQL migrations → entities reconciled with the TS types (schema for constraints, TS for names, per-attribute `sql+ts`/`sql-only`/`ts-only`), router mount prefixes from `app.use`, and typed API-client modules so screens link to endpoints at all. `PIPELINE_CONFIG` lets a project keep its real paths outside this clone. Found because the unmodified pipeline extracted NOTHING from a same-stack source and reported only "models directory not found" — field-run on a dashboard-publishing migration source: 4 entities, 13 endpoints, 10 screens, all matching a hand-built ground truth — a dashboard-publishing migration project
- fix(source-sufficiency): the init file-discovery allowlist had no code extensions, so a migration source's codebase was invisible to the coverage inventory — same defect class as the 2026-08-19 .yaml miss, found live when a Node/Express+React source produced a 26-row inventory with zero .ts files; .ts/.tsx/.js/.jsx/.mjs/.cjs/.vue/.svelte/.java/.kt/.cs/.py/.go/.rb/.php now first-class — a dashboard-publishing migration project
- new(skills): `cloud-dev-environment.md` — setup and loop for developing in an ephemeral cloud container: one-time order (latest mxcli binary download → `mxcli init` → `init-project.sh` → explicit sources/ commit decision → push), the per-session re-establish/push loop, and Stage 5+ headless expectations (MxBuild CDN download, `--hub` URL, skipped-gate honesty); routed at stage P — a dashboard-publishing migration project
- fix(runbook): the "surface HTML is linked from `index.html`" done-checklist item was unsatisfiable — `gate-check.sh` regenerates the dashboard from scratch, silently wiping any hand-added link; the item now requires the link in `PROJECT.md`, which nothing regenerates — a PLM-workflow migration project
- learn(extractor-quality): a score whose ground truth is a single hand read is self-graded — label `"selfGraded": true`, never let it pass the 95% gate alone (field case: a 100% Stage 1 score whose Stage 2 re-read found two live defects, one recorded as the inverse of the source) — a PLM-workflow migration project
- process(unattended): unattended runs now keep a `## Rulings (unattended)` ledger in `PROJECT.md` — one `Ruling: what — why — cost if wrong` line per consequential self-made decision, gate and non-gate alike; the returning human reads the ledger instead of reconstructing choices from the diff — obra/superpowers (pattern only)
- process(skills): "Adding new skills" gains rules 6–8 — write for the trigger then stop, show the failure not just the rule, test retrieval against real phrasings — obra/superpowers `writing-skills` & Matt Pocock `writing-for-agents` (patterns only)
- learn(skills): `wizard-walkthrough.md` routed at the experimental tier — generate a paced confirm-and-verify bash walkthrough for human-only step batches (Stage 7 cutover, browser-only GitHub settings) instead of a prose checklist; explicit hypothesis awaiting its first field run — Matt Pocock's public `wizard` skill (pattern only)
- process(contrib): second harvest-triage round — all six queued inbox items dispatched: four promoted (above), three harvested defects retired as already fixed in the current tree (exec.sh mxbuild cache probe, exec.sh clean-build logging, design-audit exit codes), two instrument fixes kept queued awaiting a field environment (test-stack-up mxcli-local ownership, design-audit `--module`), render-don't-copy kept queued awaiting real-project testing — a PLM-workflow migration project
- new(doctor): `--install` consent gate — the plan is printed before anything downloads (each item with size, source URL and destination, the detected OS/arch so a wrong guess can be vetoed, and a company-policy escape hatch), then `[y/N]` at a terminal or an explicit `--yes` for unattended runs; no terminal + no `--yes` downloads nothing and just reports. `MXCLI_VERSION=vX.Y.Z` pins the mxcli release for standardised teams. Field-run on the Linux container: consent-refused run left zero files, `--yes` cold start fetched mxcli + the 822 MB toolchain end-to-end, idempotent re-run reports nothing-to-download
- new(doctor): `--install` now also fetches a missing project `./mxcli` first (right OS/arch asset from the mendixlabs/mxcli GitHub releases, `.exe` name on Windows for the Git Bash mapping), then continues straight into the toolchain download — one command from a bare machine to a running mxbuild gate; and every doctor WARN/FAIL now carries its remedy on the next line (core tools, Java, Node, project mxcli, incomplete clone were reported without one) — full cold-start field run on a Linux container against TFC-TCXGraphPOC: real 822 MB CDN download, real mxbuild 11.12.1 run against the .mpr capturing real CE errors in 31s; that run also confirmed the real 11.12.1 mxbuild has no `--version` (the `--help` fallback carried the probe)
- new(doctor): `--install <project-dir>` — when no runnable mxbuild is discovered, download the standalone toolchain locally via the project's own `./mxcli setup mxbuild` (the same download the headless container build uses, cached at `~/.mxcli/mxbuild/<version>/`); `find_mxbuild`/`find_java` in `project-bin/_common.sh` now discover that cache as a fallback after Studio Pro, so the `exec.sh` mxbuild gate runs from it too — no Studio Pro install required (Linux/containers included) — surfaced onboarding a colleague's machine (APAC PRS team)
- fix(doctor): mxbuild/mxcli probe no longer reads a failed `--version` as "cannot run" — the latest Studio Pro's mxbuild dropped that flag (field report: macOS, latest Studio Pro, 2026-08-31), so the probe now falls back to `--help` and only exit 126/127 or both flags failing counts as broken — Danny Liao (APAC PRS)
- fix(check-portability): the `stat -f` rule now accepts a GNU `stat -c` on the PREVIOUS line as well as the next — GNU-first is the recommended chain order (BSD-first never falls back on Linux), and the forward-only window flagged the graph-sweep fix itself, turning master's CI red for three pushes
- new(render-routing): coverage audit — every `skills/**/*.md` must have a routing row or an exemption with a reason in `bin/lib/routing-exempt.tsv`; unrouted skills and stale exemptions fail `--check` (CI-enforced), render mode warns but still renders (a guard must never block its own remedy). First field run found 6 unrouted skills: 3 tombstones exempted, and `full-harness-audit.md`, `test-result-audit.md`, `finding-disposition.md` — two with zero inbound references — now routed
- learn(bug-logs): BUG-98 (`calculated by` silently dropped at write time — attribute stored as plain stored value, BSON-verified; detection only by live retrieve), BUG-99 (import-mapping array-to-child observed empty — severity NOT established, two confounds unruled, discriminating test recorded in the entry), BUG-100 (default Compose project name `docker` collides containers/volumes across projects; fix `COMPOSE_PROJECT_NAME` per project, recommended `mxcli docker init` set it at scaffold) — a WMS conversion project & an exam-prep app project
- learn(skills): `learned-detection-gaps.md` — the 6-rung verification ladder plus a 9-row register of constructs that pass `mxcli check`/`exec`/DESCRIBE and fail at native `mx check`, Studio Pro, or live runtime; grounded in five independently confirmed field cases across four projects — a WMS conversion, a martial-arts-academy PoC, a product-provisioning PoC & an exam-prep app project
- learn(skills): orphan-skill promotion from a personal toolkit — `learned-dg2-patterns.md` (two contradictory grid skills reconciled: corruption belongs to `ALTER PAGE INSERT`, not the grid form; native-vs-pluggable decision rule), `security-is-not-a-later-script.md` (entity + grants one script; ready means `SHOW SECURITY MATRIX` proves it — fills the empty build/security group), `scriptable-sp-verification.md` (headless SP load capture; explicitly not a pass/fail oracle), `learned-local-db-confusion.md` (three local Postgres instances; resolve the port from the project's own compose file), `gate-check-file-locations.md` (Stage 0 files live at project root until Stage 1 exists) — a personal toolkit, via a WMS demo & a product-provisioning PoC project
- learn(mdl-preflight): enum values named with reserved words (`Receive`, `Move`, `Open`…) cannot be targeted by `alter enumeration … modify value`, quoting doesn't help — restate the full value set via `create or modify enumeration` — a WMS conversion project
- new(design): navigation layout (top-bar vs side-nav) is now a first-class branding-interview question in `design-artifacts.md` Step 0b — a never-asked layout is a missing interview step, not a silent default
- process(instruments): "Shipping an instrument" gains rules 6–7 for blocking guards — a guard must accept evidence it did not itself create, and must never block the action that resolves it (warn once, let it through)
- process(contrib): render-don't-copy agent-file distribution redesign queued in `contrib/inbox/` — sync's stub detector would have replaced ~33KB of completed agent config with templates; plus one shared copy-list for init/sync (9-vs-7 mismatch measured) — a personal-toolkit design note
- fix(graph-sweep): GNU-first `stat` try-order — on GNU, `stat -f` doesn't fail, it reads `-f`/`-t` as filesystem-info flags and prints plausible garbage, so the BSD-first fallback never fired and every Linux run FAULTed as "catalog is stale"; found independently on two projects (2026-08-19 and 2026-08-14), verified on a Linux field box — a PLM-workflow migration project & a martial-arts-academy PoC
- learn(bug-logs): BUG-96 (cross-module `ALTER PAGE … INSERT` of a DataGrid2 column writes malformed BSON — Studio Pro loader crashes, mxcli's own reader silently omits the column, only `create or replace page` clears it) and BUG-97 (~15th cumulative write-class exec op corrupts the whole project; recovery lesson: the `.mpr` alone is not a backup, `mprcontents/` is primary storage) — a martial-arts-academy PoC project
- process(contrib): first harvest-triage round — two project harvests triaged: graph-sweep fix and BUG-96/97 promoted (source drafts deleted per the same-commit rule), 7 remaining defect/process items queued genericized in `contrib/inbox/`; stale-install diffs identified as sync debt, not toolkit fixes
- learn(contrib): three steal-candidates queued in `contrib/inbox/` — generated interactive walkthroughs for human-only steps, a "Ruling:" decision ledger for unattended runs, skill-authoring meta-guidance — obra/superpowers & mattpocock/skills (public repos, patterns only)
- learn(ui-preflight): controlled A/B baseline of `ui-preflight-pages.md` (5 drafting reps per arm, same page) — skill inlined: 0 judgement violations across all reps; skill absent: every ToeicBuddy field defect reproduced. Delivery rule added (dispatch prompts must inline the file — a citation is not a read), stale class/gallery caches replaced with read-the-real-file instructions, page-column promotion path added (`process/preflight-skill-baseline-2026-08-31.md`) — ToeicBuddy
- process(skills): authoring rules for behaviour-shaping skills added to `CLAUDE.md` "Adding new skills" — baseline-test like an instrument, prohibitions vs recipes by failure type, denominators on completion criteria, no cached copies of another project's environment
- new(design): branding interview can borrow the public ui-ux-pro-max-skill style/palette/typography database as ideation input when the client has no brand — data only, never its CSS-framework implementation guidance (`design-artifacts.md` Step 0b)
- process(leak-guard): CI leak-guard step no longer hard-fails without a denylist — generic probes (emails, GUIDs, local paths) still gate every PR; the client-name check is the maintainer's personal local hook (`bin/install-hooks.sh` + gitignored `.leakguard-deny`), not a contributor requirement
- process(contrib): contribution pipeline — `contrib/inbox/` low-friction lane, `CONTRIBUTING.md`, PR template, CI checks on every PR, `bin/harvest-learnings.sh` to auto-draft inbox entries from a project's stored learnings; wired into `checkpoint-cutover.md` as the closing action so every project's wrap-up harvests
- process(leak-guard): init-project.sh ends with a three-line denylist nudge — new client at kickoff → add the name to .leakguard-deny and the LEAKGUARD_DENY secret; advisory only, never blocks
- process(changelog): this file, seeded from the last month; append rule added to `CLAUDE.md`
- fix(portability): `fixture-manifest.sh` and `report-disposition-check.sh` invoked a bare Python name — now resolve through `_common.sh` `require_py` (surfaced by promoting `check-portability.sh` to CI)

## 2026-08-28
- fix(page-scope): SHOW PAGES header rows with Excluded/Folder/Params columns were counted as pages — 6 real pages inflated to 13 (F-042, found in the MarkUseCase build) — Mark's session
- fix(e2e): two-tree layouts (`.mpr` under `app/`) now resolve — `project.config.template.js` root walk probes `app/`, `design-audit.js` probes beside the root; same bug as F-020, found twice (F-042 cluster)
- fix(verify-module): `page-scope.sh` wired in as a mandatory rung before the design audit — its artifact finally has a producer in the chain
- new(check-scripts): `bin/check-scripts.sh` parses every shipped script under its own interpreter at setup time (CRLF, bashisms, syntax); wired into `doctor.sh`, `init-project.sh`, `sync-project.sh`
- fix(design-audit): Windows-safe mxcli invocation — Node's `execFileSync` gets `.exe`/`.cmd` probes and an env override (MSYS maps bare names for bash, not for Node)
- process(shipping): "field-proof before merge" rule in `CLAUDE.md` — golden input captured never hand-written, both layouts, both platforms, one cited field run, a producer for every consumed artifact (root cause of the whole F-042 cluster)
- learn(theme): token port is not the component-CSS port — the theme-apply trap, from the MarkUseCase build
- new(page-fidelity): every score appended to the project's `docs/PAGE-FIDELITY.tsv`; first non-stub row per page is the first-build score of record vs the ≥80% target; `--stub` exempts declared forward-reference stubs
- process(install): `report-disposition-check.sh` declared noinstall — gate-check runs it from the toolkit clone — Mark

## 2026-08-27
- new(doctor): build-toolchain verification — mxbuild/java executed not just found (exit 126 = wrong-platform binary), Docker recommended for the self-verification stack
- new(page-fidelity): `project-bin/page-fidelity.js` — scored wireframe-fidelity instrument (headings/actions/content/classes, weighted), routed and installable; measured 32% median first-build on a real field corpus without it, 90% first-draft with it — a topbar-titled portal project's field runs
- new(check-page-shell): `project-bin/check-page-shell.sh` gates the drafted page shell (column, layout/nav, one H1) against the wireframe before exec — measured 0/10 on a real first build

## 2026-08-26
- learn(css): DROP MODULE removes no CSS — a model reset is not a styling reset — a topbar-titled portal project
- fix(design-audit): expresses its own failure instead of being stamped PASS by the caller
- fix(sync): `--upgrade-bin` accumulates instead of keeping only the last entry
- process(exec): module brief bound to the write as a build-plan row; `exec.sh` advises, never refuses
- fix(privacy): genericized a leaked client name in a test-result audit

## 2026-08-25
- fix(windows): unrun mxbuild gate on Windows; gate-check's O(n²) fork storm under Git Bash (152 ms/fork with Defender measured)
