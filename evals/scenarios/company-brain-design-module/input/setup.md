# Setup

Needs: a real design-system MPK (provided by the company), a scaffolded app with mxcli, the
toolkit checked out. Steps in `../rubric.md` → Setup. Run the control arm first (no `--company`),
then the treatment arm, same prompt, fresh session each.
