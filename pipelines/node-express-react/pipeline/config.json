{
  "_comment": "Set these paths after cloning — they point at YOUR local source workspace (never commit real paths). knowledgeBaseDir should point at analysis/<project>/knowledge-base per conversion-runbook.md's workspace convention, not inside the source checkout.",
  "sourceDir":        "<path-to-cloned-source-repo-root>",
  "knowledgeBaseDir": "<path-to-project-workspace>/knowledge-base"
}
