# <ComponentName>

**File:** `<vendor>.<ComponentName>.mpk`
**Kind:** widget | module | design-system module | theme
**Version:** <x.y.z>
**Mendix version range:** <e.g. 10.18 – 11.x> (tested on: <versions actually tried>)
**Namespace / module name once imported:** `<ModuleName>`
**Owner:** <team or person>
**Approved:** <YYYY-MM-DD> by <who>

## What it is for

<Two sentences: the problem it solves and the situation in which to reach for it.>

## Install — the CLI command that actually worked

<The exact `./mxcli ...` command and the mxcli version it was verified on. CLI first: this
toolkit builds headlessly. If the CLI could not do it on that version, say so plainly and name
the defect (bug id, missing subcommand) — a GUI step is a recorded limitation, never the
recommended path.>

## After import

- Entities/pages/microflows it adds that a build must not duplicate: <list, or "none">
- Settings or constants it requires: <list, or "none">
- Known conflicts (Atlas version, other components): <list, or "none">

## Proven in

| Project | Mendix | Date | Notes |
|---|---|---|---|
