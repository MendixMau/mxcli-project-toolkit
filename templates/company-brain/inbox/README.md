# inbox/ — the zero-friction drop

Copy `TEMPLATE.md`, paste what you have, commit. No quality bar; nothing routes here; nothing
may cite an inbox file. Triage weekly: promote into `skills/`, `components/`, `examples/`,
`proposals/` — or delete. `{{TOOLKIT_ROOT}}/bin/harvest-learnings.sh <project> --to <this dir>`
drafts entries from a project's bug logs, promotion tables and patched scripts.
