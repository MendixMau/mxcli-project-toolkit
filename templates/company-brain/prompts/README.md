# prompts/ — starter prompts for recurring big tasks

One `.prompt.md` per task family (full e2e run, full UI loop, existing-app audit, …). Each opens
with the prerequisites card the task needs (app reachable, a credential per persona, seed data,
the denominator, the pass bar) so the run does not discover them mid-way.
