# Promoted skills — pointers only

Each file below is a stub: the content lives, genericized, in the shared toolkit at
`{{TOOLKIT_ROOT}}/skills/<name>.md`. The stubs stay so that project routing rows that name these
paths keep resolving; a session that lands here reads the shared copy.

| Stub | Shared copy | Promoted | Note |
|---|---|---|---|
